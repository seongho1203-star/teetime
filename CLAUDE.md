# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 일할 때 보는 안내다.

## 사용자 지침

- **한국어 존댓말로 응답할 것.** 저장소 소유자는 한국어로 소통한다.
- **사용자는 개발자가 아니고 폰으로 일한다. 쉬운 말로 짧게 설명할 것.**
  전문 용어를 그냥 쓰지 말고(트리거·RLS 같은 말은 풀어서), 손으로 할 일은
  **번호를 매겨 한 단계씩** 적는다. 붙여넣을 것은 덩어리 하나로 준다 —
  작은 화면에서 여러 곳을 오가며 긁어모으는 일이 제일 번거롭다.
  화면에서 눌러야 할 곳은 `내 정보 → 알림 켜기`처럼 **누를 순서 그대로** 적는다.
- **수정사항은 `main`에 직접 커밋 & 푸시한다.** 별도 브랜치나 PR을 만들지 않는다.
  사용자가 "브랜치로 해줘"라고 명시할 때만 브랜치를 쓴다.
- **커밋과 푸시까지 묻지 말고 끝낼 것.** 일이 끝나면 커밋하고 푸시한 뒤 무엇이
  바뀌었는지 알린다. 다만 푸시는 곧 실서비스 반영이므로 **푸시 전에 확인할 것**
  (`npm run build`로 타입 검사, 헤드리스로 화면 확인).
- **훅은 걸지 않는다.** `.claude/settings.json`을 만들지 말 것.
- **`schema.sql`과 알림 함수는 이제 저절로 올라간다**(사용자 요청 —
  `저절로 하는 걸로 하자`). `main`에 밀면 `.github/workflows/deploy.yml`의
  `db` 잡이 **앱보다 먼저** 돈다. 아래 '밀면 DB부터 올라간다' 꼭지를 볼 것.
  **그러므로 SQL을 대화창에 붙여넣어 달라고 하지 말 것** — 그냥 커밋하면 된다.
  다만 **비밀값을 아직 안 넣어 두었으면 조용히 건너뛴다.** 그때는 예전
  방식이 그대로 필요하다: 사용자는 **폰으로** 일하므로 **한 번에 복사할 수
  있는 한 덩어리**로 내놓을 것 — SQL은 이번에 필요한 부분만 모아 다시
  실행해도 안전하게, 함수는 파일 전체를 그대로.
  **함수는 저장소를 스스로 보지 않는다** — 배포 단추만 다시 눌러 봐야
  예전 코드가 다시 올라갈 뿐이다. 이 점도 함께 알릴 것.

## 프로젝트 개요

**앱 이름은 `까꿍`이다** (저장소 이름만 `teetime`으로 남아 있다).
**앱스토어 목록에 뜨는 이름만 `까꿍데이`다** — 스토어 이름은 전 세계에서
하나뿐이어야 하는데 `까꿍`을 이미 누가 쓰고 있었다. **사용자가 앱 안 이름은
`까꿍` 그대로 두기로 정했다**(홈 화면 아이콘 밑 · 로그인 화면 · 알림창 ·
manifest). **어긋난 것이 아니니 맞추려 들지 말 것.**
**50~100인 골프 모임용 앱**(사용자가 정해 준 전제다). 라운드 모집·참가신청,
투표, 공지, 대화. **사람 수를 늘 이 규모로 놓고 볼 것** — 서른 명에서는
멀쩡하던 것이 백 명에서 부러진다. 조회는 통째로 받으면 안 되고(아래
'지난 것은 받지 않는다'), 사람 목록은 늘어놓지 말고 찾게 한다(`FIND_AT`).
카카오 오픈톡에서 투표와 인원 모집이 대화에 묻히는 문제를 없애려고 만들었다.

**형제 저장소 `JTFAG`와 헷갈리지 말 것.** 그쪽은 4인 모임의 스코어·정산 앱이고
빌드 도구가 없는 정적 파일 뭉치다. 이쪽은 완전히 다른 앱이다.

## 개발 명령어

```bash
npm run dev       # 개발 서버 (5173)
npm run build     # tsc -b && vite build — 타입 검사가 포함되어 있다
npm run lint      # oxlint
```

**화면을 눈으로 확인할 때는 `.dev/`의 도구를 쓴다.** Supabase 없이 가짜
데이터로 모든 화면이 뜬다 (네트워크 층에서 PostgREST를 흉내 내므로 앱 코드는
그대로 돈다).

```bash
npm run dev -- --port 5199 &
node .dev/shots.mjs                             # 13개 화면을 .dev/shots/ 에
node .dev/scroll.mjs '/#/rounds/r1' rd          # 한 화면을 위·가운데·아래로
node .dev/scroll.mjs '/#/rounds/r1' rd 320x693  # 넷째 인자로 화면 크기
node .dev/audit.mjs                             # 열여섯 화면을 두 크기로 훑어 검사
node .dev/scale.mjs 100 --shots                 # 100명·1년치로 통신량을 잰다
node .dev/behave.mjs                            # 눌렀을 때의 동작을 확인
node .dev/type-bench.mjs                        # 대화 입력칸이 한 글자에 얼마나 일하는가
node .dev/jank.mjs                              # 대화를 위로 훑을 때 끊기는가
node .dev/ktalk.mjs                             # 카톡과 같은 크기·배율로 찍어 잰다
node --experimental-strip-types .dev/groups-check.mts   # 조 나누는 규칙 (브라우저 없이)
```

**PostgREST 흉내는 `.dev/rest.mjs` 하나다.** 도구마다 복사해 두었더니,
앱이 `poll_votes(poll_id, option_id)`처럼 **딸린 표에서 칸을 고르기**
시작하자 옛 판이 그걸 못 읽어 **표가 0으로 나왔다** — 스크린샷만 보면
앱이 고장 난 것 같은데 실제로는 흉내가 뒤처진 것이었다. 조회 방식을
새로 쓰면 이 파일부터 맞출 것.

**`behave.mjs`는 화면에 안 나타나는 것을 본다** — 눌렀을 때 무엇을 보내나,
쓰기가 몇 번 나가나. 스크린샷으로는 안 보이고 조용히 깨지는 자리들이다
(읽음 쓰기가 한 마디마다 나가는 것, `다시 열기`가 아무 일도 안 하는 것).

**맨 끝에 `옛 스키마에서도 열리는가`가 있다.** 앱은 푸시하면 몇 분 뒤 바로
올라가는데 `schema.sql`은 사람이 손으로 붙여넣으므로, 그 사이에는 **새 표가
없는 DB에 새 앱이 붙는다.** 그때 새 표를 `unwrap`으로 읽으면 오류가 던져져
화면이 통째로 안 열린다 — 조 편성이 안 보이는 정도가 아니라 라운드 상세가
죽는다(실제로 그렇게 짰다가 여기서 잡았다). **새 표나 새 칸을 읽는 코드를
넣을 때마다 그 목록에 화면을 더할 것.**

**`audit.mjs`는 눈으로 놓치는 것을 숫자로 잡는다** — 열여섯 화면을 두 크기로
돌며 ① 가로로 삐져나온 것 ② 제 칸을 넘어 잘린 글자 ③ 30px보다 작은 누름 자리
④ 탭바에 가린 본문을 본다. 스크린샷으로는 `국민 123456-78-90…`처럼 잘린 계좌
번호를 그냥 지나쳤다. **일부러 자른 것은 세지 않는다** — `-webkit-line-clamp`과
**말줄임(`…`)**이다. 뒤를 `…`로 알린 자리는 잘린 줄 모르고 지나갈 일이 없다
(방 공지의 접힌 줄이 그렇다). 말줄임 없이 잘리는 것은 그대로 잡힌다.

키보드가 올라온 대화 화면은 `node .dev/kb-check.mjs`로 본다 — 헤드리스에는
키보드가 없어 가린 높이를 직접 넣어 흉내 낸다.

**`type-bench.mjs`는 '치면 좀 끊긴다'를 숫자로 바꾼다.** 100명·말풍선
120개를 쌓아 놓고 **글자 하나를 칠 때 다음 프레임까지 걸리는 시간**을 잰다 —
스크린샷으로도 `behave.mjs`로도 안 잡히는 자리다(화면은 멀쩡히 그려지고
보내는 값도 맞다). **한 프레임(16ms)을 넘긴 글자 수**가 눈금이다.
글칸을 손볼 때는 먼저·나중에 돌려 볼 것.

**여덟 자리를 본다** — 대화·회원 명단 찾기·모집 열기(골프장·안내)·공지
쓰기·투표 만들기·라운드 댓글·정산의 사람 찾기. **긴 목록 위에 얹힌 칸만
고른 것이다** — 가입·내 정보처럼 뒤에 목록이 없는 폼은 글자마다 다시
그려도 쌀 수밖에 없다. 새로 그런 칸을 만들면 `SPOTS`에 더할 것.

**이 도구는 서버에서 도므로 폰보다 빠르다.** 여기서 15.9ms였던 대화
입력칸이 실기기에서는 눈에 띄게 끊겼다 — **숫자를 합격선으로 읽지 말고
자리끼리 견주는 눈금으로 볼 것.**

**`jank.mjs`는 '대화를 위로 올리면 끊긴다'를 숫자로 바꾼다.** 방을 넷
(글만 · 사진 · 움짤 · 섞임) 만들어 놓고 위로 훑으며 **한 프레임(16ms)을
두 배 넘긴 프레임 수**를 센다. 첫 스크롤과 두 번째를 나눠 재는 것이 핵심이다 —
**첫 번째만 크면 '그때 처음 하는 일'이 범인**이고, 실제로 그림을 그때
받아 오는 것이 그랬다(아래 '미리 받아 두기').

- **CPU를 느리게 걸어 놓고 잰다**(`SLOW`, 기본 12배). 안 걸면 서버가 빨라
  넷 다 0으로 나와 아무것도 안 보인다.
- **한 번만 재지 말 것** — 실행마다 서너 개씩 튄다. 기본 세 번 평균이다(`REPS`).
  한 번씩만 재다가 **효과가 없는 고침을 효과가 있다고 잘못 읽었다.**
- `type-bench.mjs`와 같은 결이다 — 합격선이 아니라 **고치기 전후를 견주는 눈금**이다.

**화면 높이는 `node .dev/fit.mjs`로 잰다** (보통 390×844와, iOS `화면 확대`를
켠 320×693 두 크기). **보통 크기에서만 맞춰 두면 안 된다** — 저장소 주인 폰은
화면 확대가 켜져 있어, 보통 크기에서 딱 맞던 모집 열기가 거기서 79px 넘쳤다.

**다만 '한 화면에 들어가게'가 목표는 아니다.** 폰마다 크기가 달라
(375×667 ~ 430×932, 확대하면 그보다 작다) 어느 하나에 맞추면 다른 데서
어긋난다. 목표는 둘이다 — **여백이 헤프지 않을 것**, 그리고 **저장 단추가
어디까지 굴려도 손에 닿을 것**(`.form-actions`로 바닥에 붙여 둔다.
라운드 상세의 `.round-actions`와 같은 방식이다). fit.mjs의 숫자는 첫 번째를
보는 눈금이지 합격선이 아니다.

**`shots.mjs`의 `fullPage: true`는 sticky·fixed 요소를 엉뚱한 자리에 그린다.**
붙어 있는 것(신청 버튼, 탭바)을 판단할 때는 반드시 `scroll.mjs`를 쓸 것.
브라우저는 이미 있다 (`/opt/pw-browsers/chromium-1194/...`). `playwright install`을 실행하지 말 것.

## 구조

```
src/
  lib/        supabase 클라이언트 · 인증 · 조회 도우미 · 날짜 형식 · DB 타입
  components/ 여러 화면이 같이 쓰는 조각 (탭바·토스트·확인창·아바타·머리말)
  screens/    화면 하나당 파일 하나 + 같은 이름의 .css
  styles/     tokens.css(색·간격) → global.css(공통 클래스)
  public/     그대로 나가는 파일. 아이콘·이모티콘·서비스워커와,
              스토어가 필수로 요구하는 **웹페이지 둘**(아래)
supabase/
  schema.sql  테이블 · RLS · 함수. **DB의 유일한 원본이다.**
docs/설치.md   Supabase·카카오·Apple 로그인을 처음 붙이는 절차
docs/여러-모임으로-넓히기.md  밴드처럼 **여러 골프 모임을 담는 앱**으로
               넓히는 것에 대한 검토. **아직 안 정했다** — 다시 물어보면
               처음부터 따지지 말고 이 파일을 볼 것. 결론은 '큰 공사 전에
               다른 모임에 공짜로 줘 보고 쓰는지 확인한다'다.
docs/출시-전-할일.md  **앱(Capacitor)으로 감쌀 때 함께 해야 하는 것들.**
               웹으로는 못 하는 일이라 미뤄 둔 것이니, 감싸는 작업을
               시작하면 **이 파일부터 열 것.**
               **껍데기는 이미 폰에 깔렸다**(0-3번 — 아이폰 먼저 · 애플
               개발자 등록 · 맥 없이 클라우드 빌드 · `server.url`).
               **큰 순서를 사용자가 정했다 — 네이티브 일은 한꺼번에,
               맨 마지막에.** 지금은 웹만 고친다(밀면 폰에서 바로 보인다).
               앱 쪽을 건드리는 순간 한 바퀴가 30분이 되므로, 화면·기능·
               오류를 다 끝낸 뒤에 네이티브 것들을 한 판으로 묶는다 —
               **입력칸 네이티브화 · FCM 알림 · 알림음 · 출시용 전환.**
               그 파일 맨 위 '큰 순서' 꼭지에 무엇을 묶는지 적어 두었다.
```

### 밀면 DB부터 올라간다 — 손으로 붙여넣지 않는다

`.github/workflows/deploy.yml`이 `db → build → deploy` 세 잡으로 돈다.
**`build`가 `needs: db`인 것이 이 짜임의 전부다** — 앱은 밀면 몇 분 뒤
올라가는데 DB는 사람을 기다려서, 그 사이에 늘 **`새 앱 + 옛 DB`** 상태가
생겼다. 순서를 못박아 그 틈을 없앴다.

- **바뀐 것만 반영한다.** `supabase/schema.sql`이 바뀌었을 때만 SQL을
  돌리고, `supabase/functions/` 아래가 바뀌었을 때만 함수를 올린다.
  손으로 돌린 것(`workflow_dispatch`)은 둘 다 한다.
  - **견주는 것은 '직전 커밋'이 아니라 '밀어 올린 구간 전체'다**
    (`github.event.before` … `github.sha`, 그래서 `fetch-depth: 0`).
    `fetch-depth: 2` + `HEAD^ HEAD`로 두었더니 **커밋을 둘 이상 한 번에
    밀면 마지막 하나만 보여**, 앞 커밋에서 고친 schema.sql이 통째로
    안 보였다 — 이 잡이 없애려던 `새 앱 + 옛 DB`가 조용히 그대로 돌아온다.
  - **모르겠으면 돌리는 쪽으로 기운다.** 견줄 데가 없거나(갓 만든 브랜치·
    강제 푸시) 비교가 실패하면 둘 다 돌린다. 건너뛰면 DB만 옛것으로 남아
    **조용히** 어긋나는데, 그냥 돌리면 50초를 더 쓸 뿐이다(파일이 여러 번
    돌려도 안전하게 써 있다).
- **비밀값이 없으면 조용히 건너뛴다.** `SUPABASE_DB_URL` /
  `SUPABASE_ACCESS_TOKEN`이 없다고 **앱 배포까지 막히면 안 된다** — 그때는
  예전처럼 손으로 붙여넣으면 그대로 돌아간다.
- **SQL은 SQL 편집기와 같은 길로 돈다** — 위에서부터 한 줄씩, 오류가 나면
  거기서 멈춘다(`ON_ERROR_STOP=1`). **한 덩어리 트랜잭션으로 묶지 말 것**:
  사람이 손으로 돌려 확인해 온 것과 다른 길을 타면 여기서만 깨지는 자리가
  생긴다. 파일이 여러 번 돌려도 안전하게 써 있으므로(`create if not exists`
  165군데) 도중에 멈췄으면 고쳐서 다시 밀면 된다.
- **접속 주소는 pooler여야 한다.** GitHub은 Supabase의 `direct connection`
  으로 못 닿는다(IPv6). 못 붙으면 워크플로가 그 말을 오류에 실어 준다.
- **알림 함수는 `--no-verify-jwt`로 올린다.** 빼면 트리거가 부를 때 관문이
  401로 막아 **알림만 조용히 안 온다**(아래 '알림' 꼭지의 그 자리다).
  Edge Functions의 Secrets(`VAPID_*`·`NOTIFY_SECRET`)는 안 건드린다.
- **첫 앱관리자 이름·번호는 워크플로가 끼워 넣는다.** `schema.sql`에는
  자리표시자(`'[이름]'`)만 있는데, 그냥 돌리면 **SQL 편집기에서 손으로
  채워 둔 값이 매번 자리표시자로 되돌아간다.** 이미 올라간 사람의 등급은
  그대로지만 DB를 새로 세울 때 길이 막힌다. 그래서 Secrets의
  `SUPER_NAME`·`SUPER_PHONE`이 있으면 돌리기 **직전에** 끼워 넣는다 —
  **저장소 파일은 자리표시자 그대로다**(공개 저장소라 그 규칙이 먼저다).
- 넣을 비밀값과 화면 순서는 `docs/설치.md` 8번에 있다.

**그래도 `옛 스키마에서도 열리는가` 검사는 그대로 둔다.** 이 순서가 DB 쪽
틈은 없애 주지만, **폰에 옛 화면이 남아 있는 경우**(GitHub Pages가 문서를
10분쯤 물고 있다)는 그대로다 — 새 DB에 옛 앱이 붙는 반대 방향이다.

### 서버 코드가 없다 — 규칙은 전부 DB 안에 있다

권한도, 경합 처리도 Postgres가 한다. 화면은 요청만 한다.

- **`supabase/schema.sql`과 `src/lib/types.ts`는 짝이다.** 한쪽만 고치지 말 것.
- **`useRealtime`으로 구독하는 표는 `schema.sql` 맨 아래 발행 목록에도 있어야 한다.**
  빠져도 오류가 안 난다 — 그 표만 조용히 실시간이 안 먹는다. `polls`와
  `profiles`가 빠져 있어 투표를 지워도 탭 숫자가 그대로였고 승인해도 대기
  화면이 안 바뀌었다. 표를 새로 구독하면 그 목록에 넣을 것.
- 스키마를 고쳤으면 `schema.sql`을 고쳐서 **통째로 다시 실행**한다.
  여러 번 실행해도 안전하게 써 두었다(`create if not exists` / `drop policy if exists`).
- **`create table if not exists`는 이미 있는 표를 고치지 않는다.** 표 정의만
  바꾸면 새로 만드는 곳에서만 먹고, 돌아가고 있는 저장소는 그대로다.
  칸을 더하면 `alter table ... add column if not exists`를, **검사 규칙을
  바꾸면 `drop constraint` + `add constraint`를** 함께 적을 것.
  등급을 늘리고 이걸 빠뜨려 부운영자 임명이 `profiles_role_check`에 걸렸다.

### 신청은 반드시 `join_round`를 거친다

이 앱에서 가장 중요한 조각이다.

클라이언트에서 "지금 몇 명이지?"를 세어 보고 `signups`에 넣으면, 두 사람이
동시에 누를 때 둘 다 "자리 있음"을 읽어 **정원 4명짜리에 5명이 들어앉는다.**
그래서 `join_round`가 `rounds` 행을 `for update`로 잠근 뒤에 세고 넣는다.

**`signups`에 직접 insert하는 RLS 정책을 열지 말 것.** 지금은 읽기와 관리자
권한만 열려 있고, 그게 이 함수를 우회할 수 없게 하는 장치다.
취소(`leave_round`)와 강제 제외(`kick_signup`)도 같은 이유로 잠그고 처리한다 —
확정자가 빠질 때 대기 1번을 올리는 일이 겹치면 안 되기 때문이다.

#### 빈 자리를 채우는 규칙은 `promote_waitlist` 한 곳에 있다

**빈 자리를 세어 그만큼 올린다.** 취소 · 강제 제외 · **정원 늘리기** 셋이
이 함수를 같이 쓴다. 예전에는 취소와 제외가 각자 `대기 1번 하나만` 올리는
코드를 따로 들고 있었고, 그래서 **정원을 4명에서 8명으로 늘려도 대기자가
그대로 남았다** — 자리는 넷이나 비었는데 아무도 안 올라가니 운영자는
늘렸다고 알리고 대기자는 영영 대기였다. `rounds_capacity_grew` 트리거가
`when (new.capacity > old.capacity)`로 이 함수를 부른다.
**정원을 줄일 때는 아무도 안 뺀다** — 이미 확정된 사람을 앱이 말없이
내리면 영문을 모른 채 자리를 잃는다. 그건 사람이 `✕`로 할 일이다.

**대기에서 확정으로 올라가면 알림이 간다**(`🎉 자리가 났습니다`).
자리는 남이 취소할 때 나므로 본인은 아무것도 안 한 채 확정된다 — 이게
없으면 앱을 직접 열어 봐야 알고, 라운드 전날 자리가 나도 모르고 안 나오게
된다. 웹훅은 `signups`의 `after update`에 `when (old.state = 'waitlist' and
new.state = 'confirmed')`로 걸려 있다(`docs/설치.md` 7번).
**발송기는 `signups`에 `UPDATE`가 오면 그게 곧 '올라갔다'는 뜻으로 본다** —
라운드·투표의 `UPDATE`가 '다시 열림'인 것과 같은 규칙이라, 무엇을 가리는지는
트리거의 `when`에만 적혀 있다. 한쪽만 고치면 엉뚱한 때 알림이 간다.

**대기 신청이 들어오면 모집을 연 사람에게 알린다**(`🙋 대기 신청` · 사용자
요청). 자리가 없어 대기를 건 사람이 있다는 것은 **정원을 늘릴지 정할 사람**
에게 필요한 소식인데, 지금까지는 라운드에 직접 들어가 봐야 알 수 있어
대기자가 며칠씩 그냥 기다렸다. 지금 대기 인원도 함께 적는다 — 그게 곧
늘릴지 말지를 정하는 숫자라, 없으면 결국 앱을 열어 세어 봐야 한다.

- **`INSERT`인 것이 승격(`UPDATE`)과 갈리는 자리다.** 트리거는
  `when (new.state = 'waitlist')`로 대기만 고르고, **발송기도 같은 것을 한 번
  더 본다** — 한쪽만 고쳐도 조용하도록. **확정으로 들어오는 신청에는 안
  보낸다**: 자리가 남아 신청하는 것은 늘 있는 일이라 그것까지 울리면 모집
  하나에 폰이 열 번 운다.
- **연 사람 한 명에게만 간다.** 운영진으로 넓히지 말 것 — 정원을 만지는
  사람이 그 사람이다. 제가 열고 제가 대기를 건 경우는 뺀다.
- **`tag`는 신청 한 건마다 다르다**(`wait-<신청 id>`). 라운드로 묶으면
  둘째 대기자가 첫째를 알림창에서 밀어내는데, 대기자 둘은 **서로 다른
  사실**이라 하나로 덮으면 안 된다.
- **이 트리거만 `schema.sql`에 있다**(`notify_signups_wait`). 나머지 알림
  트리거는 `docs/설치.md` 7번에 있는데, 그것들은 **주소와 `NOTIFY_SECRET`이
  든 `notify_push()`를 함께 만드는** 자리라 공개 저장소에 못 둔다. 이건 그
  함수를 부르기만 해 비밀값이 없으므로 **밀면 저절로 걸린다**(사용자가 SQL을
  붙여넣지 않아도 된다). `notify_push()`가 없는 저장소에서는 `do $$` 블록이
  **조용히 건너뛴다** — 알림을 안 켠 곳에서 배포가 통째로 멈추면 안 된다.
  **알림 트리거를 새로 만들 때 이 방식을 쓸 것.**

#### 조 편성은 `set_round_groups` 한 번으로 저장한다

조 번호는 사람에 붙고(`signups.grp`, null이면 안 정한 것), **언제 공개됐는지와
조별 시각**은 `round_groups`에 라운드당 한 줄로 둔다. 화면은
`/#/rounds/:id/groups`(`screens/RoundGroups.tsx`)이고 **모집을 연 사람과
운영진**만 들어간다 — 라운드를 고칠 수 있는 사람과 같은 잣대이고, DB도
같게 막혀 있다.

- **표를 둘로 나눈 것은 알림 때문이다.** 열여섯 명을 배정하면서 `signups`를
  열여섯 줄 고치면 웹훅이 열여섯 번 울린다. 한 줄짜리 `round_groups`에
  걸면 저장 한 번에 알림도 한 번이다(`🚩 조 편성` → **그 라운드의 확정
  참가자에게만**, 짠 사람은 뺀다).
- **`p_grps`가 곧 전부다.** 목록에 없는 사람은 조에서 빠진 것으로 본다 —
  화면이 늘 확정자 전원을 실어 보내므로 '빼기'를 따로 만들 필요가 없다.
  **화면을 고칠 때 이 전제를 깨지 말 것**: 일부만 보내면 남이 조에서 사라진다.
- **바뀐 게 없으면 아예 안 쓴다.** 조를 짜다 보면 저장을 여러 번 누르게
  되는데, 그때마다 `round_groups`에 쓰면 그때마다 폰이 울린다.
- **아무도 조에 안 들면 편성을 걷어낸다**(`round_groups` 행을 지운다).
  그래야 `조 편성 지우기`가 되고, 지운 것이 알림으로 나가지도 않는다.
- **나누는 규칙은 `lib/groups.ts`에 있다** — 화면이 아니다. 넷이고 앞으로
  더 늘 수 있는데 화면 안에 두면 눈으로만 확인하게 된다.
  `node --experimental-strip-types .dev/groups-check.mts`가 브라우저 없이
  숫자로 붙들어 둔다(19가지).
  - **`신청 순서`** · **`랜덤`** · **`성별 조합`** · **`나이 조합`** 넷이고,
    `조 편성 조건` 자리에서 누르면 **바로 나뉜다**(`적용`을 따로 두지 않는다).
  - **`size`는 한 조의 최대 인원이다.** 아홉 명을 `4명씩`으로 나누면
    `4·4·1`이 아니라 `3·3·3`이다 — 골프에서 한 명이 혼자 도는 조는 없다.
    조 개수만 올림으로 정하고 사람은 고르게 흩는다(`groupSizes`).
  - **`성별 조합`은 적은 쪽을 먼저 흩는다.** 여자 둘·남자 여섯을 두 조로
    나눌 때 여자를 먼저 담아야 각 조에 하나씩 가고 `남남남여`가 된다 —
    남자를 먼저 담으면 한 조가 남자로 다 차 버린다.
  - **`나이 조합`은 뱀 모양이다**(`1 2 3 / 3 2 1 / …`). 나이순으로 세워
    놓고 라운드로빈으로 돌리면 1조가 늘 조금씩 더 나이가 많다 — 뱀 모양은
    그 치우침이 상쇄된다(여덟 명·두 조에서 조 평균이 정확히 같아진다).
  - **성별·태어난 해를 모르는 사람도 조에 들어간다.** 지금은 둘 다 필수라
    새로 비는 일은 없지만, 로그인 전이라 아직 못 받은 사람이 명단에 남을
    수 있다 — 빼면 그분들만 `미배정`에 남는다. 아는 쪽을 먼저 맞춰 놓고
    모르는 사람을 뒤에 고르게 채운다.
  - 화면은 **안 적은 사람이 몇인지 알려 준다**(`.grp-missing`). 안 알려
    주면 왜 이렇게 갈렸는지 물어볼 데가 없다.
  - 이름 옆에 `여 · 65년생`을 적는다 — 그 값이 안 보이면 손으로 옮길 때
    무엇을 깨뜨리는지 모른 채 옮기게 된다.
  - **켜진 조건을 표시하지 않는다.** 누르는 순간 나뉘고 그다음 손으로
    옮기므로, 표시를 남기면 '지금 그 규칙대로'라는 거짓말이 된다.
- **성별·태어난 해는 `profiles.gender`·`profiles.birth_year`다.**
  **DB에서는 비워 둘 수 있다**(`null` 허용) — 이미 있던 100명분 행을
  한꺼번에 채울 수가 없기 때문이다. **필수는 앱이 지킨다**: 가입 화면 ·
  `내 정보` · 로그인 뒤의 `FillProfile` 셋이 다 빈 값을 안 받는다.
  받는 자리는 **가입 화면 · `내 정보 → 프로필 수정` · 로그인 뒤 한 번**이고
  칸은 `components/GenderAge.tsx` 하나다. 값 검사(`birthValue`)는 화면이
  아니라 `types.ts`에 있다 — 화면 파일에서 함수를 내보내면 fast refresh가
  깨진다(`pollClosed`와 같은 이유).
  **나이가 아니라 태어난 해를 받는다** — 나이는 해가 바뀌면 틀린 값이 되고
  아무도 고치러 오지 않는다.
  예전에는 조 편성 화면만 명단을 넓게 받았지만, **이름표가
  `83/신성호/광산구`가 되면서 모든 화면이 그 칸들을 받는다**
  (`GroupPerson`은 이제 `Person`과 같다).
- **끌어서 옮기지 않는다.** 손가락으로 끄는 것은 폰에서 잘 안 잡히고
  헤드리스로 확인할 수도 없다. 사람마다 조 번호 칩을 눌러 옮기고,
  **조가 여섯을 넘으면 고르는 칸으로 바뀐다** — 칩이 두 줄로 접히면 이름이
  밀려 누구를 옮기는 중인지가 안 보인다(320px에서 실제로 그렇다).
- **빈 조 하나를 늘 더 보여 준다.** 그 빈 칸이 곧 `새 조 만들기`라, 단추를
  따로 두지 않는다. 빈 조에는 시각 칸을 안 띄운다.
- **조별 시각은 시·분만 받는다**(`type="time"`). 날짜는 라운드가 이미 들고
  있고, `datetime-local`은 `2026. 09. 12. 오전 07:30`처럼 길어 조 제목 줄을
  통째로 먹었다. 저장할 때 라운드의 한국 날짜에 붙인다.
- 라운드 상세에서는 조별로 묶어 그린다. **내 조는 작은 표(`내 조`)로만
  알린다** — 칸을 통째로 분홍으로 칠해 봤는데, 분홍은 '지금 눌러야 할 것'
  자리라 명단이 그 색을 덮으면 뜻이 무너진다.
- 조가 정해진 뒤에 대기에서 올라온 사람은 `미배정`으로 뜬다. 이걸 안 보이게
  하면 조가 하나 빈 채로 나간다.

#### 라운드가 코앞이면 알린다 — 이 앱에서 유일하게 시각으로 도는 일

**골프는 새벽에 나간다.** 몇 시인지·몇 조인지 보려고 앱을 열어 라운드까지
들어가야 했고, 그래서 깜빡하고 안 나오는 일이 실제로 생겼다.

```
pg_cron (10분마다) → queue_round_reminders()
      ↓ 때가 된 라운드만
round_reminders 에 한 줄  → notify_push() 트리거 → Edge Function → 폰
```

- **두 갈래인데 종류마다 하나씩이다**(사용자가 정했다).
  `day_before`는 **필드만**, 전날 **한국 시각 20시가 지나서** 한 번 —
  아침에 오면 이미 늦으므로 **저녁**이다.
  `soon`은 **스크린만**, 시작 두 시간 전. **스크린에 전날 알림을 다시 붙이지
  말 것** — 저녁에 잠깐 모이는 자리라 하루 전에 알려 봐야 그날 하루를 다
  보내고 잊는다. 그래서 한 라운드가 두 번 울리는 일은 없다.
- **한 라운드에 한 번만 간다** — `unique (round_id, kind)`가 그 자물쇠다.
  크론이 10분마다 돌아도 두 번째부터는 `on conflict do nothing`이라 조용하다.
  **넣는 것이 곧 보내는 것**이라, 이 표에 줄이 들어가면 알림이 나간다.
- **`p_now`는 시험 때문에 있다.** 이 함수가 하는 일의 전부가 '지금이 언제인가'라
  지금을 못 바꾸면 전날 20시 규칙을 확인할 길이 없다. 크론은 인자 없이 부른다.
- **사람마다 본문이 다르다**(발송기의 `Note.bodyBy`). 같은 라운드라도 조와
  조별 시각이 사람마다 달라서, 한 문구로 보내면 `조 편성을 확인하세요`밖에
  못 적는다 — 그러면 결국 앱을 열어야 해서 보내는 뜻이 반쯤 없어진다.
  조를 안 짠 라운드나 아직 배정 안 된 사람에게는 그냥 공통 문구가 간다.
- **확정 참가자에게만** 간다. 안 가는 라운드가 몇 시인지는 아무도 안 궁금하다.
  아무도 확정이 아니면 조용히 끝난다.
- **취소된 라운드는 뺀다.** 마감(`closed`)은 뺄 것이 없다 — 모집을 닫았을 뿐
  그날 라운드는 그대로 있다.
- **`round_reminders`에는 읽기만 연다.** insert를 열면 아무나 줄 하나로
  100명의 폰을 울릴 수 있다(`signups`에 직접 insert를 안 여는 것과 같다).
  넣는 것은 `security definer`인 그 함수뿐이다.
- `tag`는 `remind-`로 모집 알림(`round-`)과 갈라 둔다 — 그 모집과 별개로
  읽어야 할 소식이다. 두 갈래끼리는 같은 tag다: 전날 알림을 아직 안 지웠으면
  두 시간 전 알림이 그 자리를 대신하는 것이 맞다.
- **pg_cron을 처음 켜는 자리다.** 안 켜져 있으면 Supabase 화면의
  **Database → Extensions**에서 `pg_cron`을 켜고 `schema.sql`의 9번만 다시
  돌리면 된다. `cron.schedule`은 같은 이름이면 덮어쓰므로 여러 번 돌려도
  일이 두 개가 되지 않는다.

#### 회원 명단의 `올해 7회`

**세는 것은 DB가 한다**(`attendance_counts`). 화면이 신청 기록을 통째로 받아
세면 100명·1년치가 수백 KB인데, 여기서 세면 사람마다 한 줄이라 몇 KB로 끝난다.

- **지난 라운드만 센다**(`tee_at < now()`). 신청해 둔 앞으로의 라운드는 아직
  나간 것이 아니다. 취소된 라운드와 대기자도 안 센다.
- **연도는 한국 날짜로 정한다** — 기기 시간대를 따르면 새해 첫날 해외에 있는
  사람에게만 작년으로 세어진다.
- **함수가 없는 저장소에서는 그 줄을 아예 안 적는다.** 오류를 빈 목록으로
  넘기면 모두가 `올해 0회`가 되어 **거짓말이 된다** — `.dev/behave.mjs`의
  옛 스키마 칸이 그걸 지킨다(보호 줄을 빼면 실제로 빨갛게 뜬다).
- **참석 횟수는 모두에게 보인다.** 누가 꾸준히 나오는지는 감출 것이 아니고,
  연말에 개근을 챙길 때도 쓰인다. 같은 줄의 전화번호·차량번호만 운영진 몫이다.

#### 명단 차례 고르기

**`이름` · `나이` · `지역` · `성별` 넷이다**(사용자 요청). 100명 명단은
화면 여덟 장이라, 나이나 지역으로 묶어 보고 싶은 자리가 실제로 있다.
규칙은 `sortPeople()`(Members.tsx) 하나에 모여 있다.

- **기본은 이름 가나다순**이고 `fetchProfiles()`도 이름으로 받아 온다 —
  누구를 찾을 때 먼저 떠올리는 것이 이름이라서다. 기억해 두지 않으므로
  나갔다 오면 이름순으로 돌아온다.
- **모르는 값은 늘 뒤로 보낸다.** 태어난 해나 거주지역을 아직 안 적은
  사람이 맨 앞에 몰리면 정렬이 고장 난 것처럼 보인다 — `null`은 빈 글자나
  0이 아니라 **모른다**는 뜻이라 순서에서 빼는 것이 맞다.
- **같은 값끼리는 이름순이다.** 안 그러면 다시 그릴 때마다 줄이 뒤바뀐다.
- `나이`는 **연장자가 앞**이다(태어난 해가 이른 순).
- `성별`은 **남 → 여 → 모름**이다. 그 줄에 성별이 글자로 안 적혀 있어
  (얼굴 테두리 색으로만 갈린다) 모아 보려면 차례밖에 길이 없다.
- **예전의 `참석`을 걷어낸 자리다**(사용자 요청 — `참석말고 성별로
  변경해줘`). 참석 횟수는 **줄마다 `올해 N회`로 그대로 보이므로** 차례까지
  그것으로 매길 자리가 아니었다. 그 칩만 세는 함수가 있을 때 나왔는데,
  성별은 명단에 늘 실려 오므로 **이제 넷 다 늘 나온다.**
- **거른 뒤에 줄 세운다.** 반대로 하면 찾기로 좁힐 때마다 차례가 다시
  잡히는 것처럼 보인다 — 순서는 그대로고 몇 줄만 빠져야 한다.
- 차례를 매기는 것은 **회원 묶음뿐**이다. `가입 신청`과 `추방`은 그대로다.
  (`.dev/behave.mjs`에서 잴 때 셋 다 `.member-row`라 **안 갈라내면 대기자가
  섞여 들어와** 앱이 멀쩡한데 검사가 빨개진다 — 고정 자료로 걸러 낸다.)
- 칩 모양은 라운드 목록의 종류 가리개(`.kind-chip`)와 같은 값이다.
  **한 화면에 칩이 두 벌 서지 않으므로** 생김새를 나누지 않았다.

### 가입은 운영진 승인제 · 직책은 넷 + 일반회원

`pending`(대기) · `member`(일반회원) · `treasurer`(총무) · `staff`(부운영자) ·
`admin`(운영자) · `superadmin`(앱관리자) · `banned`(추방).
**앱관리자는 앱을 만든 사람 한 사람**이고, 그 아래로 운영자 → 부운영자·총무 순이다.
말은 `ROLE_LABEL`·`TITLES`·`ROLE_TAG`(types.ts)에 모아 두었다.

- `is_member()` — 회원 이상. 읽기의 문. **직책을 늘리면 이 목록부터 고칠 것.**
  직책을 넷으로 늘리면서 여기를 빠뜨려, **앱관리자와 총무가 라운드 신청·투표를
  못 했다**(`join_round`·`cast_vote`가 이 함수를 직접 본다). 읽기는 멀쩡해
  보였는데 `rounds_admin` 같은 운영진 정책이 따로 열어 주고 있었기 때문이다 —
  **읽히니까 괜찮다고 넘기지 말 것.**
- `is_admin()` — **운영진**(부운영자·운영자·앱관리자). 이름은 그대로 두었다 —
  정책 스무 군데가 이 이름을 쓰고, 부운영자가 하는 일이 운영자와 같기 때문이다.
  **총무는 여기 안 든다** — 돈만 만지고 승인·강퇴는 안 한다.
- `is_owner()` — 운영자 이상. **부운영자·총무 임명**이 이 사람 몫이다.
- `is_super()` — 앱관리자 한 사람. **운영자 임명**만 이 사람 몫이다.
- `can_settle()` — 총무 + 운영진. 정산을 만들고 금액을 고칠 수 있는 사람.

**임명은 한 계단씩만 올라간다.** 부운영자가 남을 운영진으로 올리지 못하게
`profiles_staff_upd`가 양쪽을 본다 — `using`은 고치기 전 행(운영진 행은 손 못 댐),
`with check`는 고친 뒤 행(운영진으로 못 올림). 운영자도 같은 방식으로
`profiles_owner`가 `role not in ('admin','superadmin')`을 양쪽에 건다 —
운영자가 다른 운영자를 만들거나 앱관리자를 건드릴 수 없다.
화면에서 버튼을 감추는 것만으로는 부족하다.

**첫 앱관리자는 이름과 전화번호로 저절로 정해진다**(`claim_superadmin`).
앱을 처음 설치했을 때 아무도 승인해 줄 사람이 없는 문제를 푸는 자리다.
**`AFTER` 트리거여야 한다** — RLS의 `with check`는 `BEFORE` 트리거가 끝난
**뒤에** 검사하므로, `BEFORE`에서 `role`을 올리면 `profiles_self_upd`에 걸려
막힌다. `security definer`라 표 주인 자격으로 도는 `AFTER` update가 유일한 길이다.
전화번호는 `regexp_replace(phone,'[^0-9]','','g')`로 숫자만 남겨 견준다.

**이 트리거는 행이 새로 들어오거나 저장될 때만 돈다 — 이미 있는 행은
안 훑는다.** 그래서 기능을 만들기 전부터 있던 사람은 SQL을 돌려도 그대로
`pending`이고, **앱에서 `내 정보 → 저장`을 한 번 눌러야** 그때 검사가 돌아
직책이 붙는다. 실제로 저장소 주인이 여기서 한 번 멈췄다 — 고장이 아니라
아직 안 걸린 것이니, `앱관리자 없음`을 보면 **이것부터 확인할 것.**

**진짜 이름·번호는 저장소에 없다.** `schema.sql`에는 자리표시자로 두고
실제 값은 SQL 편집기에 붙여넣을 때만 채운다 — 이 저장소는 공개다.
(한 번 실제 값이 커밋된 적이 있다. 지금 파일에서는 걷어냈고, **예전 기록에
남은 것은 저장소 주인이 그냥 두기로 했다** — 다시 꺼내지 말 것.)

카카오나 Apple로 로그인하면 트리거가 `profiles` 행을 만들고 `role='pending'`이
된다. `is_member()`가 false라 아무것도 못 읽는다. 이게 이 앱의 문이다.

- **`profiles`의 정책 안에서 `profiles`를 직접 조회하면 무한 재귀가 난다.**
  그래서 `is_member()` · `is_admin()` · `my_role()`을 `security definer`로 두고
  정책은 그 함수만 부른다. 이 고리를 끊는 장치이므로 없애지 말 것.
- 본인 행은 `pending`이어도 읽을 수 있어야 한다 — 그래야 `승인 대기중` 화면이 뜬다.
- **행이 없으면 앱이 다시 만든다**(`createPending` in auth.tsx + `profiles_self_add`).
  로그인 트리거는 `auth.users`가 처음 생길 때만 돈다. 운영진이 명단에서
  지운 사람은 카카오 계정이 남아 있어 트리거가 안 돌고, 프로필도 없어
  **어느 화면에도 못 들어가는 상태**가 됐다. 정책은 `role='pending'`만
  허용하므로 이 길로 등급을 올릴 수는 없다.
- 스스로 `role`을 올리지 못하게 `profiles_self_upd`의 `with check`가
  `role = my_role()`을 본다.
- **추방(`banned`)은 행을 남겨 두는 것이 곧 막는 방법이다.** 지우면 위의
  `createPending`이 다음 로그인 때 되살려 신청이 또 들어온다. 남겨 두면
  `is_member()`가 false라 아무것도 못 보고, 스스로 등급도 못 바꾼다.
- **회원 탈퇴(`delete_me`)는 그 반대다 — 계정까지 지운다.**
  **스토어가 요구하는 자리라 없애지 말 것**: 계정을 만드는 앱은 그 계정을
  **앱 안에서** 지울 수 있어야 한다(애플 심사 5.1.1(v), 자동 검사에도 걸린다).
  문은 `내 정보 → 회원 탈퇴`이고 **로그아웃 바로 아래**다 —
  메뉴 속에 숨기면 `찾기 어렵다`로 반려될 수 있다.
  - **지우는 일은 DB가 한다.** `auth.users` 한 줄을 지우면 프로필·신청·표·
    반응·읽음·정산 몫·알림 구독이 `on delete cascade`로 딸려 간다.
    화면이 표를 하나씩 지우면 중간에 끊겼을 때 **반쯤 지워진 사람**이 남는다.
  - **대화 글과 그가 연 라운드·공지는 남는다**(`on delete set null`).
    남의 대화가 중간중간 비면 읽을 수 없게 되고, 라운드는 다른 사람들이
    이미 신청해 둔 자리다(카톡에서 나간 사람 말이 남는 것과 같다).
  - **확정 자리는 비는 즉시 메운다** — 안 그러면 자리는 비었는데 대기자가
    그대로 남는다. `leave_round`가 푸는 그 문제라 `promote_waitlist`를
    같이 쓴다. **지우기 전에 어느 라운드였는지 적어 두어야 한다.**
  - **화면이 하는 것은 DB가 못 하는 둘뿐이다** — 이 기기의 알림 등록을
    끊는 것(`disablePush`)과 **저장소의 사진 파일**을 지우는 것.
    **순서가 있다: 알림 → 사진 → 계정.** 계정을 먼저 지우면 그다음 줄이
    권한을 잃어 **사진이 저장소에 영영 남는다.**
    앞의 둘은 실패해도 그냥 넘어간다 — 나갈 길이 막히면 안 된다.
  - **앱관리자는 못 나간다.** 한 사람뿐이라 나가면 운영자를 임명할 사람이
    없어진다.
  - `.dev/behave.mjs`의 `회원 탈퇴` 칸이 **순서까지** 붙들어 둔다
    (뒤집으면 실제로 빨갛게 뜬다).
- **첫 앱관리자를 손으로 만드는 길도 남겨 두었다** (`docs/설치.md` 5번 방법 A).
  위 트리거가 안 도는 저장소에서 쓴다.

#### 로그인 길은 카카오와 Apple 둘이다

**`Apple로 계속하기`는 애플 심사 4.8 때문에 있다.** 카카오 같은 **남의
로그인만** 쓰는 앱에는 맞먹는 로그인을 하나 더 내놓으라고 요구한다 —
규정 글에 카카오가 이름으로 적혀 있지는 않지만 **한국 앱이 이걸로 반려된
사례가 많다.** 그래서 넣었다. **빼지 말 것.**

- **둘이 한 코드를 쓴다**(`startOAuth` in supabase.ts). 돌아올 곳·앱에서
  바깥 브라우저로 여는 것·오류를 던지는 것이 똑같고, 갈리는 것은
  **공급자 이름과 scopes뿐**이다. 두 벌로 두면 한쪽만 고치게 된다
  (앱에서 웹뷰를 안 옮기는 `skipBrowserRedirect` 줄이 특히 그렇다).
- **단추 크기가 같아야 한다.** 애플은 `Apple로 로그인`을 다른 로그인만큼
  **눈에 띄게** 두라고 못박아 두었다 — 작게 밀어 두면 그것으로 반려될 수
  있다. `.kakao-btn`과 `.apple-btn`이 크기 규칙을 함께 쓰고 색만 각자다.
  `.dev/behave.mjs`가 두 단추의 크기를 재서 붙들어 둔다.
- **애플에는 `scopes`를 안 준다.** 애플은 이름을 **맨 처음 허락할 때 딱
  한 번만** 주고 그다음부터는 안 준다 — 그 한 번을 놓치면 되받을 길이
  아예 없으므로 **애초에 안 받고** 앱이 직접 묻는다. 그래서
  **`needsProfile()`이 닉네임도 본다** — 이름이 빈 사람은 명단에서 누군지
  알 수 없어 운영진이 승인할 수도 없다. `FillProfile`이 그때만 닉네임 칸을
  하나 더 띄운다(카카오로 들어온 사람에게는 안 보인다).
- **이메일이 가림 주소(`…@privaterelay.appleid.com`)로 올 수 있다.**
  앱은 이메일을 안 쓰므로 아무 상관이 없다.
- **사용자가 등록을 해야 살아난다** — `docs/설치.md` **3-3번**.
  그 전까지는 단추가 보이지만 누르면 오류가 난다. 앱(iOS) 쪽에는 손댈
  것이 없다 — 웹과 같은 길로 바깥 브라우저를 거치므로 `entitlement`도
  필요 없다.

**가입 때 받는 것은 여섯이고 다 필수다** — 사용자가 정해 준 차례 그대로
**닉네임 · 전화번호 · 태어난 해 · 성별 · 차량번호 · 거주지역**이다.
칸 차례를 임의로 바꾸지 말 것. 성별·태어난 해는 조 편성의 `성별 조합`·
`나이 조합`이 보고, **태어난 해와 거주지역은 이름표에도 들어간다**(아래).

**전화번호와 차량번호는 운영진만 본다**(사용자 요청). RLS는 줄 단위라 한 표
안에서 칸만 가릴 수가 없어 **표를 나눴다** — `profile_private(id, phone, car)`,
정책은 `본인` 또는 `is_admin()` 둘뿐이다(총무는 안 든다).
칸 단위 권한(`grant select (칸)`)으로도 되지만 그러면 `select('*')`가 통째로
막혀 **앱이 칸 목록을 하나 빠뜨리는 날 조용히 깨진다.**
- 받는 곳은 `fetchContacts()` 하나다. 회원이 불러도 정책이 **자기 한 줄**만
  돌려주므로 화면에서 감출 필요가 없다 — 애초에 안 실려 온다.
- 저장은 `saveMyProfile()` 하나다(두 표에 나눠 쓴다). **`profiles`를 먼저
  쓴다** — 첫 앱관리자를 가리는 트리거가 `profile_private`에 걸려 있고 이름은
  `profiles`에서 읽기 때문이다.
- 내 것은 `useAuth().contact`에 들어 있다(프로필과 **나란히** 받아 시작이
  안 느려진다). `profile.phone`은 이제 없다.
- **`claim_superadmin`이 `profile_private`으로 옮겨 갔다.** 이름만 고치고
  전화번호를 안 건드리면 안 돈다 — 그때는 다시 저장하면 된다.
- **차량번호가 보이는 곳은 회원 명단 하나뿐이고 운영진에게만 보인다**
  (사용자 요청). 예전에는 라운드 참가자 줄과 `내 정보` 머리말에도 적었는데
  둘 다 걷어냈다 — 남의 차량번호가 여러 화면에 흩어질 이유가 없다.
  **되살리지 말 것.** 내 것은 `내 정보 → 프로필 수정`을 열면 칸에 들어 있다.

**화면에 적는 이름은 `83/신성호/광산구`다**(`personLabel` in types.ts) —
태어난 해 뒤 두 자리 · 닉네임 · 거주지역. 100명 모임에서는 닉네임만으로 누군지
모른다는 것이 사용자가 짚어 준 이유다.
- **모르는 조각은 그냥 뺀다** — `신성호/광산구`도 `신성호`도 정상이다.
  빈칸을 `//`로 남기면 고장 난 것처럼 보인다.
- **`@언급`은 이 값을 쓰지 않는다.** 거기는 `name` 그대로여야 한다
  (`lib/mention.ts` · 발송기의 `mentionedIds`). 부르는 말은 닉네임이다.
- **문장 안에서도 안 쓴다** — `박승수님이 걷습니다` · `○○님을 승인했습니다`
  같은 줄은 닉네임 그대로다. 긴 이름표는 **목록의 이름 자리**에만 쓴다.
- 이름이 길어지면서 좁은 화면(320px)에서 잘리는 곳이 넷 나왔다.
  `node .dev/audit.mjs`가 잡아 준 자리이니, 이름표가 들어가는 줄을 새로
  만들면 **그걸 돌려 볼 것**: 회원 명단·투표 상세·참가자 줄은 글자를 한 톤
  낮췄고, 투표 카드의 '누가 골랐나' 줄은 두 줄까지 펴게 했다.

**남녀는 얼굴 테두리 색으로 가른다**(사용자가 고른 방법이다. `--male`·
`--female` · `.avatar.av-m`/`.av-f`). 이름표가 이미 길어 글자를 더 붙일
자리가 없었다. **이 두 색은 얼굴에만 쓴다** — 뜻이 있는 색(분홍·잔디·노랑·
빨강·보라)과 섞이면 안 되므로 토큰을 따로 두었다. `border`가 아니라
`box-shadow`인 것은 26px 얼굴의 그림이 줄어들지 않게 하려는 것이다.
성별을 모르면 테두리가 없고, 색만으로 가르지 않도록 `aria-label`에 함께 적는다.

**명단(`Person` · `fetchPeople()`)이 넓어졌다** — `gender`·`birth_year`·
`region`이 이름표와 테두리에 쓰이므로 모든 화면이 받는다. 100명에서 35.7KB →
38.5KB로 늘었고 한 달 어림은 1.20GB 그대로다(`node .dev/scale.mjs 100`).
대신 `car`가 빠졌고, 조 편성만 넓게 받던 것(`GroupPerson`)이 없어졌다.
**칸이 없는 저장소에서는 좁은 목록으로 물러난다**(`fetchPeople`) — 안 물러나면
명단을 받는 화면이 **전부** 죽는다. `.dev/behave.mjs`의 `옛 스키마` 칸이
그때 진짜 PostgREST처럼 400을 주고, 보호 줄을 빼면 여섯 건이 빨갛게 뜬다.

**이미 승인된 분들은 가입 화면을 다시 안 본다.** 그래서 로그인 뒤 앱에
들어가기 직전에 한 번 막고 받는다(`screens/FillProfile.tsx`,
`App.tsx`의 `Gate`). 적고 나면 다시는 안 뜬다.
**`needsProfile()`은 성별·태어난 해·거주지역 셋을 본다.**
**`null`과 `undefined`를 반드시 갈라야 한다** —
`null`은 '칸은 있는데 안 적음'이고 `undefined`는 **DB에 그 칸이 아예 없는
것**(스키마를 아직 안 돌린 저장소)이다. 후자에서 막으면 저장도 안 되는
화면에 **회원 모두가 갇힌다.** `.dev/behave.mjs`의 `옛 스키마에서도
열리는가`가 그걸 지킨다 — 보호 줄을 빼면 실제로 네 건이 빨갛게 뜬다.
차량번호는 **골프장에 미리 차를 등록할 때** 쓴다 — **회원 명단에서 운영진에게만**
보인다(다른 화면에는 아예 안 나온다).
**카풀 때문이 아니다**(사용자가 바로잡아 준 것이니 되돌리지 말 것).
거주지역은 **여덟 글자까지**다(`REGION_MAX` · DB의 `profiles_region_check`).
자유롭게 적게 두되, 길게 적으면 이름표가 줄을 통째로 먹는다.
`profiles.handicap`은
쓰지 않지만 예전 값이 남아 있어 칸만 남겨 두었다.
프로필 사진은 `내 정보`에서 올리고 Storage의 `avatars` 통에
`{uid}/{시각}.jpg`로 들어간다 — **파일 이름에 시각을 넣는 것은 주소를
바꾸려는 것이다.** 같은 이름으로 덮으면 브라우저가 옛 사진을 계속 보여 준다.

**카톡 프사는 가입할 때 딸려 온다** — DB의 가입 트리거(`schema.sql`)와
`createPending`(auth.tsx)이 `raw_user_meta_data ->> 'avatar_url'`을 그대로
`profiles.avatar_url`에 넣는다. **계정이 처음 만들어질 때만 도는 자리라**
이미 있던 사람은 카톡에서 프사를 바꿔도 안 따라온다(가입할 때 한 번 복사해
둔 주소다). 그건 고장이 아니니 손대지 말 것.

- **그 주소를 그릴 때 `https`로 올린다**(`Avatar`의 `https()`).
  **카카오는 `http://k.kakaocdn.net/…`으로 준다.** 웹에서는 문서가 `https`라
  브라우저가 알아서 올려 받아 줘서(mixed content auto-upgrade) 여태 그냥
  떴는데, **앱에서는 문서가 `capacitor://localhost`라 그 올림이 없고** iOS가
  http 통신을 통째로 막아(App Transport Security) 그림만 조용히 실패했다 —
  **앱으로 옮기자마자 얼굴이 다 글자로 바뀐 것이 이것이다**(사용자 제보 —
  `앱으로 빌드한 후 프사가 글씨로 나왔던거야`). 웹에서는 멀쩡했으므로
  **`웹에서 되니까 괜찮다`로 넘기지 말 것.**
- **DB를 고치는 길로 가지 말 것.** 넣는 자리만 고치면 그 뒤로 가입하는
  사람만 맞고 이미 `http://`로 저장된 회원 전부는 그대로 글자다.
- **`NSAllowsArbitraryLoads`로 열지 말 것** — 앱 전체의 http를 여는 일이고,
  애플 심사에서 사유를 대야 한다. 주소 한 글자를 고치는 쪽이 맞다.
- **깨진 주소를 담는 것은 참·거짓이 아니라 그 주소다**(`bad`). 참·거짓으로
  두면 사진을 바꿔 올린 뒤에도 표가 남아 새 사진이 멀쩡한데 글자로 보인다.
- `.dev/behave.mjs`의 `카톡 프사는 https로 올려 받는다` 칸이 붙들어 둔다 —
  **헤드리스로도 그대로 잡힌다**(보러 간 주소가 곧 앱에서 막히느냐를 가른다).

### 화면은 앱 아이콘에서 나왔다

선글라스 낀 골프공 스티커 — 흰 공, 잔디 초록, `까꿍`의 분홍, `day`의 노랑,
두꺼운 검은 테두리. 그 다섯을 `tokens.css`로 옮겼다.

**모자에 있던 남의 상표(`Titleist`)는 지웠다.** 스토어에 올릴 때 아이콘에
남의 상표가 있으면 반려 사유가 된다. 지금 모자는 글자 없는 흰색이다 —
**상표가 있는 옛 그림으로 되돌리지 말 것.** 파일은 넷이고 모두 같은 그림에서
나왔다: `icon-192` · `icon-512` · `apple-touch-icon`(180) ·
`icon-maskable`(512, **80%로 줄여 흰 바탕 가운데**에 둔 것 — 안드로이드가
아이콘을 동그랗게 잘라도 그림이 안 잘리게 하는 여백이다).
하나만 바꾸면 자리마다 다른 아이콘이 뜨므로 **넷을 함께 만들 것**
(첫 화면에 박아 둔 그림까지 세면 **다섯**이다 — 아래 스플래시 참고).

**만드는 방법은 정해져 있다.** 원본에서 흰 여백을 걷어내 그림만 남기고,
정사각 흰 바탕 가운데에 **보통 아이콘은 95%, `icon-maskable`은 80%**로 앉힌다.
큰 자리(1024)에서 만들어 마지막에 각 크기로 줄이고(LANCZOS), **256색으로
줄여 저장한다** — 스티커라 색 수가 적어 눈에는 그대로인데 파일이 1/3이 된다
(512는 246KB → 88KB). 줄인 뒤에는 **바깥 테두리가 순백인지 확인할 것**:
디더링 잡티가 한 점이라도 남으면 흰 바탕 위에서 네모난 자국으로 보인다.

**바탕색 셋이 다 흰색이어야 한다** — manifest의 `background_color`,
`.boot`의 `background`, 그림 자체의 바탕. 하나만 어긋나면 앱을 열 때
그림 둘레에 다른 색 네모가 스쳐 지나간다(`background_color`가 `--bg`였을
때 실제로 그랬다). `theme_color`는 상태 막대 몫이라 `--bg` 그대로 둔다.

- **밝은 화면이다.** 골프장 햇빛 아래에서는 어두운 화면이 안 보인다.
  예전에는 짙은 남색이었다. 어두운 테마는 없다 — 하나만 잘 만든다.
- **글꼴은 `Pretendard` 하나다**(사용자 요청). 제목에 쓰던 `Jua`(통통한
  손글씨)를 걷어냈다 — 제목과 본문이 서로 다른 글꼴이면 숫자와 영문의
  모양·굵기가 줄마다 어긋난다. Pretendard는 한글·영문·숫자가 한 벌이다.
  `--font-display`는 이름만 남아 같은 글꼴을 가리킨다 — 제목에 굵기로 무게를
  준다. **`dynamic-subset`을 쓴다** — 화면에 실제로 나온 글자만 받아 오므로
  한글 웹폰트인데도 몇십 KB로 끝난다. 통짜(`Pretendard.css`)로 되돌리지 말 것.
  **`.dev`의 확인 도구는 이 글꼴을 끊는다** — 화면마다 밖에 다녀오면
  열두 장 찍는 데 몇 분이 걸린다. 글꼴까지 보려면 그 route를 잠시 빼면 된다.
- **미는 손짓은 '뒤로 가기' 하나뿐이다**(`lib/tabs.ts`의 `useBackSwipe`).
  상세 화면에서 **오른쪽으로 밀면 뒤로 간다**(사용자 요청 — 아이폰의 그
  손짓과 방향이 같다). 탭 화면에서는 뒤로 갈 데가 없어 안 받는다.
- **탭 사이를 미는 기능은 없다. 두 번 넣었다가 두 번 다 걷어냈다 —
  되살리지 말 것:**
  - 처음에 뺀 까닭은 **아이폰에서 좌우가 다르게 느껴진다**는 제보였다.
    원인은 우리 코드가 아니었다 — 코드는 양쪽이 대칭인데, 아이폰은
    **왼쪽 가장자리에서 미는 '뒤로 가기'에만 사파리가 제 애니메이션을
    붙여 준다.** 그 한 방향만 부드러워 나머지가 더 도드라진 것이다.
  - 앱에는 그 손짓이 없어 대칭이 되므로 다시 넣었는데, 실기기에서 써 보고
    **사용자가 지웠다**(`탭바 슬라이딩은 삭제해줘. 뒤로가기는 놔두고`).
    탭을 옮기는 일은 탭바가 맡는다. **탭 사이는 미끄러지지도 않는다** —
    `tab-slide-*` 규칙도 함께 걷어냈다.
  - **세 번째로 물어보고 또 안 넣기로 했다**(사용자 질문 — `탭메뉴
    이동하는것도 뒤로가기처럼 만들수있어?`). 뒤로 가기가 손가락을
    따라오는 것은 **떠날 때 찍어 둔 앞 화면 그림**이 있어서인데,
    **탭은 아직 안 그린 화면으로 가는 것이라 끌고 나올 그림이 없다.**
    길이 셋뿐이고 셋 다 값이 있다 — ① 옆 탭을 진짜로 그린다(그 탭의
    조회가 다 나가고 대화면 읽음까지 찍힌다. 통신량 규칙에 정면으로
    걸린다) ② 지난번 그림을 끌고 나온다(처음 가는 탭은 빈 채로 밀리고,
    대화처럼 자주 바뀌는 탭은 끝에서 한 번 다시 그려지며 깜빡인다)
    ③ 뒤를 비워 둔다(허전하다). **사용자가 ①을 듣고 안 넣기로 정했다.**
    다시 물어보면 처음부터 따지지 말고 이 세 갈래를 그대로 내놓을 것.
  - **가로로 미는 손짓에 이미 임자가 있는 자리는 통째로 넘긴다**(`taken`):
    `.chat-row`(왼쪽으로 밀어 답장) · 글칸 · 덮는 창, 그리고 **가로로
    굴러가는 것**(이모티콘 탭 줄 등). 마지막 것은 클래스로 적지 않고
    **실제로 넘치는지**를 본다 — 새로 만든 곳을 빠뜨리지 않으려는 것이다.
  - `TAB_PATHS`는 탭바와 **같은 순서여야 한다** — 한쪽만 고치지 말 것.
    지금은 **'이 화면이 탭인가'를 가리는 데만** 쓴다.
  - **앞 화면이 손가락을 따라 딸려 나온다**(사용자 요청 — `아이폰처럼
    손가락따라가면서 뒤로가기`). 지금 화면이 손끝을 그대로 따라가고, 앞
    화면은 그 뒤에서 **1/4만큼 어긋나 따라오며** 어둡던 것이 밝아진다.
    화면 폭의 34%를 넘기거나 튕기면 넘어가고, 아니면 제자리로 돌아온다.
    `.dev/behave.mjs`의 `손가락을 따라 뒤로 가기` 칸이 붙들어 둔다.
    - **앞 화면은 떠날 때 찍어 둔 죽은 그림이다**(`snap`). 리액트로 한 번
      더 그리는 길(`<Routes location>`)로 **가지 말 것** — 화면이 통째로
      다시 살아나면서 딸린 일까지 다시 돈다(대화를 열면 읽음이 찍히고,
      투표 목록은 결과 알리기를 부르고, 실시간 구독이 두 벌이 된다).
      그림만 필요한 자리에 그 위험을 질 이유가 없다.
    - **찍는 자리는 `pushState`다.** 리액트의 효과는 전부 DOM이 바뀐
      **뒤에** 도는데 그때는 앞 화면이 이미 없다. 라우터가 주소가 바뀐 걸
      알아채기 전에 도는 유일한 자리라 거기서 찍는다(원래 하던 일은 그대로
      부르므로 라우터는 아무것도 모른다).
    - **`pathname`으로 가리지 말 것 — 이 앱은 해시 라우팅이다.**
      어느 화면에 있든 `pathname`이 `/`라, 그것으로 '탭인가'를 보면
      **모든 길이 탭으로 보여 그림을 한 장도 안 찍는다**(앞 화면이 늘 빈
      채로 깔렸다). `routeOf()`가 해시를 본다.
    - **손이 멈췄으면 빠르기를 없던 것으로 본다**(`STALE` 120ms).
      안 그러면 끌다가 망설이고 놓았을 때 마지막 빠르기가 그대로 남아
      **넘어가 버린다.** 튕김 기준도 800px/s로 두었다 — 낮추면 천천히
      끌다 놓은 것까지 걸린다.
    - **굴리기를 막는 듣기(`passive: false`)를 늘 걸어 두지 말 것.**
      문서에 붙어 있으면 브라우저가 굴릴 때마다 우리 코드를 먼저
      기다린다 — 손짓이 우리 것으로 정해진 **뒤에** 붙였다 끝나면 뗀다.
      알아채는 쪽은 `passive`다.
    - **지금 화면에 바탕색을 깔아야 한다**(`html.back-drag`). `.page`는
      속이 비어 있어 그냥 밀면 뒤에 깔린 앞 화면이 두 겹으로 비친다.
    - **걷는 일을 rAF에만 매달지 말 것.** 깔아 둔 그림이 화면에 그대로
      남는 일이 실제로 있었다(사용자 제보 — `뒤로가기하면서 오류가나더니
      저렇게됐어` · 사진). 그림은 **죽은 복사본**이라 아무것도 안 눌리고,
      지금 화면은 `transform`으로 밀려나 있어 **앱이 통째로 죽은 것처럼
      보인다**(`pointer-events: none`이라 탭바만 눌린다).
      막는 자리가 넷이고 넷이 한 벌이다:
      ① `touchend`가 안 온 채로 새 손짓이 시작되면 **먼저 걷는다**
      (iOS는 시스템 손짓에 가로채이면 그것을 아예 안 준다 — 그때
      `live`만 내리면 앞 그림을 **놓아 버려** 다음 `build()`가 새 그림을
      덮어쓴다) ② 손짓 도중에 무엇이 던져져도 걷는다(`guard`)
      ③ `end()`에 **rAF와 별개로** 0.6초 예비 타이머를 둔다(rAF는 앱을
      덮어 두면 아예 안 돈다) ④ `sweepGhosts()`가 **화면이 바뀔 때와 앱으로
      돌아올 때**(`pageshow`·`visibilitychange`) 남은 것을 훑어 걷는다.
      ④는 **깐 지 `GHOST_MAX`(1.5초)가 지난 것만** 본다 — 뒤로 가는 그
      0.23초가 곧 화면이 바뀌는 때라, 안 그러면 돌고 있는 손짓을 걷어 버린다.
      `.dev/behave.mjs`의 `손가락을 따라 뒤로 가기` 칸 끝 넷이 붙들어 둔다
      (보호 줄을 빼면 실제로 넷이 빨갛게 뜬다).
    - 걷는 것은 **새 화면이 한 번 그려진 뒤**다(rAF 두 번). 바로 걷으면
      그 한 프레임에 옛 화면이 비친다. 넘어간 직후에는 `skipSlide`로
      `useScreenSlide`를 한 번 건너뛴다 — 이미 옮겨 놓은 화면을 또
      미끄러뜨리면 두 번 움직인다.
    - 움직임을 줄여 달라고 해 둔 기기에서는 **끌리는 것 없이 곧바로**
      간다(효과가 갈라져 있다).
    - `allowsBackForwardNavigationGestures`(네이티브 그 손짓)는 **Capacitor가
      설정으로 안 열어 준다** — 열려면 `ios/`에 Swift를 더해 앱을 다시 만들어야
      하고, 해시 라우팅에서 제대로 도는지도 확실하지 않다. 지금 방식은
      웹이라 밀면 바로 올라간다.
  - **화면에 들고 날 때만** 새 화면이 온 쪽에서 미끄러져 들어온다
    (`useScreenSlide`, 40px). 방향은 `useNavigationType()`이 알려 준다
    (PUSH는 오른쪽에서, POP은 왼쪽에서).
    감싸는 칸을 새로 만들지 않았다 — `.app`이 flex 기둥이라 사이에 무엇을
    끼우면 `.page`의 `flex: 1`이 어긋난다. `.app`에 방향(`slide-l`/`slide-r`)만
    적어 두고 **첫 자식**(=지금 화면)을 CSS가 움직인다. 탭바는 마지막
    자식이라 가만히 있는다. `transform`·`opacity`만, 0.2초 한 번이다.
- **모서리는 통통하게**(`--r` 18px). 스티커의 결이다.
- **첫 화면(스플래시)은 그 스티커 자체다** — 흰 바탕에 그림 하나.
  **규칙도 그림도 `index.html`에 박혀 있다.** CSS 파일과 자바스크립트가
  도착하기 **전에** 떠 있어야 하기 때문이다 — 밖으로 빼면 그때까지 빈 화면이라
  없애려던 그 순간이 그대로 남는다.
  **그림도 파일로 부르지 않는다.** `icon-512.png`를 `<img>`로 걸었더니 문서를
  받고 나서 한 번 더 다녀와야 해서, LTE에서 **흰 화면이 먼저 보이고 조금 뒤에
  그림이 떴다.** 지금은 400px WebP(품질 80, 13KB)를 base64로 `.boot::before`의
  `background`에 넣어 문서와 함께 온다. 원본 512 PNG는 88KB라 못 박는다
  (base64로 부풀면 그보다 더 든다).
  `::before`인 것도 이유가 있다 — React 쪽 로딩 화면(`App.tsx`의 `Gate`)이
  `<div className="boot" />` 하나만 그려도 같은 그림이 나와서, **그림이
  자바스크립트 묶음에 또 들어가지 않는다.** `<style>`은 문서에 남아 있으므로
  규칙도 한 번만 적혀 있다 — **global.css에 또 만들지 말 것.**
  **나타나는 연출은 넣지 말 것.** 첫 프레임에 보이는 것이 이 화면의 전부라,
  0.5초 동안 서서히 뜨게 하면 없애려던 그 흰 화면이 그대로 남는다.
  바탕이 흰색(`--bg`가 아니라)인 것은 스티커의 바탕이 흰색이라 그렇다 —
  `--bg`로 두면 그림 둘레에 네모난 자국이 보인다.
  **아이콘을 바꾸면 이 그림도 다시 만들어 넣어야 한다** (넷 + 인라인 하나).
- **대화가 오가는 자리(`.chat-list`)만 보라다**(사용자 요청 — 카톡처럼).
  밝은 화면이라는 규칙에 둔 **하나뿐인 예외**이고, 색이 바뀌는 것은 그
  목록 하나다 — 머리말·입력칸·이모티콘 서랍은 밝은 그대로다(카톡도 그
  짜임이다). 내 말풍선은 카톡 노랑(#FEE500)이 아니라
  **아이콘의 노랑**이다. 카톡에서 가져온 것은 **크기와 짜임**이다(아래 참고).

### 홈은 '내가 뭘 해야 하나'에 답한다

**위에서부터 급한 순서로 쌓고, 해당 없는 칸은 통째로 사라진다.**
한가한 주에는 저절로 단출해지므로 빈 상태를 크게 알릴 필요가 없다.

1. **다음 라운드** — 화면의 주인공. 언제·어디·날씨·**내 조**·자리, 그리고
   **내 상태가 곧 단추 자리다**(`신청하기` / `신청 완료` / `대기 2번`).
   신청했는지 보려고 들어갈 일이 없어야 한다.
   - **내 조 줄(`.next-grp`)은 `3조 · 티오프 오전 7:38 · 박승수`다.** 새벽에
     나가면서 몇 조인지·몇 시에 치는지 보려고 라운드 상세까지 들어갈 일이
     없어야 한다. 조를 안 짠 라운드에는 이 줄이 통째로 없다(대부분이 그렇다).
   - **조 번호와 조 시각이 다른 곳에서 온다.** 번호는 신청 기록에 딸려 오고
     (`SignupHome` — 홈만 `grp`를 더 받는다), 시각은 `round_groups`라
     **NextRound가 따로 한 줄 부른다.** 라운드 목록 조회에 매달지 않은 것은
     그 표가 없는 저장소에서 400이 나면 **홈이 통째로 죽기** 때문이다 —
     따로 부르면 오류를 그냥 넘기고 시각만 안 뜬다.
   - 이름은 **닉네임 그대로** 적는다(`83/신성호/광산구`가 아니라). 긴 이름표는
     목록의 이름 자리 몫이고 이 줄은 문장에 가깝다.
2. **내가 할 일** — 아직 표를 안 던진 투표 · 안 읽은 대화 · (운영진) 승인 대기.
   - **투표 줄은 `#/polls/<id>`로 그 투표에 바로 들어간다**(사용자 제보 —
     `투표를 누르면 해당 투표로 가야 되는데 그냥 투표 택바 메뉴로만 가지고`).
     한동안 `/polls`(목록)로 하드코딩돼 있어, 밑의 라운드 줄은 그 라운드로
     들어가는데 투표만 목록에서 다시 찾아야 했다 — 마감된 것이 쌓인 주에는
     어느 것이었는지도 모른다. **알림을 누르면 목록이 아니라 그 건으로
     가는 것과 같은 잣대다.** `.dev/behave.mjs`의 `홈의 \`내가 할 일\` 투표
     줄` 칸이 붙들어 둔다 — **고정 자료에서는 내가 p1에 이미 표를 던져 놓아
     그 줄이 안 뜨므로**, 거기서만 내 표를 빈손으로 돌려주어 상태를 만든다.
3. **모집중** — 다음 것 말고 열려 있는 라운드. 자리 남음만 보인다.

**고정 공지 칸은 걷어냈다**(사용자 요청). 공지 탭이 따로 있고 안 본 개수도
그 탭에 숫자로 뜨므로, 홈에 같은 것을 또 늘어놓을 이유가 없었다.
되살리지 말 것 — 홈에서 `posts` 조회와 실시간 구독도 함께 뺐다.

**홈 머리말은 왼쪽이 얼굴 + 인사말, 오른쪽이 🔔 알림이다**(사용자 요청 —
`종 옆에 프로필을 악마제리님 이라고되어있는곳 왼쪽에 프로필을 옮기고 기존
프로필 자리에 알림을 옮겨줘`). 얼굴이 '누구의 화면인가'를 왼쪽에서 먼저
말하고, 오른쪽에는 누를 것 하나만 남는다. **얼굴만 링크다** — 인사말까지
링크로 묶으면 무엇을 누르는 자리인지 흐려진다.

```
안녕하세요                    ← 왼쪽 끝. 얼굴 옆이 아니다.
[얼굴] 악마제리님        🔔②
```

- **`안녕하세요`를 얼굴 옆 묶음에 넣지 말 것**(사용자 요청 — `안녕하세요는
  왼쪽으로 당기고`). 처음에 `[얼굴][인사말+이름]`으로 두었더니 인사말이
  **얼굴 너비만큼 안으로 밀려** 다른 화면의 제목과 시작점이 어긋났다.
  지금은 인사말이 `.head-me` 바로 아래 한 줄이고, `.head-name`이
  얼굴과 이름만 묶는다.
- **얼굴은 이름 줄과 나란히 선다**(`.head-name`의 `align-items: center`).
  인사말까지 아우르는 가운데에 두면 **두 줄 사이에 걸쳐 13px쯤 위로 떠**
  어느 줄에 붙은 것인지 흐려진다(사용자 요청 — `프로필을 조금 내려서
  악마제리님과 정렬을 맞춰주고`).
- `.head-me`(왼쪽)와 `.head-side`(오른쪽) 둘 다 **`min-width: 0` /
  `flex: 0 0 auto`가 한 쌍이다** — 없으면 이름이 긴 분의 화면(320px)에서
  왼쪽 묶음이 제 글자 길이만큼 벌어져 종을 밀어낸다.
- **`.dev/behave.mjs`가 자리를 재서 붙들어 둔다** — 얼굴 x < 이름 x < 종 x ·
  오른쪽 링크는 하나 · **인사말 x ≤ 얼굴 x** · **얼굴과 이름의 세로 가운데
  어긋남 ≤ 2px**. 클래스 이름만 보면 CSS가 뒤집혀도 초록으로 뜬다.

종은 **알림함**(`/#/alerts`)으로 가고 **안 읽은 알림 개수**가 붙는다 —
아래 '알림' 꼭지의 `알림함` 참고. **분홍을 안 쓴다**(홈에서 '지금 눌러야 할
것'은 다음 라운드 카드다) — 종은 흐린 획이고 **숫자에만** 빨강을 쓴다
(탭바의 `alert`와 같은 '내가 안 본 것'이다).

**탭바 순서는 홈 · 공지 · 라운드 · 투표 · 대화다**(사용자가 정했다).

예정된 라운드가 없으면 빈칸 대신 `모집 열기`를 놓는다 — 누구나 열 수 있는
앱이라 그 자리가 곧 초대장이다.

**색은 뜻으로 가른다. 예쁘라고 칠하지 말 것.**

- **분홍(`--brand`)은 '지금 눌러야 할 것'에만.** 한 화면에 하나가 기준이다.
  알려 주기만 하는 값(D-day, `참가 확정`, `신청함`)은 `.badge dim`으로 내린다.
- **잔디 초록(`--grass`)은 '좋은 상태'** — 자리 막대, 다음 라운드 카드,
  탭바의 **열려 있는 개수**. 분홍과 섞지 말 것.
- **노랑** = 기다리는 것(고정 공지 · 승인 대기 · 임박한 D-day).
- **빨강** = 내가 안 본 것(탭바의 `alert`). 분홍과 헷갈리지 않게 주황 쪽으로
  당겨 두었다 — 분홍 뱃지와 빨강 뱃지가 나란히 서는 자리가 있다.
- **보라(`--wait`)는 대기자 한 가지에만 쓴다** — 라운드 목록의 `· 대기 1`,
  홈의 `대기 2번`, `.badge wait`. 확정도 아니고 마감도 아닌 제3의 상태라
  위 넷 중 어느 것으로도 안 읽혔다. **다른 뜻으로 번지게 하지 말 것.**
- **파랑(`--info`)은 딱 둘이다** — 대화의 `@언급`과 명단의 `부운영자` 표.
  뜻이 있는 색이 아니라 **그 자리에서만 쓰는 표시**다. 늘리지 말 것.
- **`--male`·`--female`은 얼굴 테두리 한 곳에만 쓴다.** 위의 뜻이 있는
  색들과 섞이면 안 되므로 따로 둔 토큰이다. 뱃지·단추로 번지게 하지 말 것.
- **자리가 다 차면 `모집 마감`으로 적는다.** 4/4인데 `모집중`이라고 붙어 있으면
  아직 들어갈 수 있는 줄 알고 눌러 보게 된다. **대기 신청은 그대로 열려 있고**,
  그건 안에서 단추(`대기 신청`)가 말해 준다 — 표는 밖에서 훑을 때 쓰는 말이다.
  운영진이 손으로 닫은 것(`status='closed'`)과 같은 표를 쓴다: 회원 눈에는
  둘 다 '지금은 자리가 없다'로 같은 뜻이다.
- **열림과 끝남은 한 쌍이다** — `.badge live`(잔디)와 `.badge done`(회색).
  라운드(`모집중`/`모집 마감`·`종료`)와 투표(`진행중`/`마감`)가 같은 쌍을 쓴다.
  카드 바탕도 함께 바꾼다(`--surface-2`) — 투명도만 낮추면 목록을 훑을 때
  여전히 흰 카드라 잘 안 갈렸다. **`dim`(테두리만)으로 되돌리지 말 것.**
  목록 맨 앞은 늘 이 상태표이고, D-day는 그 뒤에 붙는 알려 주는 값이다.
- **잔디 그라디언트는 다음 라운드 카드에만.** 강조색을 늘리지 않고 주인공을
  세우는 방법이라, 다른 곳으로 번지게 하지 말 것.
- 카드 사이 `--gap-sm`(10px), 카드 안 여백 13px. 16px씩 두었더니 성겼다.

**날씨는 `rounds.lat/lon`으로 Open-Meteo에서 받는다**(`lib/weather.ts`).
키가 필요 없어 서버 없이 되는 유일한 선택지였다. 예보 범위(16일)를
벗어나거나 좌표를 못 찾으면 **그 줄만 조용히 빠진다** — 없는 값을 채우지 말 것.

**좌표는 골프장 이름에서 찾는다**(`lib/courses.ts`, 전국 574곳).
사람에게 위도·경도를 치라고 할 수는 없다. 모집을 열 때 이름을 치면 목록에서
골라 주고, 고르면 `lat/lon`이 함께 저장된다. **좌표가 비어 있는 예전 라운드는
읽을 때 이름으로 붙인다** — 옮겨 심지 않아도 날씨가 뜬다. 띄어쓰기와
대소문자는 무시하고, 딱 맞는 것이 없으면 이름이 들어 있는 것 중 가장 긴 것을
쓴다(`화순`과 `화순CC`가 같이 걸릴 때 뒤엣것).
**`courses.ts`를 손으로 고치지 말 것** — JTFAG가 OpenStreetMap에서 받아
만드는 파일을 옮긴 것이라, 갱신은 그쪽 워크플로를 돌린 뒤 다시 옮긴다.
같은 곳·같은 날은 다시 받지 않는다(캐시). 홈은 실시간 이벤트마다 다시
그려져서, 안 막으면 요청이 금방 쌓인다 — JTFAG에서 실제로 한도(429)에
걸려 날씨가 통째로 안 뜬 적이 있다. **`res.ok`를 반드시 볼 것.**

### 필드와 스크린은 같은 표에 산다

`rounds.kind` — `field`(필드) / `screen`(스크린). **비울 수 없고 기본값이
`field`다.** 이 칸이 생기기 전에 올린 라운드는 모두 필드였으므로 기본값이
그대로 맞는 답이 된다.

**표를 나누지 않았다.** 신청·대기·정원·알림이 완전히 같은 일이라 표를
나누면 `join_round`부터 화면까지 전부 두 벌이 된다. 다른 것은 **말과 있고
없음뿐**이라 값 하나로 갈린다.

|            | 필드              | 스크린          |
|------------|-------------------|-----------------|
| 장소 이름   | 골프장 (목록에서 찾아 줌) | 매장 (직접 침)  |
| 시각        | 티오프            | 시작            |
| 1인 요금    | 그린피            | 게임비          |
| 캐디·카트   | 있음              | **없음**        |
| 날씨        | 있음              | **없음** (실내) |

- **`r.kind`를 직접 보지 말 것.** `roundKind(r)`(types.ts)를 쓴다 — 스키마를
  아직 다시 안 돌린 저장소에는 그 칸이 없어 `undefined`가 온다.
- **스크린은 골프장 목록에서 찾아 주지 않는다.** `courses.ts`는 필드 목록이고,
  스크린골프방은 만들 때 일부러 걸러 낸 것들이라(JTFAG의 `NOT_A_COURSE`)
  쳐 봐야 안 나온다. 검색칸을 붙이면 '왜 안 찾아지지' 하는 자리만 생긴다.
- **캐디·카트는 화면에서 감추는 것으로 끝내지 않는다.** 필드로 열었다가
  스크린으로 바꾸면 값이 남아 상세에 조건이 그대로 뜬다 — `RoundEdit`이
  **저장할 때 null로 지운다.** 좌표도 같은 이유로 지운다(안 지우면 이름으로
  찾는 예비 길에 걸려 엉뚱한 골프장 날씨가 붙는다).
- **종류표는 둘 다 단다**(`.badge kind`). 스크린에만 표를 달면 표가 없는 줄이
  '필드'인지 '종류가 생기기 전 것'인지 헷갈린다. 상태가 아니라 **이름표라
  뜻이 있는 색을 안 쓴다** — 색(분홍·잔디·노랑·빨강)은 D-day·확정 몫이다.
  대신 **둘 다 채우고 무게로 가른다**: `필드`는 잔디 연두(`--grass`),
  `스크린`은 회녹색(`--text-dim`). **글자는 둘 다 흰색이다.**
  연두 위 흰 글자는 대비가 2.4:1로 낮다 — 한 톤 낮춘 `--grass-deep`(3.6:1)으로도
  해 봤지만 **사용자가 보기 좋은 쪽으로 정했다.** 두 글자짜리 이름표라 그대로
  둔 것이니, 나중에 '왜 여기만 대비가 낮지'로 되돌리지 말 것.
  **필드에만 잔디색을 쓰는 것은 정해 둔 예외다** — 잔디는 원래 '좋은 상태' 몫이라
  바로 옆의 `모집중`과 같은 색이지만, 그쪽은 옅은 칠에 초록 글자이고 이쪽은
  꽉 찬 칠이라 무게로 갈린다. 필드=잔디가 곧바로 읽히는 이점이 더 크다고 봤다.
  **스크린 쪽에는 색을 주지 말 것** — 둘 다 색이 되면 상태 색과 뒤엉킨다.
  섞여 있는 목록에서 눈에 걸려야 하는 쪽은 스크린이고, 이게 상태 색과 안
  부딪히는 유일한 길이다. **색으로 가르지 말 것.**
- **제목 줄에도 아이콘을 세운다**(`.round-kind-mark`, 목록 카드·상세).
  표는 작아서 다른 표들(모집중·D-day·참가 확정)과 섞여 보인다 — 목록을 훑을 때
  눈이 가는 곳은 이름 줄이라 거기서 갈려야 한눈에 들어온다. `aria-hidden`이라
  소리로는 안 읽힌다(종류는 옆의 표가 말해 준다).
- 목록 위의 가리개(`전체`·`필드`·`스크린`)는 **둘이 섞여 있을 때만 나온다.**
  필드만 올리는 달에는 누를 일이 없는 단추 셋이 자리만 차지한다.
- **베껴 여는 것은 스크린만 된다**(`같은 조건으로 새로 열기` → `/#/rounds/new?from=<id>`).
  스크린은 같은 매장에서 같은 게임비로 되풀이해 열리므로 매번 처음부터 치는
  것이 그대로 낭비였다 — **필드는 갈 때마다 골프장이 달라 베낄 것이 없다.**
  `RoundEdit`이 `preset`으로 받아 칸만 채우고 **`round`는 null이라 저장하면
  새 모집으로 들어간다**(원본은 안 건드린다). **시각만 비워 둔다** — 지난
  날짜가 채워져 있으면 그대로 저장해 버린다. 회원 누구나 모집을 열 수 있으므로
  이 단추도 누구에게나 보인다.
- **`📣 대화방에 공유`는 필드·스크린 둘 다 된다.** 모집을 열면 대화방에
  저절로 한 줄이 남지만 **그때 한 번뿐이라**, 대화가 하루에 백 마디씩 쌓이면
  위로 밀려 사라진다 — 자리가 남아도 아무도 모른다. 이 단추가 그 라운드를
  대화방 맨 아래로 다시 올린다. 지난 라운드·취소된 라운드에는 안 보인다.
  - **`system` 글로 넣는다** — 눌리는 카드로 그려지고 다른 안내와 같은 자리에
    선다.
  - **폰 알림도 함께 간다 — `system` 줄 가운데 이것 하나만 그렇다.**
    `system` 줄은 원래 발송기가 그냥 돌아서는데, 모집을 열 때 저절로 남는
    줄은 `⛳ 새 모집`이 이미 나간 뒤라 또 울리면 두 번 울리기 때문이다.
    사람이 눌러 올린 공유는 **자리가 남았다고 다시 알리는 것이 목적**이라
    대화방에만 남으면 그 묻히는 문제가 그대로 되풀이된다.
    가르는 것은 `messages.notify` 한 칸이다 — 화면이 세우고
    **`chat_notice()`는 안 건드린다**(기본값 false). 발송기는
    `system && notify`인 줄만 울린다(`⛳ 라운드 공유` / `🎯 스크린 공유`,
    tag는 모집 알림과 **같은** `round-`라 앞엣것을 대신한다).
  - **알림 문구는 발송기가 라운드에서 다시 짠다**(`shareNote`). 대화 글은
    회원이 손으로도 넣을 수 있는 값이라, 거기 적힌 것을 그대로 100명의
    알림창에 띄우지 않으려는 것이다 — 화면이 만든 글은 **대화방에 보이는
    카드 몫**이고 알림에는 안 쓰인다.
  - **칸이 없는 저장소에서도 공유는 된다.** 앱은 푸시하면 몇 분 뒤 올라가지만
    `schema.sql`은 사람이 손으로 붙여넣으므로 그 사이가 있다. `notify`(알림)와
    `round_id`(눌리는 카드)는 **있으면 좋은 것**이지 공유를 막을 것이 아니라,
    없는 칸을 **덜 아쉬운 것부터 하나씩 빼며** 다시 넣는다.
    **오류 코드가 둘이라는 것이 핵심이다** — Postgres는 `42703`을 주지만
    **PostgREST는 칸 목록을 제가 들고 있어 DB에 닿기도 전에 `PGRST204`로
    물린다**(`Could not find the 'round_id' column ... in the schema cache`).
    `42703`만 보다가 실기기에서 공유가 통째로 실패했다. **insert에 새 칸을
    붙일 때는 이 두 코드를 함께 볼 것.** `.dev/behave.mjs`의 흉내도
    `PGRST204`를 준다 — 진짜와 다른 코드를 주면 시험이 통과해도 소용없다.
  - **글은 화면이 만든다**(`RoundDetail`의 `share`). 날짜 문구를 SQL에 또
    적으면 화면과 두 벌이 된다. 전체 대화방을 고르는 잣대만 DB의
    `chat_notice`와 같게 맞춰 두었다(`round_id`가 없는 방 중 가장 먼저 만든 것).
  - **조사를 받침으로 고른다** — `필드를` / `스크린을`. 하나로 굳히면 둘 중
    하나는 늘 어색하다(`스크린를`).
- 알림 제목도 갈린다 — `⛳ 새 모집` / `🎯 새 스크린`.
- 말은 `KIND_LABEL`·`KIND_ICON`·`PLACE_LABEL`·`TEE_LABEL`·`FEE_LABEL`
  (types.ts)에 모아 두었다. 모집 열기·목록 카드·상세·홈이 같은 말을 쓰게
  하려는 것이다.

### 라운드 조건은 두 쌍의 '하나만'이다

`rounds.caddie`(`caddie`/`none`)와 `rounds.cart`(`included`/`excluded`) —
화면에는 `캐디`·`노캐디`·`카트 포함`·`카트 미포함` 네 개의 체크칸으로 보인다.
(스크린에는 이 줄이 통째로 없다 — 위 '필드와 스크린' 참고.)

**한 줄에 하나만 켜진다.** 캐디와 노캐디를 함께 켤 수는 없기 때문이다.
누른 것을 다시 누르면 '안 정함'(null)으로 돌아간다 — 조건 없이 열던
예전 라운드도 그대로 살아야 한다. 그래서 두 칸 모두 nullable이다.

말은 `CADDIE_LABEL`·`CART_LABEL`(types.ts)에 모아 두었다. 모집 열기와 목록
카드가 같은 말을 쓰게 하려는 것이다. **상세의 표만 짧은 형태**
(`CADDIE_SHORT`·`CART_SHORT` — `있음`/`없음`, `포함`/`미포함`)를 쓴다.
거기는 이름 칸이 따로 있어 긴 말을 그대로 넣으면 `캐디: 캐디`가 된다.

**상세의 표는 칸 수가 늘 짝수여야 한다.** 두 칸짜리 격자라 홀수면 옆이
비어 이 빠진 것처럼 보인다(캐디·카트를 한 칸에 묶었다가 실제로 그랬다).
그래서 필드는 캐디·카트를 **각각 한 칸씩 늘 세우고**(안 정했으면 `미정`),
스크린은 둘 다 없앤다 — 6칸 아니면 4칸이다.

### 없앤 칸 둘 — `한 줄 설명`과 `신청 시작`

둘 다 사용자가 지우라고 했다. **되살리지 말 것.** DB의 칸(`rounds.title`,
`rounds.opens_at`)은 기록으로 남겨 두고 **`RoundEdit`의 payload에서만 뺐다** —
update는 보낸 칸만 고치므로 예전 값이 그대로 살아남는다.

- **`한 줄 설명`(`title`)** — `안내`(note)와 하는 일이 겹쳤다.
  화면은 계속 읽는다: `r.course || r.title`이 목록·홈의 예비 이름이고,
  상세는 둘 다 있으면 제목 아래 작은 줄로 보여 준다.
- **`신청 시작`(`opens_at`)** — 쉰 명 남짓에서 출발선을 맞출 일이 없었다.
  이쪽은 **화면도 더는 안 읽는다.** 정할 길이 없는데 예전 값이 남아 있으면
  풀 방법도 없이 신청이 잠긴 채로 굳는다. `RoundDetail`에서 `notYet`과
  `신청 대기` 뱃지·안내줄을 함께 걷어냈다.

### 투표는 목록에서 던지고, 상세에서 들여다본다

`/#/polls`의 카드에서 바로 표를 던진다 — 목록에 들어가서 또 눌러야 하면
투표율이 떨어진다. **제목을 누르면 상세**(`/#/polls/:id`)로 간다.

- **항목 목록과 투표는 `PollOptions` 하나뿐이다**(Polls.tsx에서 내보낸다).
  목록 카드와 상세가 같이 쓰고, 다른 것은 이름을 어디까지 적느냐뿐이다 —
  카드는 셋까지 적고 `외 N명`으로 접고(한 줄로 잘린다), 상세(`full`)는 다 편다.
  **투표하는 길을 두 벌로 만들지 말 것** — 한쪽만 고치는 일이 생긴다.
- **`pollClosed()`는 types.ts에 있다.** 목록·상세·탭 숫자가 같은 잣대를 써야
  목록에는 진행중인데 눌러 들어가면 마감인 일이 안 생긴다. 화면 파일에서
  함수를 내보내면 fast refresh가 깨진다는 경고도 함께 붙는다.
- **마감 시각은 필수다**(사용자 요청 — `그걸 입력안하고 만들면 직접
  마감하기전까지 계속 진행중으로되어있는거야? 그러면 안될거같은데`).
  예전에는 선택이었고, 비워 두면 **누가 손으로 `마감`을 누를 때까지 영영
  진행중**이라 안 닫힌 투표가 목록에 그대로 쌓였다.
  - 대신 **바로누름 셋**(`3일 후` · `7일 후` · `2주 후`, `QUICK_CLOSE`)을
    함께 뒀다 — 안 그러면 올릴 때마다 달력에서 시·분까지 골라야 한다.
  - **새로 올릴 때만 지난 시각을 막는다.** 고칠 때는 지난 시각을 넣는 것이
    곧 '지금 닫는다'라, 막으면 그 길이 없어진다.
  - **이미 올라간 마감 시각 없는 투표는 그대로다** — 진행중으로 남고,
    손으로 닫아야 끝난다. 목록 조회가 그 갈래를 따로 챙긴다(위 참고).
- **상세의 현황은 탭 셋이다** — `항목별`(항목마다 누가 골랐나) ·
  `멤버별`(사람마다 무엇을 골랐나) · `미참여`(아직 안 한 사람, 탭에 수까지).
  서른 명이 넘으면 한 가지 배열로는 답이 안 나온다. 날짜를 정할 때 정작
  궁금한 건 `미참여`라 그것도 한 자리를 준다.
  **익명 투표에서는 이 카드가 통째로 없다** — 안 한 사람을 알려 주면
  나머지가 곧 한 사람이 되어 익명이 깨진다.
- 상세 위쪽의 투표 칸은 이름을 안 적는다(`hideVoters`). 위는 **던지는 곳**,
  아래 탭은 **읽는 곳**이다. 같은 이름을 두 번 적지 않는다.
- **댓글은 `poll_comments`** — 아래 '댓글' 참고.
- '누가 골랐는지' 줄은 **항목마다 다 있거나 다 없다.** 표를 받은 항목에만
  붙이면 그 칸만 키가 커져 들쭉날쭉해진다(빈 줄이 접히지 않게 `min-height`).

#### 목록은 두 곳을 접어 길이를 반으로 줄인다

사용자 제보 — `투표에서 항목이 많아지거나 투표 진행하는게 많아지면 화면이
너무 길어져`. 100명·1년치로 재니 목록이 **화면 4.5장**이었다
(`node .dev/scale.mjs 100 --shots`). 지금은 **2.3장**이다.

- **진행중 카드는 항목을 다섯까지만 편다**(`OPTIONS_SHOWN` in Polls.tsx).
  날짜 투표는 후보가 열 개를 넘기도 하는데, 그런 것이 서넛만 열려 있어도
  목록이 대여섯 장이 된다. 나머지는 `항목 N개 더 보기`로 **그 자리에서**
  펴진다 — 목록에서 표를 던지는 것이 이 화면의 핵심이라 상세로 안 보낸다.
  - **내가 고른 것이 접힌 자리에 있으면 아예 펴 둔다**(`hidPick`).
    안 그러면 목록에서 내가 무엇을 골랐는지 알 길이 없어, 이미 던져 놓고
    또 던지러 들어간다.
  - **`max`는 목록 카드만 준다** — 상세(`PollDetail`)는 늘 다 편다.
- **마감된 카드는 항목을 아예 안 편다.** 대신 **1위 한 줄**(`.poll-won`)로
  줄인다 — 끝난 투표에서 할 일은 없고 무엇으로 정해졌는지만 궁금하다.
  다 보려면 제목을 눌러 상세로 간다(거기 `항목별·멤버별·미참여`가 다 있다).
  마감 다섯 개가 각각 항목을 펴고 있으면 그것만으로 화면 두 장이었다.
  - **동점이면 다 적는다**(`topOptions`). 하나를 골라 버리면 거짓말이 된다 —
    **대화방에 남는 결과 카드(`post_poll_result`)와 같은 규칙이라 한쪽만
    고치지 말 것.** 표가 하나도 없으면 `아무도 투표하지 않았습니다`다.
- `.dev/behave.mjs`의 `투표 목록이 길어지지 않는다` 칸이 붙들어 둔다.
  **고정 자료의 p1은 항목이 일곱이다** — 다섯보다 많아야 접히는지가 보인다.

#### 투표 수정 — 표가 들어온 뒤에는 잠기는 것이 있다

`PollEdit`이 만들기와 수정을 함께 한다(`/#/polls/:id/edit`, 라운드·공지와
같은 짜임). 문은 상세 오른쪽 위의 `수정`이고, **올린 사람과 운영진**만
보인다. DB도 같게 막혀 있다(`polls_upd` · `poll_options_own` · `*_admin`).

- **표가 하나라도 들어오면 `익명`과 `복수 선택`이 잠긴다.**
  `익명`을 끄면 **비밀인 줄 알고 고른 사람이 그대로 드러난다** — 나중에
  마음을 바꿔서 될 일이 아니다. `복수 선택`을 끄면 이미 여러 개 고른
  사람의 표가 남아, '하나만 고르는 투표'에 두 표를 가진 사람이 생긴다.
  제목·설명·마감 시각·항목 글자는 그대로 고칠 수 있다 — 오타를 바로잡는
  것이 수정의 대부분이고 뜻이 뒤집히지 않는다.
- **항목을 지우면 그 표도 함께 사라진다**(`poll_votes`가 `on delete cascade`).
  그래서 항목마다 받은 표를 옆에 적어 두고(`.option-votes`), 표가 있는
  항목을 지울 때는 몇 표가 없어지는지 세어 한 번 더 묻는다.
- **안 바뀐 항목에는 쓰기를 안 보낸다.** 글자와 순서가 그대로면 건너뛴다 —
  실시간 이벤트가 그만큼 덜 돈다.
- `sort`는 **화면에 보이는 순서로 다시 매긴다.** 가운데를 지우면 번호에
  구멍이 나기 때문이다.
- **날짜는 우리가 직접 그린 달력에서 고른다**(`📅 날짜로 항목 넣기` ·
  `components/DayCal.tsx`). **모임 투표의 거의 전부가 날짜 정하기**라
  손으로 치면 요일을 세어 봐야 하고 오타도 난다 — 날을 누르면 그 자리에서
  `10월 4일 (일)` 항목이 되고, **다시 누르면 빠진다**(`toggleDate`).
  `추가`를 따로 두지 않은 것은 조 편성 조건과 같은 결이다(누르면 바로 된다).
  - **브라우저의 날짜 칸(`<input type="date">`·`datetime-local`)을 쓰다가
    걷어냈다.** 세 판을 오갔으니 그 자취를 적어 둔다 — **다시 브라우저 칸으로
    되돌리지 말 것:**
    1. 처음엔 직접 그린 달력이었다.
    2. `날짜선택하는게 라운드와 비교했을때 왜케 작아` → 모집 열기의 티오프는
       아이폰이 띄우는 제 창이라 우리 달력이 그 옆에서 자잘해 보였다.
       그래서 **투표도 브라우저 칸으로 바꿨다.**
    3. 사용자가 라운드 `티오프`와 투표 `마감 시각`을 잇따라 찍어
       **`이런게 아예 다른데?`**라고 물었다 — 라운드 쪽 창이 1.5배쯤 크고
       꼬리가 없었다. **우리 코드에서 둘은 완전히 같은 칸이었다**(헤드리스로
       쟀다 — 둘 다 `type="datetime-local"` · `.input` · 16px · 328×44px,
       위에 `transform`도 `zoom`도 없다). 아이폰이 **칸 옆에 꼬리를 달아
       붙이는 팝오버**와 **가운데에 크게 띄우는 창** 중 하나를 그때그때
       고르는 것뿐이고, 그 안은 `<html lang="ko">`인데도 영어다.
       모양·크기·언어 어느 것도 웹에서 정할 수가 없다.
    4. 그래서 **라운드와 투표 둘 다 우리 달력으로 되돌렸다**(사용자가 고른
       길이다 — `2번으로하자`). 두 화면이 늘 같은 모양이고 글자도 한글이다.
  - **아이폰이 날짜 칸을 누르는 순간 오늘을 던지는 문제도 여기서 함께
    없어진다.** 예전에는 그 신호에 매달려 **고르기도 전에 오늘이 항목으로
    들어갔고**(사용자 제보 — 5·12·19·26을 골랐는데 2일이 끼어 있었다),
    `넣기` 단추를 한 쌍으로 두어 막고 있었다. 직접 그린 달력에는 그 신호
    자체가 없어 단추도 필요 없다. `.dev/behave.mjs`의 `투표에 날짜 넣기`
    칸이 붙들어 둔다.
  - **같은 날짜는 두 번 안 넣는다**(항목 글자로 견준다). 항목이 곧 진실이라
    따로 목록을 들고 있지 않고, 그래서 둘이 어긋날 자리가 없다.
    달력의 `marked`가 그 잣대를 그대로 쓰므로 **칠해진 날 = 항목에 있는 날**이다.
  - **빈 줄부터 채운다** — 새 투표는 빈 칸 두 개로 시작하는데 아래에 새 줄을
    붙이면 저장할 때 걸러지긴 해도 **화면에 빈 칸이 남아** 안 적은 것처럼 보인다.
  - **두 줄까지 줄면 지우는 대신 글자만 비운다**(`dropRow`). 항목이 둘보다
    적은 투표는 뜻이 없어 `✕`가 그때 잠기는데, 눌러도 아무 일이 없으면
    고장 난 것처럼 보인다.
  - 요일을 붙이는 `dateLabel()`은 `lib/format.ts`에 있고
    **`new Date('2026-10-04')`로 읽지 않는다** — 그건 UTC 자정이라 한국에서
    보면 하루가 밀린다.

### 댓글은 세 곳에 달리고 코드는 한 곳이다

공지 · 투표 · 라운드에 댓글이 있다. 라운드 댓글은 `카풀 자리 있나요`처럼
**그 라운드에만 걸리는 말**이 대화방에서 묻히지 않게 하려고 뒀다.

- 표는 셋이다 — `post_comments` · `poll_comments` · `round_comments`.
  하나로 합쳐 `target_type`을 두는 길도 있지만 외래키가 느슨해지고 정책이
  복잡해진다. 표가 하나 더 생기는 편이 싸다. **모양과 정책은 셋이 같다.**
- **화면 코드는 `components/Comments.tsx` 하나뿐이다**(모양은 global.css의
  `.comment*`). 목록·적는 칸·넣기·지우기까지 여기서 하고, 화면은 어느 표에
  다는지(`target`)와 다시 불러오기(`onChange`)만 넘긴다. 화면마다 따로
  만들면 아래 규칙 중 한쪽만 고치게 된다.
- **적는 칸은 대화 입력칸에서 겪은 것을 그대로 옮겨 왔다**(아래 'iOS 키보드'
  참고). 한글을 치는 자리라 같은 증상이 그대로 났다:
  - `value`로 묶지 않는다 — 보낼 때 `ref`에서 직접 읽는다.
  - 안내 글씨는 `placeholder`가 아니라 직접 그리고 **초점이 가는 순간**
    치운다. 실제로 첫 글자를 칠 때 `댓글 남기기`가 번쩍였다.
  - **`등록`이 켜지고 꺼지는 기준도 초점이다.** 적은 글자로 정했더니 조합
    중에 값이 잠깐 비어 보이는 순간마다 단추 색이 오가 **깜빡였다.**
    초점이 있으면 켜 두고, 비어 있으면 눌러도 아무 일이 없게 한다.
  - `등록`은 `onMouseDown`을 막는다 — 초점을 뺏기면 키보드가 내려간다.
  - **친 글을 state에 안 넣는다**(`hasText` ref). 넣었더니 글자마다 댓글
    목록이 통째로 다시 그려졌다. 읽는 곳(안내 글씨·`등록`)은 둘 다 초점이
    오갈 때만 보므로 ref로 충분하다. **state로 되돌리지 말 것.**
    그리는 중에 ref를 읽는 자리라 oxlint가 경고하는데, 일부러인 것을
    `oxlint-disable react/refs` 한 줄로 적어 두었다.
  - **칸이 늘어나는 방식도 대화창(`growDraft`)과 똑같다** — 재는 일을
    **다음 프레임으로 미루고**, 늘 때는 넘칠 때만, 지웠을 때만 되돌려
    다시 잰다. **한쪽만 고치지 말 것.**
- **라운드 상세에서는 댓글이 맨 아래, `신청 버튼보다 뒤`에 온다.**
  그 버튼은 바닥에 붙어 있다가(`position: sticky`) 제자리에 닿으면 물러나므로,
  댓글을 읽으러 내려가면 저절로 비켜 준다. 위에 두면 적는 내내 칸을 가린다.
- **댓글에는 알림을 안 보낸다.** 셋 다 그렇다 — 새 공지·새 모집·새 투표는
  알리지만 거기 달린 댓글까지 밀면 대화 알림과 다를 바가 없어진다.

### 정산은 라운드에 붙는다

라운드가 끝나면 그린피·카트비를 걷는다. 카톡에서 계좌를 다시 올리고
"누가 보냈나"를 세던 일을 없애려고 만들었다 (`components/Settlement.tsx`).

**표는 둘이다.** `settlements`(제목·안내·은행·계좌·총액)와
`settlement_shares`(사람마다 얼마 · 보냈는지). **한 표에 사람별 금액을
JSON으로 담지 않았다** — 그러면 '내 것만 보냈다고 표시'가 남의 줄까지
덮어쓰는 경합이 되고, 알림도 사람별로 못 나눈다.

- **회원 누구나 만들고, 만든 사람이 챙긴다**(사용자가 정한 것이다).
  100명 모임에서 라운드를 여는 사람이 제각각인데 총무 한 사람이 모든 돈을
  걷는 것은 무리라, 걷는 사람이 곧 만드는 사람이 되게 열었다.
  **여는 만큼 좁히는 것이 정책 넷이다** — 누구나 계좌번호를 걸고 100명에게
  알림을 밀 수 있게 되기 때문이다:
  - `settlements_add` — 만들 때 `created_by = auth.uid()`. **남의 이름으로
    못 만든다**(남의 계좌인 것처럼 꾸미는 길을 막는다).
  - `settlements_own` · `shares_own` — 고치고 지우는 것은 **만든 사람**뿐.
    몫도 같은 잣대다(`owns_settlement()`) — 정산과 몫이 따로 놀면 남의
    정산에 내 몫을 끼워 넣는 길이 생긴다.
  - `settle_reminders_add` — 독촉은 **만든 사람과 총무·운영진**만. 남의
    정산에 보내면 그 사람 계좌로 오라는 알림이 100명에게 나간다.
  - `*_admin` — **총무·운영진(`can_settle`)은 그대로 전부 만진다.** 만든
    사람이 한동안 안 들어올 때 대신 챙길 자리가 없으면 돈이 공중에 뜬다.
- **`owns_settlement()`는 잣대를 한 곳에 모으려고 둔 것이다** — 정책 넷과
  트리거 하나가 같이 본다. `security definer`인 것은 `settlements`를 읽는
  그 조회가 다시 RLS를 타지 않게 하려는 것이다.
- 금액은 **만든 사람과 총무·운영진만** 고친다: `shares_amount_locked()`
  (BEFORE UPDATE)가 그 밖의 사람이 `amount`를 바꾸면 막고, 본인은 `paid`만
  뒤집을 수 있게 `shares_own_paid` 정책이 열려 있다. **이 트리거를 빼면
  자기 몫을 0원으로 고쳐 놓고 냈다고 표시할 수 있다.**
- **정산 카드에 `누구님이 걷습니다`를 적는다.** 한 라운드에 여러 사람의
  정산이 붙을 수 있게 되면서, 계좌만 덩그러니 있으면 그게 누구 것인지
  물어보러 가야 한다.
- **1/N은 화면이 계산한다**(`splitEvenly`). 총액을 고른 사람 수로 나누되
  **10원 단위로 내림**하고 남는 잔돈은 첫 사람에게 몰아 준다 — 합이 총액과
  1원도 안 어긋나야 총무가 다시 세지 않는다.
  **한 사람 금액을 손으로 고치면 그 사람은 예외로 굳고**(`fixed`) 나머지가
  남은 금액으로 다시 나뉜다. `신성호만 1만원, 나머지는 2만원씩` 같은 자리다.
- **알림은 `settlement_shares`의 INSERT에 건다** — `settlements`에 걸면
  모두에게 같은 문구가 가서 "나는 얼마인데?"를 또 물어야 한다. 지금은
  자기 몫 금액이 알림에 그대로 적혀 온다(`only: [r.user_id]`).
  발송기의 `note.only`가 빈 배열이면 아무에게도 안 보내고 조용히 끝낸다.

#### 총무 화면과 입금 독촉

`/#/settle`(`screens/Settle.tsx`) — 문은 `내 정보`에 있고 **회원 누구나
들어온다**(정산을 만드는 것이 누구나라 걷는 사람도 누구나다).
**탭바에는 안 넣는다**: 탭 다섯의 순서는 사용자가 정한 것이고, 라운드를
여는 달에만 쓰는 화면 때문에 모두의 탭을 늘릴 이유가 없다.

- 정산은 라운드에 붙어 있어서, 지금까지 "누가 안 냈지"를 보려면 라운드를
  하나씩 열어야 했다. 여기 모아 놓으면 한 화면에서 끝난다.
- **기본은 `내가 올린 정산`이다.** 돈은 올린 사람 계좌로 들어가므로 챙길
  사람도 그 사람이다 — 라운드를 여럿이 나눠 여는 모임에서 남이 걷는 돈까지
  한 화면에 깔리면 누구 것인지 헷갈리고 **남의 정산에 독촉을 눌러 버린다**
  (사용자가 짚어 준 것이다. 처음에는 전부 보여 줬다).
  `전체` 탭은 남겨 둔다 — 올린 사람이 한동안 안 들어올 때 총무가 대신
  챙길 자리가 없으면 이 화면을 만든 뜻이 반쯤 없어진다. 정산은 어차피
  회원 누구나 라운드에서 볼 수 있으므로(`settlements_read`) **감추는 것이
  아니라 눈을 좁혀 주는 것**이다.
- **`전체` 탭은 총무·운영진에게, 남이 올린 것이 있을 때만 나온다**
  (`canToggle`). 일반회원에게 열면 남의 돈 서른 건이 깔릴 뿐이라 내 것만
  보면 된다. `전체`에서는 카드마다 `박승수님이 올림`을 붙이고, 남의
  정산에 독촉을 누르면 확인창이 **그 사람 계좌로 들어간다**고 한 번 더
  알린다.
- **맨 위는 안 걷힌 금액 하나다.** 총무가 제일 먼저 알고 싶은 것은 목록이
  아니다. **빨강이 아니라 먹색으로** 적는다 — 빨강은 '내가 안 본 것' 몫이고
  이건 경고가 아니다.
- **다 걷힌 정산은 목록에서 뺀다**(아래 한 줄로 개수만 센다).
  `RECENT`(30)로 한도를 정한 것은 라운드 목록과 같은 이유다.
- **안 낸 사람만 세운다.** 100명 모임에서 다 낸 사람 이름까지 늘어놓으면
  목록이 화면을 덮는다. 이름표를 누르면 입금완료가 된다 — 현금으로 받는
  일이 흔하다. **누를 수 있다고 머리말에 적어 둔다**(`눌러서 입금완료`):
  이름표가 단추로는 안 보여서, 예전에 정산 줄이 눌리는 줄인 것을 아무도
  몰랐던 일이 그대로 되풀이된다.
- **`입금 알림 보내기`는 `settle_reminders`에 한 줄만 넣는다.** 누구에게
  보낼지는 **발송기가 고른다**(그 정산에서 `paid = false`인 사람만) —
  화면이 사람을 고르면 규칙이 두 벌이 되고, 그 사이 입금한 사람에게도 간다.
  다 낸 정산이면 보낼 사람이 없어 조용히 끝난다.
- **독촉 알림에 금액은 안 적는다.** 한 건이 여러 사람에게 가는데 낼 돈은
  사람마다 다르다(중간 참여자). 잘못 적느니 안 적고, 눌러서 들어가면 자기
  몫이 크게 적혀 있다.
- 보낸 기록이 남는 것도 값이 있다 — `마지막 알림 …`을 보고 하루에 세 번
  보내는 일이 없다. 그래서 `settle_reminders`에는 지우기를 안 열었다.
- **`입금금액`(읽는 곳)과 `입금완료`(누르는 곳)를 갈라 놓는다.**
  예전에는 줄 하나가 통째로 눌리는 단추였는데 **단추처럼 안 생겨서
  눌리는 줄인지조차 몰랐다**(사용자 제보 — 그 기능이 있는 줄도 몰랐다).
  지금은 금액을 크게 얹고 그 아래 **꽉 찬 분홍 단추**를 세운다. 누르면
  잔디색 `입금완료 ✓`가 되고, 잘못 눌렀을 때 되돌리는 법을 그 아래
  한 줄로 적어 둔다. 총무에게 따로 말하러 갈 일이 없어야 한다.
  계좌에는 복사 단추를 둔다 — 손으로 옮겨 적다 틀리면 돈이 엉뚱한 데로 간다.
- **`토스로 보내기`는 지름길이지 대체가 아니다**(`tossUrl()` in Settlement.tsx).
  `supertoss://send`에 은행·계좌·**내 몫**을 실어 보내면 채워진 송금 화면이
  뜬다. **토스가 없는 폰에서는 아무 일도 안 일어나므로 복사 단추를 그대로
  둔다.** 분홍을 안 쓰는 것도 그래서다 — '지금 눌러야 할 것'은 `입금완료`
  하나이고 이건 있으면 편한 길이다.
  은행 이름은 사람이 손으로 치는 칸이라 `국민은행`처럼 적히므로 **끝의 `은행`을
  뗀다**(`카카오뱅크`·`토스뱅크`는 `뱅크`로 끝나 안 걸린다). 계좌번호의 `-`도
  뺀다. 못 알아보면 토스가 은행만 고른 채로 열릴 뿐이라 손해가 없다.
- **계좌번호는 절대 자르지 않는다.** 한 줄에 `은행 번호`로 이어 붙였더니
  좁은 화면(320px)에서 `국민 123456-78-90…`이 됐다 — 옮겨 적을 수도, 복사된
  것이 맞는지 볼 수도 없다. **은행 이름을 윗줄로 올려** 번호가 한 줄을 통째로
  쓰게 했다. `truncate`(말줄임)를 다시 붙이지 말 것.
- **고르는 명단은 회원 전체다. 참가자로 좁히지 말 것.** 라운드는 안 하고
  **뒷풀이만 온 사람**이 있어서, 좁히면 그 사람을 넣을 길이 아예 없어진다.
  대신 확정 참가자(`joined`)를 `참가자`로 앞에 세우고 나머지는 `그 외`로
  접어 둔다 — 대개 참가자끼리 끝나므로 평소엔 짧게 보인다.
  이미 고른 사람이 `그 외`에 있으면 접지 않는다(`restOpen`) — 고른 것이
  화면에서 사라지면 지운 것처럼 보인다.
  `모두 넣기`는 참가자 묶음에만 있다. 정산은 대개 그 사람들 전부라
  네 번 누르게 할 이유가 없다.
- **`그 외`가 열둘(`FIND_AT` in types.ts)을 넘으면 늘어놓지 않고 찾게 한다.**
  쉰 명으로 재 봤더니 마흔여섯 명이 펼쳐지며 그 목록만 **602px**,
  화면(844px)의 3/4를 먹었고 한 명 고르려면 눈으로 다 훑어야 했다.
  검색칸을 두니 597px 늘던 것이 94px가 됐다. **열둘 이하면 검색칸을
  안 띄운다** — 네 줄이면 훑는 게 빠르고, 칸만 하나 더 생겨 성가시다.
  **검색어에 안 걸려도 이미 고른 사람은 남긴다** — 사라지면 뺀 줄 안다.
  이 칸도 `placeholder`를 안 쓴다(한글 조합 중 번쩍임 — 아래 'iOS 키보드').
  **회원 명단도 같은 잣대를 쓴다** — 100명이면 명단이 6,495px(화면 여덟 장)라
  한 사람을 임명하려면 끝까지 훑어야 했다. 거기서는 **이름·차량번호·전화번호**
  셋으로 찾는다(골프장에 차를 등록하다 `1234`가 누구 것인지 되짚는 자리가 있다).
- **입력칸에 예시 글씨(`placeholder`)를 넣지 않는다**(사용자 요청).
  칸 이름이 이미 무엇을 적는 자리인지 말해 주는데, 흐린 예시까지 다섯 칸에
  깔리면 **다 적은 화면인지 빈 화면인지 헷갈린다.**

### 라운드·투표를 열면 대화방에 한 줄 남는다

`announce_to_chat()`(rounds·polls의 트리거)이
`신성호님이 라운드 모집을 열었습니다 · 무등산CC` 같은 글을 전체 대화방에
넣는다. 모집을 열어 놓고 카톡에 따로 알리던 일을 없앤 것이다.
말은 트리거 안에 있다 — 스크린이면 `스크린 모집`, 투표면 `투표를 올렸습니다`다.

- **다시 연 것도 알린다**(`...reopen` 트리거, AFTER UPDATE). 예전에는 새로
  올릴 때만 적어서, **마감했던 투표를 다시 열면 대화방에 아무 말도 안 남았다**
  (사용자 제보). 라운드도 같다 — `모집 다시 열기`와 `취소 되돌리기` 둘 다.
  말은 `다시 열었습니다`로 갈라 적는다. 새로 올린 것과 같은 말이면 새 투표가
  올라온 줄 안다.
  **고친 게 그것뿐일 때는 조용히 지나간다** — 제목만 바꾸거나 마감할 때는
  안 남는다. 가리는 조건은 **함수 안에** 있다(트리거의 `when`이 아니다):
  `when`에 적으면 라운드·투표 두 벌로 갈라져 한쪽만 고치게 된다.
- **누가 했는지는 `tg_op`으로 갈린다.** 새로 올린 것은 `created_by`,
  다시 연 것은 `auth.uid()`(지금 누른 사람)다 — 남이 연 투표를 운영진이
  다시 열 수 있다. SQL 편집기에서 고치면 `auth.uid()`가 없어 만든 사람으로
  되돌아간다.
- **`poll_shut()`은 화면의 `pollClosed()`와 같은 잣대여야 한다.**
  마감 시각이 지나 끝난 투표는 `closed`가 아직 false라, 그것만 보면
  '다시 열림'을 못 알아챈다.
- **`messages.system`이 true인 글이다.** 말풍선이 아니라 가운데로 그려지고,
  아바타도 이름도 안 붙는다. 말풍선으로 그리면 누가 말을 건 것처럼 보이고
  답장·밀기까지 붙는다.
- **라운드도 투표도 눌리는 카드가 된다**(`LinkCard` in Chat.tsx —
  `messages.round_id` / `poll_id`). 예전에는 가운데 한 줄이라 **눌러도 아무
  일이 없어서**, 보려면 탭을 옮겨 목록에서 다시 찾아야 했다(사용자 제보 —
  대화를 보다가 바로 들어가지는 것이 이 카드의 전부다).
  붙는 곳이 넷이다: 모집·투표를 열 때(`announce_to_chat`), 조 편성이 나올
  때(`set_round_groups`), 투표가 끝났을 때(`post_poll_result`), 그리고
  라운드 상세의 `📣 대화방에 공유`.
  **넷을 한 코드로 그린다**(`LinkCard`) — 따로 만들면 한쪽만 고치게 된다.
  갈 곳에 맞는 말이 붙는다(`라운드 보러 가기` / `투표 보러 가기`).
- **안내 글을 두 줄로 적는다** — 첫 줄이 흐린 머리말, 둘째 줄이 제목이다.
  한 줄로 이어 붙였을 때는 카드가 밋밋해 그냥 안내문처럼 보였고, **눌러서
  들어가는 것**을 아무도 안 알아챘다. 다만 **줄 수를 세지 않는다**: 첫 줄만
  갈라 놓고 나머지는 있는 대로 그리므로 문구가 늘어도 안 깨진다. 한 줄짜리는
  그 줄이 곧 내용이라 머리말로 흐리게 깔지 않고 제목으로 세운다.
- **닫는 쪽도 알린다**(사용자 제보). 예전에는 열 때만 적어서 **마감·취소·삭제에는
  대화방에 아무 말도 안 남았고**, 사람들은 아직 살아 있는 줄 알고 신청하러
  들어갔다. 지운 라운드의 옛 카드는 `on delete set null`로 조용히 안 눌리는
  줄이 될 뿐이라 **왜 안 들어가지는지도 몰랐다.**
  - `모집을 마감했습니다` · `라운드를/스크린을 취소했습니다` — `announce_to_chat`의
    UPDATE 갈래다. 라운드가 아직 있으므로 **카드는 그대로 눌린다.**
  - `모집을 지웠습니다` · `투표를 지웠습니다` — `announce_gone()`(`after delete`).
    **카드로 만들지 않는다** — 갈 곳이 이미 없다.
  - **투표 마감은 여기서 안 적는다** — 끝나는 순간 `post_poll_result`가 결과
    카드를 남기므로(1위까지 적힌다) 또 적으면 두 줄이 쌓인다.
  - **폰은 안 울린다.** 웹훅의 `when`은 '다시 열림'만 고르므로(`docs/설치.md` 3번)
    닫는 소식은 대화방 줄로만 간다 — 마감은 급히 알릴 일이 아니다.
- **`chat_notice`의 인자를 늘릴 때는 옛 판을 남김없이 `drop`할 것.**
  `create or replace`는 인자 수가 다르면 **함수를 하나 더 만들 뿐**이라,
  인자를 늘려 온 자취가 그대로 쌓인다 — 둘짜리 → 셋짜리 → 넷짜리.
  기본값이 달려 있어 **적은 인자로 부르면 여럿이 한꺼번에 맞고**,
  Postgres는 `function chat_notice(text, uuid) is not unique`로 거절한다.
  실기기에서 `지우기`가 이렇게 통째로 막혔다(`announce_gone`이 둘로 불렀다) —
  **DB에 쌓인 자취는 저장소 파일만 봐서는 안 보인다**는 것이 이 자리의 교훈이다.
  지금은 `drop`을 둘 다 적어 두었고, **부르는 곳도 인자 넷을 다 적는다** —
  그래야 어쩌다 남은 판이 있어도 딱 하나만 맞는다.
  `pg_proc`을 한 번 훑어 보면 남은 판이 바로 나온다:
  `select p.oid::regprocedure from pg_proc p join pg_namespace n
   on n.oid = p.pronamespace where n.nspname='public' and p.proname='chat_notice';`
- **이 글에는 푸시를 안 보낸다** — 발송기가 `r.system === true`면 바로
  돌아선다. 라운드·투표는 이미 자기 알림(`⛳ 새 모집` / `🗳 새 투표`)이
  나가므로, 안 막으면 한 번 여는데 알림이 두 번 온다.
  **예외는 `notify`가 선 줄 하나뿐이다** — 사람이 `📣 대화방에 공유`를
  눌러 올린 것이다(위 '필드와 스크린' 꼭지). `chat_notice()`는 그 칸을
  안 세우므로 저절로 남는 줄은 예나 지금이나 조용하다.
  **다시 열 때도 폰이 울린다** — 아래 '알림' 꼭지의 `after update` 웹훅이
  그 몫이다. 이 대화 줄과는 따로 나가므로 여기서 막는 것과 상관없다.
- 트리거가 `security definer`인 것은 **방과 이름을 찾는 조회가 RLS에
  안 걸리게** 하려는 것이다(`rooms` · `profiles`). 글의 주인(`user_id`)은
  그대로 여는 사람이라 대화에서는 그 사람이 남긴 것으로 남는다.
- **전체 대화방은 `round_id is null`인 방 중 가장 먼저 만든 것**이다.
  방이 하나도 없으면 아무것도 안 넣고 조용히 지나간다 — 안내 한 줄 때문에
  모집 열기가 통째로 실패하면 안 된다.

#### 투표가 끝나면 결과 카드가 남는다

카톡이 하던 것을 옮긴 것이다(사용자 요청) — 마감된 줄 모르고 지나가면
무엇으로 정해졌는지 물어보러 다녀야 한다. 만드는 곳은 `post_poll_result()`
하나이고, 대화에서는 **한 줄이 아니라 눌리는 카드**로 그려진다.

- **끝나는 길이 둘인데 하나만 DB 사건이다.** 손으로 `마감`을 누르면
  `polls`가 갱신되므로 `polls_result` 트리거가 곧바로 돈다. 그런데
  **마감 시각이 지나 끝나는 것은 아무 일도 안 일어난다** — 시간이 흐르는
  것은 사건이 아니다. 그래서 **홈·투표 목록·투표 상세가 '끝났는데 아직 안
  알린 것'을 보면 그 함수를 부른다**(`announceClosedPolls` in lib/db).
  정해진 시각에 도는 것(pg_cron)을 새로 켜지 않으려는 것이고, 아무도 앱을
  안 열었으면 볼 사람도 없으므로 늦어도 탈이 없다.
  **홈이 함께 부르는 것이 핵심이다** — 투표 탭에서만 부르면 아무도 그 탭을
  안 여는 날 결과가 하루 종일 안 남는다. 홈은 모두가 처음 닿는 화면이고
  이미 투표를 받고 있어(`closed=false`) 조회가 늘지 않는다.
  **탭바에서는 부르지 않는다** — 거기 조회는 대화 한 마디마다 다시 도는데,
  칸을 넓히면 그 통신량이 곧바로 두 배가 된다.
  **`.dev/behave.mjs`에서 잴 때는 문서를 새로 열어야 한다**(`countPosts`) —
  `#`만 바뀌는 이동은 화면을 새로 안 만들어서, 같은 투표를 한 번만 부르는
  규칙에 걸려 두 번째 화면에서는 늘 0번이 나온다.
- **한 줄만 남는 것은 DB가 지킨다.** 함수가 행을 잠그고(`for update`)
  `polls.result_at` 도장을 보고 나서 넣으므로, 백 명이 동시에 앱을 열어도
  먼저 든 하나만 쓴다. 앱의 `tried`는 그 위에 얹은 예의일 뿐이다(실시간
  이벤트로 다시 그려질 때마다 헛걸음을 안 보내려는 것).
- **다시 열면 도장을 지운다** — 다시 마감하면 그때 또 알려야 한다.
  안쪽 `update`가 트리거를 다시 부르지만 끝난 상태가 안 바뀌어 거기서 멈춘다.
- **동점이면 다 적는다**(`공동 1위 · A, B (3표)`). 하나를 골라 버리면
  거짓말이 된다. 표가 하나도 없으면 `아무도 투표하지 않았습니다`다.
- **머리말에 이모지를 안 넣는다.** 알림 제목(`🗳 새 투표`)은 폰이 제 글꼴로
  그리지만 이 줄은 브라우저가 그린다 — `🗳`가 없는 기기에서는 네모난 두부가
  나온다(헤드리스에서 실제로 그랬다).
- **카드는 줄 수를 세지 않는다**(`PollResult` in Chat.tsx). 첫 줄만 갈라
  흐리게 놓고 나머지는 있는 대로 그리므로, 문구가 늘거나 줄어도 안 깨진다.
  `messages.poll_id`가 있는 `system` 글만 카드가 되고, 없는 예전 안내 줄은
  지금처럼 가운데 한 줄이다.
- **1위 줄에 분홍을 안 쓴다** — 분홍은 '지금 눌러야 할 것' 자리이고 이건
  알려 주는 값이다. 좋은 결과라 잔디색이 뜻에 맞는다.
- **푸시는 안 보낸다.** 다른 `system` 줄과 같다 — 발송기가 `r.system`이면
  바로 돌아선다. 대화방에 남는 것으로 충분하고, 안 그러면 투표 하나에
  알림이 두 번 간다.

### 탭을 넘길 때 기다리지 않게 — 마지막 것을 먼저 보여 준다

`useAsync(fn, deps, 키)`(lib/db.ts)에 **세 번째 인자**를 주면 마지막 결과를
기억해 두었다가 다음에 먼저 보여 주고, 새 값은 뒤에서 받아 슬쩍 갈아 끼운다.

**탭 전환이 느린 것은 앱이 느려서가 아니다.** 화면이 새로 만들어지며 자료를
다시 물어보는데, 그 기다림이 **통째로 인터넷 왕복 시간**이다. 재 본 값이다:

| Supabase 왕복 | 탭 눌러 내용이 나오기까지 |
|---|---|
| 0ms | 10~58ms |
| 150ms | 190ms |
| 300ms | 340ms |

그래서 **앱(Capacitor)으로 감싸도 이건 안 없어진다** — 껍데기만 폰에 들어갈
뿐 조회는 그대로 인터넷으로 나간다. 기억해 두게 하니 300ms 왕복에서
**340ms → 40ms**가 됐고 스피너가 아예 안 뜬다.

- **로그아웃하면 반드시 비운다.** `db.ts`가 `onAuthStateChange`를 직접 듣고
  사람이 바뀌면 통째로 버린다. 로그아웃 단추 자리에서 지우면 빠뜨리는 길이
  생긴다(토큰 만료, 한 폰을 둘이 쓰며 갈아타기). **`supabase.ts`가 `db.ts`를
  부르게 하지 말 것** — 두 파일이 서로 물고 돈다.
- **대화(`Chat`)와 수정 화면(`RoundEdit`·`PostEdit`)에는 안 쓴다.** 대화는
  안 읽음 줄과 스크롤 붙이기가 얽혀 있고, 수정 화면은 옛 값이 폼에 채워지면
  그대로 저장될 수 있다.
- 상세 화면의 키에는 **id를 붙인다**(`round:{id}`). 주소만 바뀌고 화면은
  살아 있어서(리액트가 다시 안 만든다), 키가 바뀌면 앞 내용을 지우는 처리가
  `useAsync` 안에 들어 있다 — 없으면 6차를 눌렀는데 5차가 남는다.
- 오래된 것부터 버린다(`CACHE_MAX` 30). 상세 화면이 쌓여도 안 불어난다.
- 화면들이 이미 `loading && !data`로 스피너를 띄우므로 **화면 코드는 안
  고쳐도 된다** — 키만 넘기면 된다. `if (loading)`만 보는 화면(대화)은 예외다.

### 사용법 화면은 실제 화면의 말을 그대로 쓴다

처음 들어온 분에게 보여 줄 곳이다(`screens/Help.tsx`). **공지로 올리지
않았다** — 새 공지가 쌓이면 묻히는데, 앱 안에 두면 늘 같은 자리에 있다.

들어가는 문이 둘이다:

- **`내 정보` 메뉴의 `앱 사용자 가이드`** — 한동안 **홈 머리말**에 있었는데
  (메뉴를 열어야 보여서 처음 들어온 분이 못 찾았기 때문이다),
  **그 자리를 🔔 알림에 내주고 메뉴로 돌아왔다**(사용자 요청 — `앱가이드
  위치를 다른데로 옮기고 그 자리에 종모양 알림을 만들어서`).
  머리말에 단추를 둘 세우면 얼굴까지 셋이라 이름이 긴 분의 화면에서
  줄이 접힌다. **양쪽에 두지 말 것**(어디로 들어갔는지가 헷갈린다).
  `.head-side`는 `flex: 0 0 auto`다: 이름이 길 때 단추가 눌려 글자가
  세로로 접히지 않게 붙들어 둔 것이다(320px에서 실제로 걸릴 자리).
- **승인 대기 화면의 `📖 기다리는 동안 사용자 가이드 보기`** — 승인 전에는
  라우터가 안 열려 `/help`로 못 간다. 그래서 `Pending`이 `<Help onBack>`을
  직접 띄운다. `TopBar`의 `onBack`은 **이것 하나 때문에 있다** — 거기서는
  주소를 옮겨 봐야 같은 화면이 다시 나오므로 띄운 쪽이 닫아야 한다.

**이름은 `앱 사용자 가이드`다**(사용자 요청). 예전엔 `까꿍 사용법`이었다 —
세 자리(메뉴·화면 제목·대기 화면 단추)가 같은 말을 쓰게 맞춰 두었으니
한쪽만 고치지 말 것.

**말투는 사용자가 정한 것이다** — 전문적인 안내문이다. 구어체로 되돌리지
말 것. 여기 적힌 버튼 이름은 실제 화면의 말이라(`신청하기` · `대기 신청` ·
`신청 완료` · `입금완료` · `복사` · `토스로 보내기` · `📣 대화방에 공유` ·
`📋 같은 조건으로 새로 열기` · `🚩 조 편성` · `📅 날짜로 항목 넣기` ·
`입금 알림 보내기` · `이 기기로 받기`), 화면 문구를 바꾸면 **이 파일도 함께
고칠 것** — 안 그러면 설명이 거짓말이 된다.

**기능을 새로 넣으면 이 화면에도 꼭지를 더할 것.** 아홉 꼭지가 앱의
차례가 되어 있다 — 홈 · 라운드 · 조 편성 · 리마인더 · 정산 · 투표와 공지 ·
대화 · 회원 정보 · 설치와 알림. 안 적으면 만들어 놓고 아무도 모르는
기능이 된다(조 편성·리마인더·토스 송금·날짜 고르기가 실제로 한동안
그랬다). 9번 안에 **🔔 알림함**도 적어 두었다 — 배너를 놓쳤을 때 되짚는
자리라, 안 적으면 그 자리가 있는 줄을 모른다.

9번의 `홈 화면에 추가`는 **웹으로 쓰는 동안만 필요한 안내다.** 앱으로
감싸면 그 단계가 없어지므로 그때 이 꼭지를 손볼 것(`docs/출시-전-할일.md`).

**꼭지 제목을 flex 로 두지 말 것.** 아이콘·번호·제목을 각각 한 덩어리로
보아서 제목이 길면 그것만 통째로 다음 줄로 밀린다(2번이 실제로 그랬다).

### 지난 것은 받지 않는다 — 무료 통신량을 지키는 규칙

**화면이 보여 주는 것만 받아 올 것.** 예전에는 홈과 탭바가 라운드를
`status <> 'cancelled'`로만 걸러 **지난 것까지 전부** 받았고, 신청 기록은
아예 표 전체를 받았다. 50명이 1년 쓴 상태를 만들어 재 보니:

| | 전 | 후 |
|---|---|---|
| 홈 한 번 열기 | 367 KB | **7 KB** |
| 대화 한 마디마다 다시 받는 양 | 367 KB | **2 KB** |
| 한 달 통신량(10명이 켜 둘 때) | 21 GB ❌ | **0.13 GB** ✅ |

무료 통신량은 **월 5GB**라 회원이 늘면 어느 날 앱이 통째로 멈춘다.
셋이 함께 걸려 있었다:

- **`upcomingSince()`(lib/format)로 자른다.** 홈·탭바가 `tee_at`에 이걸
  건다. **하루 여유를 두는 것은 일부러다** — 한국 날짜로 자르는 판단은
  화면의 `daysUntil`에 맡긴다. 두 군데서 시간대를 맞추면 어긋나는 날
  오늘 라운드가 사라진다.
- **신청 기록은 라운드에 딸려 받는다**(`select('*, signups(*)')`). 따로
  부르면 지난 라운드 것까지 다 온다. 화면 코드는 예전처럼 평평한 배열을
  받도록 받아 온 뒤에 편다. `types.ts`의 `Database`에 표 사이의 관계가
  안 적혀 있어 타입은 `unknown`을 거쳐 바꾼다 — 실행에는 문제가 없다.
- **안 읽은 대화 개수는 따로 센다**(홈의 `home:unread`). 예전에는 본
  조회에 끼워 두어 **대화 한 마디마다 라운드·신청·명단까지 다시 받았다.**
  개수만 세는 조회(`head: true`)는 몸통이 아예 안 실려 온다.

#### 100명으로 다시 쟀을 때 — 규칙이 셋 더 늘었다

**이 앱은 50~100명이 쓴다**(사용자가 정해 준 전제다). 100명·1년치로 다시
재 보니 홈 말고 **목록 화면 셋이 그대로 남아 있었다.**

| | 전 | 후 |
|---|---|---|
| 투표 목록 | 885 KB ❌ | **179 KB** |
| 라운드 목록 | 479 KB ❌ | **80 KB** |
| 나머지 화면 | 68~112 KB | **40~66 KB** |
| 한 달 통신량 | 5.07 GB ❌ | **1.03 GB** ✅ |

(투표 목록은 210 → 179로 한 번 더 줄였다 — 아래 `poll_id`를 뺀 이야기다.)

**`node .dev/scale.mjs 100`으로 언제든 다시 잴 수 있다** (`--shots`를 붙이면
그 상태의 화면도 찍는다 — 숫자만 봐서는 100명일 때 명단이 화면 여덟 장이
되는 걸 모른다). 새 화면을 만들거나 조회를 늘렸으면 이걸 돌려 볼 것.

- **목록은 한도를 정한다.** 페이지 넘기기가 없으므로 받는 대로 다 그린다 —
  한도가 없으면 해가 갈수록 목록도 통신량도 함께 불어난다.
  **지난 것만 자르고, 아직 안 끝난 것은 전부 받는다**(사용자 요청) —
  `PAST_ROUNDS`(10) · `DONE_POLLS`(10)가 그 한도이고, 예정된 라운드와
  진행중인 투표에는 한도가 없다. 놓치면 안 되는 쪽이고 애초에 몇 개 안 된다.
  **투표는 진행중과 끝난 것을 따로 부른다** — 한 번에 최근 것부터 받으면 끝난
  투표가 쌓인 주에 아직 안 끝난 투표가 목록에서 밀려난다.
- **한도 밖을 볼 길을 반드시 남길 것** — 목록 맨 아래의 `지난 라운드 더 보기` ·
  `지난 투표 더 보기`다(`MORE_ROUNDS`·`MORE_POLLS`만큼 더 받는다).
  **누를 때만 더 받으므로 평소 통신량은 그대로다.** 예전에는 한도 밖이
  **앱에서 아예 안 보였는데**, 그건 감추는 것이지 줄이는 것이 아니다.
  받아 온 줄 수(`pastGot`·`doneGot`)가 한도에 닿았을 때만 단추를 내놓는다 —
  화면에 그려진 개수로 세면 안 된다(가리개로 거른 뒤라 수가 안 맞는다).
- **`진행중`을 `closed=false`로만 고르지 말 것.** 마감 시각이 지났는데
  아무도 손으로 안 닫은 투표는 그 칸이 그대로 `false`라, 한도를 풀어 두면
  **그런 것이 해마다 쌓여 목록이 통째로 길어진다.** 그래서 `Polls`는
  **시각으로 갈라 네 번 부른다** — 아직 안 끝난 것 둘(시각이 남았거나,
  마감 시각이 아예 없는 옛 투표거나)과 끝난 것 둘(손으로 닫았거나, 시각이
  지났거나). 넷을 합치는 잣대가 곧 `pollClosed()`다.
  - **`or`로 한 번에 묶지 말 것** — `.dev/rest.mjs`의 흉내가 그것을 못 풀어서,
    쓰면 **검사는 통과하는데 화면만 빈다.** 넷 다 이미 쓰던 조건들이다.
  - **마감 시각이 없는 옛 투표를 빠뜨리지 말 것.** 지금은 필수라 새로 안
    생기지만 이미 올라간 것이 있고, `is('closes_at', null)` 갈래가 빠지면
    **그 투표가 목록에서 통째로 사라진다.**
  - 그 자리에서 흉내도 함께 고쳤다 — **빈 값은 견주는 것에 하나도 안 걸려야
    한다**(`lt`·`gt`·`gte`·`lte`). `String(null)`이 `'null'`이 되어
    `gte`에 걸리는 바람에 옛 투표가 진행중과 마감에 두 번 들어앉았다.
- **안 쓰는 칸을 받지 않는다.** `select('*')`가 편해서 그렇지, 100명분·
  1년치가 되면 안 쓰는 칸만으로 화면마다 수십 KB다. 좁힌 타입 넷을
  `types.ts`에 두었다 — `Person`(명단) · `RoundLite`(목록 카드) ·
  `SignupLite`(자리 수) · `PollVoteLite`(표). **명단은 `fetchPeople()`,
  회원 명단 화면만 `fetchProfiles()`**(전화번호를 거기서만 보여 준다).
- **딸려 받는다.** `poll_votes`를 따로 부르면 지난 투표 것까지 다 온다.
  `polls(*, poll_options(...), poll_votes(...))`처럼 부모에 매달아 받는다 —
  홈이 신청 기록을 라운드에 매다는 것과 같다.
- **딸려 받았으면 `poll_id`를 다시 받지 말 것.** 어느 투표 것인지는 딸려 온
  자리가 이미 말해 준다. 표 한 줄이 `option_id`·`user_id` 둘뿐이라 UUID
  하나가 줄의 3분의 1인데, 그걸 줄마다 또 받고 있었다(**210 → 179 KB**).
  **그래서 화면도 평평한 배열로 펴지 않는다** — 펴려면 `poll_id`가 있어야
  하므로, 투표 id를 열쇠로 한 표(`Record<string, …>`)로 들고 있다가 카드에
  나눠 준다. `PollDetail`은 한 투표만 부르므로 애초에 아는 값이다.
  같은 이유로 항목도 `poll_options(id, label, sort)`만 받는다.

**DB 용량은 걱정할 것이 없다** — 50명 1년치가 18MB였으니 100명이면 40MB
언저리다(무료 500MB). 통신량도 위처럼 잡아 두었다.

**남은 한 가지는 사진이다.** 무료 Storage는 **1GB**이고 대화 사진은 한 장
**700~800KB쯤**(긴 변 2560px · JPEG 82%)으로 줄여 올린다 — 그냥 두면
**1,300장 남짓에서 차고, 차면 아무도 사진을 못 올린다.**

**그래서 90일이 지난 사진은 지운다**(카톡의 `저장 기간 만료` · 사용자 제안 —
`일주일이 지나면 올린사진을 자동으로 삭제하면되는거 아니야?`). 지우면
쌓이지 않고 늘 300MB 언저리에서 멈춘다(주 30장 · 한 장 800KB 기준).
**이 청소가 있어서 화질을 2560px으로 올릴 수 있었다** — 안 지우면 1,300장에서
막힌다. **기간은 사용자가
90일로 정했다** — 7일도 되지만 골프 모임 사진은 추억이라 한 시즌은 남긴다.

- **한 달 통신량(5GB)은 이걸로 안 줄어든다.** 그건 사람들이 사진을 **볼 때**
  나가는 것이고, 보는 일은 올린 직후 며칠에 다 일어난다 — 나중에 지워도
  이미 나간 것은 그대로다. 통신량이 걱정이면 손댈 곳은 **사진 크기**다.
  다만 통신량은 매달 0으로 돌아가고 저장 공간은 안 돌아가므로, 진짜
  막다른 길이었던 쪽은 이쪽이다.
- **정해진 시각에 도는 것(pg_cron)을 새로 켜지 않았다.** 대화방을 연
  사람의 화면이 청소한다(`lib/photos.ts`의 `purgeOldPhotos`) — 투표 결과를
  대화방에 남기는 일(`announceClosedPolls`)과 같은 결이다. 아무도 앱을
  안 열었으면 지울 일도 급하지 않고, 대화는 날마다 여는 화면이라 늦어야
  하루다. **서버도, 붙여넣을 비밀값도 없이 되는 것이 이 방식의 값이다.**
- **회원 누구나 지울 수 있게 열었다**(`chat_photos_old` 정책). 제 사진만
  지우게 두면 **앱을 떠난 사람의 사진이 영영 남는다.** 나이로 잠가 두어
  (`created_at < now() - interval '90 days'`) 요즘 사진은 누구도 이 정책으로
  못 지운다 — 90일이 지난 것은 어차피 지워질 것이라 위험이 없다.
  **`lib/photos.ts`의 `PHOTO_DAYS`와 같은 값이어야 한다** — 앱이 90일로
  골라 놓고 DB가 안 열어 주면 지우려다 조용히 막힌다.
- **DB에는 아무것도 안 쓴다.** 지워진 자리는 화면이 알아챈다 — 그림을
  못 받아 오면 `ChatPhoto`가 `사진 저장 기간이 만료되었습니다`로 바꾼다
  (글도 주소도 그대로 남는다). 칸을 늘리지 않으려는 것이고, 지우는 것과
  표시하는 것이 어긋날 자리도 없앤 것이다.
  **그 자리는 사진만큼 차지해야 한다** — 줄어들면 읽던 자리가 위아래로
  튄다(이모티콘 조각에서 겪은 그 자리다).
- **프로필 사진(`avatars`)은 안 지운다.** 늘 보이는 것이고 한 사람에
  한 장이라 쌓이지 않는다.
- 넘기면 사진만 안 올라간다(글은 그대로 오간다).

- **크기는 2560px이다. 1600px으로 되돌렸다가 다시 올렸다 — 그 왕복에서
  얻은 것을 적어 둔다.** 2560px으로 키운 직후 사진이 아예 안 올라가서
  (`사진 크기를 키운 후로 안돼`) 크기부터 되돌렸는데, **범인은 크기가
  아니라 사진 고르는 창이었다**(9판의 팝오버 — 위 '글칸은 앱에서
  네이티브다'의 `afterSheet`). 올라가는 것을 확인한 뒤 되올렸다.
  - **되돌린 것 자체는 옳았다.** 원인을 모르는 채로는 방금 바꾼 것을
    되돌려 놓고 보는 것이 가장 빠르다 — 그 사이에도 사진은 보내야 한다.
  - **그래도 교훈은 남는다: 크기를 만질 때는 화질만 볼 것이 아니라
    정말 올라가는지부터 볼 것.** `.dev/behave.mjs`의 `사진을 줄여서
    올린다` 칸이 4032px짜리를 넣어 **실제로 나가는 바이트**를 잰다
    (2015KB → 743KB). 거기 한도는 '한 장이 몇 KB인가'가 아니라
    **원본이 그대로 나가지는 않는가**를 보는 값이라 넉넉하다.
  - **엉뚱한 데를 되돌리지 않으려면 시간 순서를 볼 것.** 이번에 안 올라간
    것은 **9판부터**였는데 사용자가 알아챈 것은 10판(크기) 뒤였다 —
    `언제부터 안 됐나`가 아니라 `그 사이에 무엇이 바뀌었나`를 다 늘어놓고
    골라야 한다.
  손볼 자리는 `lib/image.ts`의 `MAX_EDGE`·`QUALITY`와 앱의 `jpegBase64`인데,
  **저장 공간이 곧 사진 장수라 사용자에게 묻고 바꿀 것.**
- **웹이 올리기 직전에 한 번 더 잰다**(`sendPhoto`의 `shrinkImage`).
  앱도 제 나름대로 줄여서 주지만(`jpegBase64`) **앱은 새로 깔아야 바뀐다** —
  옛 앱을 든 폰에서는 웹이 아무리 고쳐도 큰 사진이 그대로 온다. 실제로
  그 일이 났을 때 웹만 밀어서 고칠 길이 없었다. **이 줄을 빼지 말 것.**
  `shrinkImage`는 이미 작으면 그대로 돌려주므로 헛일을 안 한다.
- **사진 고르기가 실패하면 조용히 돌아서지 말 것.** `catch(() => null)`
  하나로 '취소'와 '고장'을 같이 삼키고 있어서, 사진이 안 올라가는데
  **아무 말도 안 떴다** — 어디가 막힌 것인지 알 길이 없었다. 지금은
  고장이면 문구를 띄우고 **웹 칸으로 물러난다.**
- **`imageSmoothingQuality: 'high'`를 지우지 말 것 — 공짜로 얻는 것이다.**
  브라우저의 기본값은 `low`인데, 4032px을 크게 줄일 때 계단이 지고 잔무늬가
  생긴다. 재 보니 **화질이 나쁘면서 파일은 오히려 더 컸다**(1600px 기준
  529KB·차이 19.1 → `high`로 449KB·차이 16.5) — JPEG이 그 잡티까지
  저장하기 때문이다. 앱 쪽 `interpolationQuality = .high`가 같은 몫이다.
- **줄이는 셈이 두 군데다 — 한쪽만 고치지 말 것.** 웹은 `lib/image.ts`,
  앱은 `NativeComposerPlugin.swift`의 `jpegBase64`다(8판부터 사진을 앱이
  고르므로 실제로는 그 길로 올라간다). 값이 갈리면 **어느 길로 올렸느냐에
  따라 화질이 달라진다.**

### 실시간

`useRealtime(테이블, 다시불러오기)`(lib/db.ts)를 쓴다. 들어온 행을 직접 갈아
끼우지 않고 **다시 불러온다** — 신청 하나가 들어오면 대기자가 확정으로 바뀌는
식으로 다른 행까지 함께 움직이므로, 들어온 행만 반영하면 화면이 어긋난다.
250ms 모아서 한 번만 부른다. 위의 '기억해 두기'와 짝이다 — 먼저 보여 준
옛 내용은 이 갱신이 곧바로 최신으로 맞춰 준다.

**들어오기 전 대화는 안 보인다.** 카톡처럼 승인된 시점부터만 읽힌다 —
`profiles.joined_at`에 도장을 찍고(`stamp_joined_at` 트리거) `messages_read`
정책이 `created_at >= chat_since()`로 거른다. **화면에서 거르지 않는다** —
그러면 통신에는 옛 대화가 다 실려 온다. 도장이 없는 행은 `-infinity`로 보아
전부 보여 준다: 못 보는 쪽으로 틀리면 대화가 통째로 사라진 것처럼 보인다.

**대화 화면은 카톡의 짜임을 빌린다.** 카톡 오픈톡에서 넘어온 분들이라
손에 익은 대로여야 한다는 게 사용자 요구였고, 정말로 '익숙함'을 만드는
것은 **크기와 짜임**이었다 — 아래 규칙 다섯과 픽셀로 잰 크기가 그것이다.

- **바탕은 보라다**(`--chat-bg` `#7369a0`, 사용자 요청 — 카톡처럼).
  한동안 카톡의 청회색이었다가 앱 바탕(`--surface-2`)으로, 다시 어두운
  남보라(`#2c2e3b`)로 갔다가 **사용자가 보여 준 카톡 화면에서 값을 그대로
  뽑아** 여기로 왔다. **눈대중으로 고르지 말 것** — 화면 사진에서 빈 자리
  픽셀을 찍어 얻은 값이다. **색이 바뀌는 것은 `.chat-list` 하나뿐이다** —
  머리말·입력칸·서랍은 밝은 그대로다.
- **그 바탕 위에 쓸 색은 `.chat-list`에 모아 두었다**
  (`--chat-chip` 날짜·안내 줄 바탕 · `--chat-on` 그 위의 글자 ·
  `--chat-soft` 이름 · `--chat-faint` 시각 · `--chat-line` 가는 줄).
  전부 **흰색의 투명도**라 바탕색을 바꿔도 함께 따라온다 — 실제로
  남보라에서 보라로 옮길 때 이 다섯은 투명도만 손봤다.
  얹히는 것이 열 가지가 넘어 한 군데씩 색을 박으면 반드시 하나를
  빠뜨린다. **뜻이 있는 색(분홍·잔디·노랑·빨강)은 그대로 쓴다.**
- **말풍선 테두리는 두께만 남기고 색을 지웠다**(`1px solid transparent`).
  밝은 바탕에서 흰 말풍선이 흐려지지 않게 두른 선이었는데 보라 위에서는
  할 일이 없다. **두께를 없애지 말 것** — 한 줄 높이 셈이 그걸 안고 있다.
- 내 말풍선은 **아이콘의 노랑**(`#ffdf47`). 오른쪽이 따뜻한 노랑인 배치는
  그대로라 쓰던 느낌이 남는다. **분홍으로 칠하지 말 것** — 분홍은 '지금
  눌러야 할 것' 자리이고, 말풍선이 화면을 채우면 그 뜻이 무너진다.
  이 화면에서 분홍은 **보내기 단추 하나**다.

카톡에서 옮겨온 규칙 넷 — 하나씩 다 이유가 있다:

1. **묶는 단위는 '같은 사람 · 같은 분'이다.** 5분으로 묶었더니 9시 09분과
   9시 10분 글이 한 덩어리가 되어, 카톡이라면 이름이 다시 붙을 자리가
   비어 보였다. `kstMinute()`(lib/format)로 견준다.
2. **시각은 덩어리의 마지막 줄에만 적는다.** 줄마다 붙이면 같은 시각이
   서너 번 되풀이돼 지저분하다.
   **사진에 글을 함께 보내면 그 글이 마지막 줄이다** — 시각을 사진 옆에
   찍으면 그 아래로 글이 더 이어져 어색하다. 그 글줄도 **`.chat-line`으로
   감싼다**: 그냥 두면 `.chat-col`이 늘여서(`align-items: stretch`) 한 마디가
   사진보다 넓게 퍼졌다.
3. **아바타는 동그라미가 아니라 모서리 둥근 네모다.** 사소해 보이지만
   '카톡 같다'는 인상에 은근히 크게 작용한다.
4. **보내기 단추는 늘 그 자리에 있고, 보낼 것이 없으면 흐려질 뿐이다.**
   한동안 '적은 게 있을 때만' 내놓았는데 **사용자 제보로 되돌렸다** —
   카톡은 부드러운데 여기는 깜빡인다고 했다. 붙였다 떼면 두 가지가 같이
   나빠진다: 단추가 생기는 순간 글칸이 **44px 좁아져 치던 글이 옆으로
   밀리고**(314 → 270px으로 쟀다), 한글 조합 중에 값이 잠깐 비어 보이는
   순간마다 **깜빡인다**. 사용자가 보여 준 카톡 화면도 그 자리에 단추가
   늘 있다(`#` → `↑`로 그림만 바뀐다).
   **`display`로 감추지 말 것** — 자리가 없어졌다 생기는 것이 곧 그 밀림이다.
   **켜지는 기준은 글자가 아니라 초점이다** — 글자로 정하면 조합 중에 다시
   깜빡인다. 댓글의 `등록` 단추가 같은 이유로 이미 그렇게 되어 있다.
   초점이 떠나도 적어 둔 글이나 골라 둔 이모티콘이 있으면 켜 둔다.
   빈칸일 때 눌러도 `send()`가 그냥 돌아선다.
   **글자마다 도는 값이 아니라 안 무겁다** — `focused`는 초점이 오갈 때만
   바뀌므로 치는 동안에는 화면이 다시 안 그려진다.
5. **이모지만 보낸 짧은 글(세 개까지)은 말풍선을 벗기고 크게 그린다.**
   작은 말풍선 안에 이모지 하나가 들어앉아 있으면 그것대로 초라하다.
   내 글의 노란 바탕도 함께 사라진다. 사진에 함께 적은 글은 그대로 둔다 —
   사진 아래 붙는 한 줄이라 거기서만 글자가 커지면 짜임이 무너진다.
   가려내는 규칙은 `lib/emoji.ts`에 있다. 두 가지를 조심할 것:
   **`\p{Extended_Pictographic}` 하나로 보면 `©`·`®`까지 걸린다**(그래서
   `Emoji_Presentation` · `U+FE0F` · 나라 글자 · 키캡 넷으로 가른다), 그리고
   **글자 수로 세면 안 된다** — `👨‍👩‍👧`나 `👍🏽`은 한 덩어리인데 여럿으로
   세어져 그냥 지나간다. `Intl.Segmenter`로 눈에 보이는 덩어리를 센다.

**입력칸 줄도 카톡을 따라 손봤다**(사용자 요청 — `+`를 더 왼쪽으로,
디자인을 카톡처럼). 카톡의 그 줄은 **`+`가 화면 가장자리에 거의 붙고**
가운데가 통째로 글칸이다.

- `.chat-input`의 좌우 여백만 `--gap`(16px)에서 `--gap-xs`(6px)로 줄이고
  `.chat-bar`의 사이를 2px로 좁혔다. **이 줄은 목록이 아니라 자판에 가까운
  줄이라** 다른 화면의 여백을 그대로 따를 이유가 없다. 대신 그 위에
  쌓이는 인용·언급 목록·이모티콘 미리보기는 화면 끝에 닿아 보여서
  `margin`으로 예전 여백을 되돌려 놓았다.
- **글칸의 테두리 색을 지웠다**(`border-color: transparent`, 초점이 가도
  그대로). 카톡의 글칸은 선이 아니라 한 톤 낮은 칠 하나로 선다.
  **`border: 0`으로 하지 말 것** — 한 줄 높이 셈(38px)이 두께를 안고 있다.
- **이모티콘 단추의 동그란 칠도 걷어냈다.** 글칸 안에 알약이 하나 더
  들어앉은 꼴이라 눌러야 할 것이 둘로 보였다. 켜진 표시는 색으로만 한다.
- `+`는 흐린 안내가 아니라 **또렷한 획**이다(`--text-dim`이 아니라 `--text`).
- 보내기 단추의 그림은 **위쪽 화살표**다(카톡과 같다. 예전엔 오른쪽이었다).
  색은 그대로 분홍이다 — 이 화면에서 '지금 눌러야 할 것'은 이것 하나다.

### 안 읽은 사람 수 (말풍선 옆의 숫자)

카톡과 같은 숫자다. **다 읽으면 사라진다.** 셈은 `lib/reads.ts`,
자리는 `Stamp`(Chat.tsx) — 시각과 한 덩어리로 세로로 쌓아, 숫자가 생기거나
사라져도 말풍선이 흔들리지 않는다. 색은 노랑이다 — '기다리는 것'
자리이고 분홍(지금 눌러야 할 것)과 부딪히지 않는다.

- **그 노랑은 `--chat-unread`(`#ffdf47`)다**(사용자 요청 — 카톡처럼).
  뜻이 있는 노랑 `--warn`(`#b97c00`)은 **흰 바탕에서 읽히라고 낮춰 둔 값**이라
  보라 위에서 누렇게 죽어 보였다. 여기만 아이콘의 밝은 노랑을 쓴다 —
  내 말풍선(`#ffdf47`)과 같은 값이라 화면 안에서 겉돌지 않는다.
  **`--warn` 자체를 밝게 올리지 말 것** — 그건 밝은 화면들이 같이 쓰는
  색이라 거기서 안 읽히게 된다. `.chat-list`의 팔레트에 함께 두었다.

- **글마다 '누가 읽었나'를 적지 않는다.** 100명이 하루 100마디면 그 기록만
  하루 만 줄이다. `room_reads`는 **사람마다 한 줄**(`last_read_at`)이라
  100줄로 끝나고 해가 지나도 안 는다. '이 글을 읽었나'는 화면이 셈한다.
- **`lib/unread.ts`와 다른 것이다.** 그쪽은 *내가* 어디까지 봤나(탭바의
  빨간 숫자)라 기기에 남기고, 이쪽은 *남들이* 어디까지 봤나라 서버에 있다.
- **읽음 이벤트로 다시 불러오지 말 것.** `useRealtime`은 다시 불러오는데,
  읽음은 사람이 볼 때마다 바뀌어서 그러면 100명분 명단을 하루에 수백 번
  다시 받는다. 읽음은 **줄끼리 안 얽히므로** 들어온 줄만 갈아 끼운다
  (대화 글을 덧붙이기만 하는 것과 같은 이유다).
- **시각은 서버가 찍는다**(`mark_room_read`). 폰 시계가 몇 초 어긋나면
  방금 온 글보다 앞선 시각이 박혀 **읽었는데도 숫자가 안 준다.**
- **쓰기는 모았다가 한 번만.** 마지막 글이 밀렸을 때만, 700ms 모아서 보낸다.
  앱을 덮어 두면(`document.hidden`) 안 보낸다 — 읽고 있는 게 아니다.
  `node .dev/behave.mjs`가 '한 번만 나가는가'를 숫자로 확인한다.
- **줄이 없는 사람은 '한 번도 안 읽음'으로 센다.** 그래서 **줄을 빠짐없이
  만들어 두는 것이 이 기능의 전부다.** 빠지면 그 사람 몫이 지난 글 전부에
  `+1`로 얹혀 영영 안 내려간다.
- **이미 있는 회원의 첫 줄은 `now()`로 찍는다. `joined_at`으로 찍지 말 것.**
  몇 달째 읽어 온 사람도 가입 시각으로 찍히면, 대화를 한 번 열기 전까지
  **지난 몇 달치가 전부 안 읽음으로 보인다** — 실제로 그렇게 넣었다가
  '숫자가 3~4에서 안 내려간다'는 제보를 받았다. 켜기 전 이야기는 다 읽은
  것으로 보는 게 맞다(그전 기록이 없어 지어낼 수가 없다). schema.sql 맨
  아래의 `insert ... on conflict do nothing`이 그 몫이고, 다시 돌려도
  사람들의 읽음 자리는 안 되돌아간다.
- **승인되는 순간 읽음 줄을 만들어 둔다**(`seed_room_reads` 트리거).
  없으면 승인만 받고 대화를 한 번도 안 연 사람 때문에 지난 글이 전부
  `+1`로 굳는다 — 그 사람은 들어오기 전 글을 **볼 수도 없는데**(`chat_since()`).
- 대기·추방은 셈에서 뺀다. 못 보는 사람이라 세면 숫자가 영영 안 준다.
- **100명 방에서는 0이 잘 안 된다** — 늘 아직 안 연 사람이 몇은 있다.
  카톡도 큰 방에서 그렇다. 숫자를 줄이려 들지 말 것(그게 사실이다).

**`여기까지 읽으셨습니다` 줄**(`.chat-unread`)은 못 읽은 동안 쌓인 글이
어디서부터인지 알려 준다. 두 가지를 조심할 것:

- **들어온 순간의 `lastSeen`을 얼려 둔다.** 같은 화면의 `markSeen`이 새 글이
  올 때마다 그 값을 지금으로 밀어 버리므로, 효과 안에서 읽으면 늘 '안 읽음
  없음'이 된다. 첫 렌더에서(효과보다 **먼저**) `useRef`에 한 번만 집는다.
- **줄 자리는 한 번 정하고 안 바꾼다**(`unreadDone`). 보고 있는 동안 들어온
  글이 '안 읽음'으로 잡혀 줄이 자꾸 아래로 따라 내려가면 그게 더 성가시다.

내가 쓴 글은 세지 않고(다른 기기에서 보낸 것), **이 기기로 처음 들어왔거나
줄이 맨 첫 글에 붙을 때는 아예 긋지 않는다** — 위가 비어 있어 뜻이 없다.

**그 줄에서 최근 대화로 내려오는 화살표를 둔다**(`.chat-jump` · 사용자 요청 —
카톡에 있는 그것). 위 규칙대로 들어오면 **읽던 자리**에 내려놓으므로, 밀린
글이 많은 날에는 최근 대화까지 한참을 굴려 내려가야 했다.

- **굴릴 때마다 state를 건드리지 말 것.** 말풍선이 `memo`라도 화면이 매번
  다시 그려지면 긴 대화에서 눈에 띄게 끊긴다(입력칸에서 겪은 것과 같은
  자리다). 참/거짓이 **뒤집힐 때만** `setShowJump`를 부른다.
- **띄우는 잣대(`JUMP_AT` 240px)와 `atBottom`의 80px을 벌려 놓았다** — 두
  값이 붙어 있으면 바닥 언저리에서 단추가 떴다 사라졌다 깜빡인다.
- **부드럽게 굴리지 않는다.** 300개까지 받아 둔 목록을 훑어 내려가는 일이라
  느린 폰에서 그대로 끊긴다. `scrollTop`을 한 번에 준다.
- **`.chat-recent`와 함께 뜨지 않는다**(`!windowed`). 검색으로 옮겨 간
  동안에는 목록에 최근 대화가 아예 없어 굴려도 소용이 없고, 그 자리는
  다시 받아 오는 `최근 대화로 ↓`가 맡는다. 생김새를 그쪽과 같게 둔 것은
  **입력칸 위에 떠서 목록을 건너뛰게 하는 단추가 둘**이기 때문이다 —
  다르게 생기면 같은 손짓으로 안 읽힌다.
- **마지막 대화를 함께 적는다**(사용자 요청 — 카톡의 그 줄). 얼굴 · 이름 ·
  한 줄 미리보기 · `↓` 순서로, 좌우 여백만 남기고 펼친 줄이다. 동그란
  화살표 하나였을 때는 **'아래에 뭐가 있나'를 눌러 봐야 알았다.**
  - 자리는 처음에 오른쪽 끝 → 가운데 → 지금의 한 줄로 옮겨 왔다.
    **동그라미로 되돌리지 말 것** — 가운데 동그라미는 마지막 말풍선을
    덮었고, 글을 적으려면 어차피 줄 하나가 필요하다.
  - **한 줄로 자른다**(`min-width: 0` + 말줄임). 두 줄이 되면 그만큼
    말풍선을 더 덮는다. `min-width: 0`이 없으면 flex 칸이 글자 길이만큼
    벌어져 말줄임이 아예 안 걸린다.
  - 미리보기는 인용과 같은 `preview()`를 쓴다 — **가린 글이 여기로 새면
    안 되기 때문이다**(사진·이모티콘도 같은 말로 나온다).
  - 안내 줄(`system`)에는 얼굴도 이름도 안 붙는다. 말풍선에서 그렇게
    그리는 것과 같은 잣대다.
  - 이름은 **닉네임 그대로**다(`83/신성호/광산구`가 아니라) — 긴 이름표는
    목록의 이름 자리 몫이고 이 줄은 문장에 가깝다(홈의 `내 조` 줄과 같다).

**보라 목록 위에 `.btn`을 얹을 때는 클래스 하나로 색을 되짚지 말 것.**
`지난 대화 더 보기`가 `.chat-more` 한 마디로 색을 덮어쓰고 있었는데,
`.btn.ghost`가 클래스 둘이라 그쪽이 이겨 **되짚어 둔 값이 하나도 안 먹었다** —
밝은 화면용 회녹색 글자(`--text-dim` `#5b6455`)가 보라(`#7369a0`) 위에 그대로
나와 대비가 2:1이었다(사용자 제보 — 안 보인다). 지금은
`.chat-list .btn.chat-more`로 확실히 이기고, 선 하나 대신 **날짜 칸과 같은
`--chat-chip` 바탕**을 깔아 눌러야 하는 것으로 읽히게 했다.
**고쳤다고 믿지 말고 `getComputedStyle`로 잴 것** — 이 자리는 눈으로도
'좀 흐리네' 정도로만 보인다.

**쉰 명이 떠들면 하루에 100~200개가 쌓인다.** 그래서 두 가지가 더 붙어 있다:

- **안 읽은 개수를 먼저 세어 그만큼 더 받는다**(`MAX_CATCHUP` 300까지).
  `PAGE`(50)만 받으면 줄이 그 안에 없어 **정작 밀린 사람에게만 안 뜬다.**
  개수 세기는 `head: true`라 몸통을 안 받는다.
- **그 줄 자리로 화면을 옮겨 준다.** 맨 아래에 내려놓으면 어디부터 읽을지
  스스로 찾아 올라가야 한다. 줄을 위에서 100px에 두어 **마지막으로 읽은 글이
  한 줄 보이게** 하고 거기서부터 아래로 읽게 한다.
  이 효과는 **`pinBottom`보다 뒤에 선언해야** 나중에 돌아 이긴다. 줄을 찾은
  순간 `atBottom`을 false로 내려 두는 것도 같은 이유다 — 안 그러면 새 글이
  올 때마다 맨 아래로 끌려 내려간다.

**대화 화면의 크기는 카톡 스크린샷을 픽셀로 재서 맞춘 값이다.** 눈대중으로
고치지 말 것.

**재는 법이 정해져 있다**(사용자 제보 — `오픈톡을 거의 옮겼다 싶을 정도로`).
사용자가 카톡 화면과 우리 화면을 **같은 폰에서 잇따라** 찍어 주면, 우리 쪽
사진에서 **아는 크기의 요소**(창 좌우 여백 `--gap` 16px이 사진에서 56픽셀
이었다)로 **배율부터 구한다**(3.5배). 머리말 44px이 152픽셀인 것과도 맞았다.
그 배율로 카톡 쪽을 환산해 값을 얻고, `node .dev/ktalk.mjs`가 **같은 크기
(345×844)·같은 배율(3.5)** 로 우리 화면을 찍어 같은 자로 잰다.
`.dev/behave.mjs`의 `대화 화면의 크기` 칸이 그 값을 붙들어 둔다.

**잰 값과 지금 값**(345px 화면 기준):

| | 카톡 | 우리 |
|---|---|---|
| 목록 좌우 여백 | 8.6px | **9px** |
| 아바타 · 아바타→말풍선 | 29.1 · 6.9 | **29 · 7** |
| 말풍선 왼쪽 x | 44.6 | **45** |
| 말풍선 바탕 | `#f5f5f5` | **`#f5f5f5`** |
| 말풍선 모서리 | 11.1 | **11px** |
| 말풍선 최대 폭 | 233.7 | **236** |
| 바탕색 | `#7369a0` | **`#7369a0`** |

**글자 크기와 안여백은 앱이 실기기에 깔린 뒤 다시 쟀다**(사용자 요청 —
`실제 앱이니 카톡과 채팅창 글씨크기를 똑같이해줘`). **위 표의 배율(3.5)로
잰 값들과 섞지 말 것** — 이번 것은 iPhone 16 Pro(1206×2622)에서 **우리
아바타 29px이 사진에서 87픽셀**인 것으로 배율을 구했고 그 값은 **3.0**이다.

| | 카톡(실기기) | 우리 |
|---|---|---|
| 줄 간격 | 54픽셀 = **18.0px** | **18px**(15 × 1.2) |
| 한 줄 말풍선 | 105픽셀 = **35.0px** | **35px**(18 + 7.5×2 + 테두리 2) |
| 가로 안여백 | 32/34픽셀 ≈ **11px** | **10 + 테두리 1** |
| 글자 잉크 | 39픽셀 = 13.0px | 39픽셀(15px Pretendard) |

**예전 값(13px)이 왜 작았는가가 이 자리의 교훈이다.** 그때는 헤드리스
사진으로 견줬는데 **거기서는 CDN이 막혀 Pretendard가 아예 안 온다** —
대체 글꼴로 그려져 잉크가 부풀어 보였고, 그걸 보고 15 → 14 → 13으로 두 번
내렸다. 실기기에서 재니 되레 카톡보다 한참 작았다.
**`14px 이상으로 올리지 말 것`이라고 적어 두었던 줄은 그래서 걷어냈다.**
다시 만질 일이 생기면 **줄 간격과 한 줄 말풍선 높이**로 견줄 것 —
그 둘은 글꼴과 무관해서 잉크보다 정확하다.
이름·시각·안 읽은 수·인용도 같은 비율로 함께 올렸다
(이름 15 × 0.90 = 13.5 · 시각 15 × 0.667 = 10).

- **`.chat-col`의 87%가 핵심이었다.** 78%로 두면 시각·안 읽은 수가 차지하는
  만큼 빠져 말풍선이 196px에서 접혔다 — `이번 주 무등산 날씨 어떤가 / 요?`
  처럼 **한 글자가 넘어가는 것**이 화면을 좁아 보이게 하던 정체다.
- **말풍선은 순백이 아니다**(`--chat-bubble` `#f5f5f5`). 보라 위에서 순백은
  한 톤 튀어 보인다. 이름 색도 카톡에서 뽑으면 **흰색 77%**다(0.92가 아니라).
- **테두리 1px은 그대로 두고 안여백에서 뺐다.** 한 줄 높이 셈이 그것을
  안고 있으므로, 숫자를 만질 땐 테두리까지 넣어 다시 잴 것
  (`document.querySelector('.chat-bubble').getBoundingClientRect().height`).

**글자 크기는 잉크로 견주지 말 것 — 비율로 맞춘다.** 카톡은 Apple SD Gothic
Neo, 우리는 Pretendard라 같은 px에서도 잉크 높이가 다르고, **헤드리스에서는
CDN이 막혀 우리 글꼴이 아예 안 온다**(대체 글꼴로 그려져 잉크가 부풀어 보인다).
대신 **같은 화면 안에서의 비율**은 글꼴과 무관하다 — 카톡에서
`이름 : 말풍선 글자 = 10.0 : 11.1 = 0.90`, `시각 : 말풍선 글자 = 0.667`이라,
말풍선 15px에 맞춰 **이름 13.5px · 시각·안 읽은 수 10px**로 두었다.
**말풍선 글자는 15px이다**(위 실기기 표 참고).
보낸 글도 굵게 하지 않는다(같은 크기라도 두툼해 보인다).

**말풍선 꼬리도 넣었다** — 카톡의 그 **왼쪽 위로 삐져나온 작은 뿔**이다
(내 글은 오른쪽). 5×4px짜리라 작지만 '카톡 같다'는 인상에는 크게 든다.
`.chat-bubble::before` 삼각형 하나(`clip-path`)이고, 규칙이 넷이다:

- **덩어리의 첫 말풍선에만**(`:not(.grouped)`). 같은 사람이 같은 분에
  잇따라 보낸 줄에는 이름도 아바타도 안 붙는데 뿔만 줄줄이 달리면 덩어리로
  묶은 뜻이 없어진다 — 카톡도 첫 줄에만 있다.
- **안 붙는 곳이 셋이다.** 사진·이모티콘은 애초에 `.chat-bubble`이 아니라
  저절로 빠지고, 이모지만 보낸 글(`.emoji-only`)은 말풍선을 벗긴 것이라
  붙일 데가 없으며, 사진 아래 딸린 글(`.chat-cap`)은 위가 사진으로 막혀 있다.
  **`.chat-cap`은 그러라고 붙여 둔 클래스다** — 지우지 말 것.
- **뿔이 붙는 모서리만 둥글기를 3px로 줄인다.** 11px 그대로 두면 그 자리에서
  가장자리가 안쪽으로 11px이나 들어와 있어 뿔과 몸통 사이에 초승달 모양
  틈이 남는다(카톡도 그 모서리는 거의 각져 있다). **나머지 세 모서리는
  11px 그대로**이고, `.dev/behave.mjs`는 그래서 **아래쪽 모서리로** 그
  값을 잰다.
- **내 말풍선의 테두리 색(`#edc82c`)은 뿔에 안 두른다.** 5px짜리에 1px
  테두리를 그리려면 겹판이 하나 더 필요한데, 보라 바탕 위에서 그 1px은
  보이지도 않는다. `filter`·`box-shadow`도 안 쓴다(말풍선이 수십 개다).

뿔은 말풍선 밖으로 나가므로 `.dev/audit.mjs`가 `글자 넘침 가로 +6`으로
잡았다 — **일부러 바깥에 내놓은 장식(`::before`/`::after`가 `absolute`)은
넘침으로 안 세게** 그 검사에 예외를 하나 넣어 두었다.

**우리 아바타의 색 테두리(남녀 구분)는 카톡에 없는 것인데, 그건 사용자가
고른 기능이라 그대로 둔다.**

**`html`에 `-webkit-text-size-adjust: 100%`를 못박아 두었다**(global.css).
iOS 사파리는 글이 길게 이어진 칸에서 **글씨를 저 알아서 키운다**(텍스트 자동
확대) — 폰에서만 도는 일이라 헤드리스로는 안 잡히고, 대화 글씨만 유독 커
보이는 자국으로 나타난다. **지우지 말 것.**

**입력칸(`.textarea`)과 안내 글씨(`.chat-hint`)의 16px은 안 내린다** —
16px 아래로 내리면 iOS 사파리가 글을 칠 때 화면을 확대한다.

**카톡 스크린샷을 다시 잴 일이 있으면** 이미지 픽셀을 3으로 나누지 말 것 —
그 폰은 화면 확대가 켜져 있어 CSS 1px이 3.35 이미지 픽셀이다.
아는 요소(아바타 29px 같은 것)로 배율부터 구할 것.

### iOS 키보드 — 폰에서 재서 얻은 것들

이 화면에서 가장 오래 헤맨 자리다. **짐작으로 고치지 말고 재서 고칠 것.**
헤드리스에는 키보드가 없어 이 손짓을 흉내 낼 수 없으므로, 값이 필요하면
임시 진단 줄을 넣어 폰에서 재는 편이 결국 빠르다(그렇게 해서 아래 셋을
찾았고, 그전까지 짐작으로 넉 대를 헛짚었다).

**아이폰 홈 화면 앱에서는 `window.innerHeight`가 키보드와 함께 줄어든다**
(707 → 333으로 쟀다). 그래서 `innerHeight - vv.height`로 키보드를 알아채면
그 값이 **늘 0**이라 아무것도 안 걸린다. 키보드 높이는
**`documentElement.clientHeight`**(707, 키보드가 떠도 안 줄어든다)에서 빼고,
'키보드가 올라왔는가'는 **입력칸의 초점**으로 안다.

**자리를 잡으면 크기를 다시 재지 않는다(`locked`).** 끄는 동안 `vv.height`가
333~707을 오간다 — 그 값으로 화면을 계속 다시 재던 것이 위로 끌 때 떨리던
정체였다. 다 올라온 뒤 650ms에 한 번 정하고 붙박는다. 초점이 떠나는 순간
푸는데, 그래야 키보드가 미끄러져 내려가는 매 단계를 따라 화면이 늘어난다
(붙박은 채로 두면 끝에서 한 번에 툭 늘어나 끊겨 보인다).

**iOS가 미는 것을 상쇄하려 들지 말 것.** 키보드가 올라와 있으면 보이는
화면(333)이 문서가 놓인 자리(707)보다 작고, 그 374 차이만큼 iOS가 화면을
민다. **그 손짓은 우리에게 이벤트조차 오지 않는다**(폰에서 `이동 0`으로
확인). `touch-action: none`도, `overflow: hidden`도, `touchmove`를
`passive: false`로 삼키는 것도, `transform`으로 따라가는 것도 **넷 다
안 통했다** — 특히 따라가기는 오히려 두 번 움직이게 만들어 더 나빴다.
움직임이 멎고 120ms 뒤에 제자리로 돌려놓는 것까지가 웹앱이 할 수 있는 전부다.
**이건 Capacitor로 감싸면 없어지는 문제다** — 키보드 플러그인이 웹뷰 자체를
줄여 주면(`resize: native`) 밀 틈이 아예 사라진다. 웹으로 더 파지 말 것.
**사용자가 출시할 때 꼭 고쳐 달라고 짚어 둔 항목이다** — `docs/출시-전-할일.md` 2번.

**홈으로 나갔다 돌아오면 키보드 자리를 걷어내야 한다.** 키보드를 올려 둔
채 홈으로 나갔다 조금 있다 돌아오면, 키보드는 사라졌는데 **입력칸이 그
자리에 그대로 떠 있고 아래가 텅 빈 채로 굳었다**(사용자 제보 · 사진).
그 사실이 우리에게 안 오기 때문이다 — 초점은 입력칸에 그대로 남아
`blur`가 안 오므로 `typing`이 계속 참이고, 게다가 **붙박아 둔(`locked`)**
상태라 크기가 바뀌어도 다시 안 잰다. 그래서 `--kb`·`--vvh`가 옛 키보드
높이로 남는다.
`visibilitychange`(와 bfcache의 `pageshow`)에서 **붙박기를 먼저 풀고
실제 높이로 다시 판단한다.** 키보드가 정말 없으면 **초점도 함께 뗀다** —
안 떼면 `typing`이 계속 거짓말을 해서 다음에 또 같은 자리에 갇힌다.
**두 번 본다**: 돌아온 첫 프레임에는 iOS가 아직 크기를 되돌리는 중이라
0.25초 뒤에 한 번 더 봐야 제 높이가 나온다.
`.dev/behave.mjs`의 `홈에 갔다 돌아오기` 칸이 붙들어 둔다 — 헤드리스에
키보드는 없지만 **그 상태 자체는 만들 수 있다**(글칸을 눌러 붙박이게 한 뒤
`visibilitychange`를 던진다). 고치기 전 코드로 되돌리면 세 줄이 빨갛게 뜬다.

**글이 적으면 목록이 위가 아니라 아래에 붙는다**(`.chat-list > :first-child`에
`margin-top: auto`). 위의 것을 고치고 나니 키보드는 잘 내려가는데 **말풍선만
화면 위에 남고 그 아래가 텅 비어 보인다**는 제보가 이어졌다 — 키보드가
올라와 있을 때는 목록이 짧아 글이 꽉 차서 아래에 붙어 보이다가, 목록이
길어지면 같은 글이 위로 올라붙어 **글이 도로 올라간 것처럼** 보인 것이다.
대화는 아래에서 쌓이므로 입력칸 바로 위에 붙는 것이 맞는다.
**`justify-content: flex-end`로 하지 말 것** — 글이 넘칠 때 위쪽이 잘려
맨 위로 못 올라가는 브라우저가 있다. 첫 줄의 `margin-top: auto`는 남는
자리가 있을 때만 밀어 내리고 넘칠 때는 0이 되어 아무 일도 안 한다.
**이 한 줄은 `behave`에 못 넣었다** — 고정 자료의 대화가 길어 목록이 늘
넘치고, 짧은 방을 흉내 내려 조회를 가로채는 것도 잘 안 걸렸다. 대신 두 줄만
있는 방을 따로 띄워 재서 확인했다(**아래 빈틈 317px → 10px**, 그리고 넘칠
때 맨 위로 올라가는 것도 그대로). 다시 손볼 일이 생기면 같은 방식으로 잴 것.

**키보드가 내려가 목록이 커지면 굴러간 자리를 다시 앉힌다**(`settleList`).
위의 둘을 다 고쳐도 **말풍선이 화면 위에 붙고 그 아래가 텅 비어 보인다**는
제보가 남았고, 결정적인 단서는 사용자의 이 한 줄이었다 —
**"저 상태에서 채팅내용을 올리면 다시 제 자리로 돌아와."**
키보드가 올라와 있는 동안 목록은 짧고(464px) 맨 아래까지 굴러가 있다.
키보드가 내려가 목록이 682px로 커지면 그 `scrollTop`은 **더는 갈 수 없는
자리**가 되는데, **크로미움은 그때 알아서 끝으로 당겨 주고 iOS 사파리는
그냥 둔다.** 그러면 보이는 창이 글 아래로 넘어가 앉아 위에 붙은 것처럼
보이고, 사람이 한 번 굴리는 순간 브라우저가 그때 당겨 줘서 제자리로 돌아온다.
그래서 키보드가 내려가는 갈래(`applyKeyboard`의 `else`)에서
**맨 아래를 보고 있었으면 끝으로, 아니면 넘어간 만큼만** 되돌린다 —
옛 글을 읽으려고 올려 둔 자리를 키보드가 내려갔다고 빼앗으면 안 된다.

**한 번만 앉히면 안 된다 — 높이가 한 번에 정해지지 않는다.** 처음에는
'지금 + 다음 프레임' 두 번으로 두었는데, 이번에는 반대로 **아래가 잘려
새 글이 안 보인다**는 제보가 왔다(실기기 · 사진). 키보드가 내려가는 동안
목록 높이도 아래 여백도 여러 단계에 걸쳐 바뀌고, 그때마다 iOS가 굴린
자리를 제 나름대로 손보기 때문이다. 그래서 **이모티콘 서랍과 똑같이**
`ResizeObserver`로 목록이 다시 재어질 때마다 따라 내리고 0.6초 뒤에 손을
뗀다 — 그 뒤는 사람이 굴리는 것이라 건드리면 안 된다.
셋을 함께 지킬 것:
- **'맨 아래였는가'는 맨 처음에 한 번만 집는다**(`stick`). 목록이 줄었다
  늘 때마다 브라우저가 스크롤 이벤트를 던져 `onScroll`이 '맨 아래가 아니다'로
  내려 버리므로, 따라가는 도중에 다시 보면 늘 거짓이 된다(서랍에서 겪은 것과
  같은 자리다).
- **키보드가 방금 내려간 때만 부른다**(`kbRef.wasOpen`). 화면을 처음 열
  때도 이 갈래를 지나는데, 거기서 따라 내리면 **`여기까지 읽으셨습니다`
  줄로 옮겨 놓은 자리를 도로 맨 아래로 끌어내린다.**
- **`wasOpen`은 아래 갈래까지 갔을 때만 지운다.** 붙박여(`locked`) 일찍
  돌아서는 판에서 지워 버리면, 정작 크기를 다시 재는 판이 왔을 때 '방금
  내려갔다'를 놓친다.

**이 자리는 `behave`에 못 넣는다** — 크로미움이 알아서 끝으로 당겨 주므로
흉내로는 **고치기 전 코드도 초록으로 뜬다.** 초록을 위한 검사를 넣지 말 것
(깜빡임에서 얻은 교훈 1과 같은 자리다).

**임시 진단 줄(`kb-probe`)은 걷어냈다** — 화면 맨 위에
`판 · 문서 · 보임 · 창 · kb · vvh · 열림 · 목록 · 자리`를 적어 두던 것이다.
이 자리를 다시 팔 일이 생기면 같은 방식으로 넣으면 된다 — 헤드리스로 안 잡히는 자리는 값을
화면에 적어 두고 사람이 읽어 주는 편이 결국 빠르다(깜빡임을 가릴 때
쓴 방식과 같다).

키보드가 올라와 있는 동안의 화면 높이는 `100dvh - --kb`가 아니라
**`--vvh`(실제로 보이는 높이)** 를 쓴다. 사파리 탭에서는 끄는 동안 주소 막대가
접히며 `dvh`까지 함께 움직인다. `--vvh`는 키보드가 없을 때 지운다 — 남겨 두면
옛 높이가 굳는다. **`.dev/kb-check.mjs`도 `--kb`와 `--vvh`를 함께 넣어야
실제와 같은 길을 탄다.**

**입력칸은 키보드가 올라오면 흐름에서 빠져 화면에 직접 붙는다**
(`position: fixed; bottom: var(--kb)`). 목록이 그 아래로 숨지 않게
`--composer`로 자리를 비우는데, 그 높이를 재는 `ResizeObserver`는
**`border-box`로 봐야 한다** — 키보드가 올라오면 아래 여백만 68px에서
10px로 줄어들어, 기본값(`content-box`)으로는 안쪽 글자 칸이 그대로라
관찰자가 깨어나지 않는다.

**화면이 뜨기 전에 손짓 듣기를 붙이지 말 것.** 대화를 불러오는 동안에는
스피너만 있어 `.chat-list`가 없다. `useEffect`를 빈 배열로 두면 그때 한 번
돌고 마는데 ref가 비어 있어 **영영 안 붙는다** — 아래로 끌어 키보드를
내리는 기능이 이래서 통째로 죽어 있었다. `roomId`를 의존성에 넣을 것.

**입력칸은 적은 만큼 늘어난다(`growDraft`).** `textarea`는 스스로 안 늘어나서,
놔두면 한 줄 높이에 갇혀 안에서 스크롤된다 — 여러 줄을 적으면 **앞줄이 위로
잘려 안 보였다.**

**재는 일을 다음 프레임으로 미룬다. 이게 규칙의 핵심이다.**
`growDraft()`는 예약만 하고(`requestAnimationFrame`, 여러 번 불려도 한 번),
실제로 재고 고치는 것은 `measureDraft()`가 그 다음 프레임에 한다.
**글자를 치는 그 순간에는 칸을 재지도 고치지도 않는다.** 잰 뒤에는 이렇게
갈린다:
- **글자가 늘 때는** 넘칠 때(`scrollHeight`가 지금 높이보다 클 때)만 한 번
  높이를 준다.
- **지웠을 때만** 높이를 `auto`로 되돌렸다 다시 잰다(안 되돌리면 한 번
  커진 뒤 안 줄어든다). 글자 수가 줄었는지로 가른다(`lastLen`).
- 보내고 나면 `clearDraft`가 **예약을 취소한다** — 안 그러면 한 줄로
  되돌려 놓은 칸을 한 프레임 뒤에 옛 높이가 덮어쓴다.

**이 규칙은 `광`을 칠 때 글씨가 깜빡인다는 제보를 쫓다 얻은 것이다.
그런데 깜빡임의 원인은 결국 우리 코드가 아니었다** — 아래를 꼭 읽을 것.
(그래도 이 규칙은 그대로 둔다: 글자마다 배치를 다시 잡게 하던 것을 걷어낸
것은 그것대로 옳고, 글칸 속도가 그만큼 좋아졌다.)

#### 깜빡임의 진짜 원인 — **웹에서는 못 고친다. 다시 파지 말 것.**

**아이폰 천지인 자판은 웹 글칸에서 조합(marked text)을 아예 안 쓴다.**
`ㄱ·`을 `고`로 바꿀 때 **칸을 진짜 비웠다가 다시 채운다:**

```
input "ㄱ·"  →  input "ㄱ"  →  input ""  →  (7~13ms 뒤) input "고"
                                  ↑ 여기서 칸이 완전히 빈다
```

그 빈 상태가 **7~13ms** 이어지고, 한 프레임(16.7ms)마다 그려지는 화면이
그 사이에 한 번 걸리면 글씨가 사라졌다 나온다. **조합 이벤트는 한 줄도
안 온다.** `자음은 괜찮은데 모음만`인 것도 이 때문이다 — 모음을 칠 때만
앞 글자를 지웠다 다시 쓴다.

**폰에 임시 시험 화면을 넣어 여섯 가지로 갈랐고 전부 똑같이 깜빡였다:**
맨 칸(자바스크립트 한 줄 없음·폰 글꼴) · 앱 글꼴 칸 · 대화창과 같은 칸 ·
**자동수정을 끈 칸** · **`<input>` 한 줄 칸** · **`contenteditable` 글 상자**.
맨 칸이 깜빡이는 이상 **우리 코드도 글꼴도 원인이 아니고**, 나머지 셋이
다 같은 이상 **웹에서 바꿀 수 있는 것이 없다.**

**카톡이 멀쩡한 이유가 여기 있다** — 네이티브 칸(UITextView)은 조합을
쓰므로 칸이 빌 일 자체가 없다.

**Capacitor로 감싸도 그대로다** — 그래도 WKWebView 안의 HTML 글칸이다.
이 자리는 iOS 키보드 꼭지의 다른 것들(화면 밀림·알림음)과 달리 **감싸는
것만으로는 안 풀린다.**

**그래서 입력칸을 네이티브로 바꿨다** — 아래 `글칸은 앱에서 네이티브다`
꼭지가 그것이다. **앱에서는 이 깜빡임이 여기서 끝난다**(`UITextView`는
조합을 쓰므로 칸이 빌 일 자체가 없다). 웹으로 열었을 때와 플러그인이
없는 옛 앱에서는 아래 규칙이 그대로 남으므로 **이 꼭지를 지우지 말 것.**
그때의 안내는 여전히 **쿼티 자판으로 치면 안 그렇다**이다 — 쿼티는
한 글자가 내내 한 글자라 지웠다 쓰는 길로 안 간다.

**세 번 헛짚은 것에서 남길 교훈 둘.**
1. **헤드리스 흉내가 실기기와 다를 수 있다.** CDP의 조합 API는 조합
   이벤트를 주는데 실제 천지인은 안 준다 — 그래서 검사는 초록인데 폰은
   그대로였다. 흉내로 초록이 떴다고 고쳐진 것이 아니다.
2. **`맨 칸도 그런가`를 제일 먼저 물었어야 했다.** 자바스크립트가 없는
   칸으로 한 번만 견줬으면 첫 판에 갈렸을 것을, 코드 안에서만 원인을
   찾느라 세 번을 돌았다. **밖에서 안 그러는지부터 볼 것.**

**다시 파야 할 일이 생기면** 시험 화면을 다시 만들면 된다 — 칸을 여럿
나란히 놓고 `beforeinput`·`input`·조합 이벤트를 **ref에만 쌓아**(화면을
다시 그리면 재려는 것을 흔든다) 값과 함께 찍어 보는 것이 전부다.
`Me.tsx` 맨 아래에 들어가는 줄을 하나 두고, 끝나면 함께 지운다.

**댓글 칸(`components/Comments.tsx`)도 같은 코드를 따로 들고 있다. 한쪽만
고치지 말 것.** 거기도 `grow`가 예약만 하고 `measure`가 다음 프레임에 잰다.
친 글을 state에 넣고 있던 것도 함께 걷어냈다 — 글자마다 댓글 목록이 통째로
다시 그려졌다(`hasText` ref로 바꿨다, 대화창과 같다).

`box-sizing: border-box`라 **테두리 두께를
더해야** 잔스크롤이 안 남는다. 위 한도(120px)는 CSS가 잡고, 거기 닿으면 그때부터
안에서 스크롤된다. 보내고 나면 `clearDraft`가 높이도 함께 되돌린다.
모서리를 `--r-pill`이 아니라 **19px**(한 줄 높이의 절반)로 못박은 것도 이 때문이다 —
한 줄일 때는 알약과 똑같고, 늘어나면 저절로 둥근 네모가 된다.
**댓글 칸(`components/Comments.tsx`)도 같은 방식이다.**

**대화 입력칸은 `value`로 묶지 않는다(uncontrolled).** 값은 칸이 들고 있고,
**보낼 때는 `currentDraft()`로 칸에서 직접 읽는다**(조합 중인 마지막 글자가
아직 안 와 있을 수 있다). 비울 때는 `clearDraft()`로 칸까지 지운다.

**글자를 칠 때 리액트를 아예 안 거친다.** '치면 카톡과 달리 좀 끊긴다'는
제보를 `node .dev/type-bench.mjs`로 재 보니 말풍선 쉰 개짜리 방에서
**19글자 중 9글자가 한 프레임(16ms)을 넘겼다**(가운데값 15.9ms · 최대 67.7ms).
원인이 셋이었고 셋 다 걷어냈다 — 지금은 가운데값 1.2~1.8ms, 넘기는 글자 0~1개다.

1. **친 글을 state에 복사하고 있었다.** 한 글자마다 화면 전체가 다시
   그려져 말풍선 쉰 개가 매번 다시 그려졌다. 참/거짓으로 줄여도 **첫
   글자에서 한 번은 다시 그려져** 거기서만 56ms가 났다(글을 치기 시작하는
   바로 그 순간이라 제일 눈에 띈다). 지금 `hasText`는 **`ref`**이고,
   그 값을 읽는 곳은 둘 다 **다시 그려지는 순간에만** 본다 — 초점이 떠날
   때의 안내 글씨와, 초점이 오갈 때의 보내기 단추다. 그래서 글자를 치는
   동안에는 화면이 아예 다시 안 그려진다. **state로 되돌리지 말 것.**
2. **말풍선을 `memo`로 감쌌다.** 초점이 오가거나 이모티콘을 고를 때처럼
   정말로 다시 그려야 할 때도 쉰 개가 통째로 다시 그려질 이유는 없다.
   **그래서 넘기는 값이 매번 같아야 한다** — `jumpTo`·`startReply`·
   `pinBottom`은 `useCallback`, `mentionNames`는 `useMemo`로 붙박아 두었다.
   새 값을 넘길 일이 생기면 **그것도 함께 붙박을 것**(안 그러면 `memo`가
   있으나 마나가 된다).
3. **`growDraft()`가 매번 목록까지 다시 재게 했다.** 칸 높이가 그대로면
   `pinBottom()`을 안 부른다 — 글자를 칠 때 줄 수가 바뀌는 일은 스무 글자에
   한 번쯤이다. (그 뒤 **칸 자체를 건드리는 것도 걷어냈다** — 위 `growDraft`
   꼭지 참고. 여기까지 하고도 조합 중인 글씨가 깜빡인다는 제보가 남아 있었다.)

**나머지 일곱 칸도 함께 쟀다**(사용자 요청). 대화만큼 나쁜 곳은 없었고,
목록을 낀 검색칸 둘만 손봤다 — **거르는 값에 `useDeferredValue`를 걸어**
글자는 즉시 찍히고 목록만 다음 프레임에 따라오게 했다:
`모집 열기 — 골프장`(574곳, 9.8 → 6.7ms)과 `정산 — 사람 찾기`(100명, 9.4ms).

**회원 명단의 찾기 칸에는 같은 것을 걸었다가 되돌렸다.** 거기서는
**첫 글자가 8ms에서 44ms로 나빠졌다** — 빈 검색어에서 첫 글자로 넘어가는
순간 백 줄이 통째로 갈리는데, 늦춰 두면 리액트가 그 큰 그림을 **두 번**
그린다(옛 값으로 한 번, 늦춘 값으로 한 번). 글자마다 조금씩 무거운 자리와
**한 번에 크게 갈리는 자리**는 처방이 다르다 — 되살리지 말 것.

**안내 글씨도 브라우저의 `placeholder`를 안 쓴다.** 한글은 한 글자가 여러
번에 걸쳐 조합되는데 iOS는 그 조합 중인 글자를 '내용 없음'으로 봐서, 첫
글자를 칠 때 `메시지`가 한 번 번쩍였다. uncontrolled로 바꾼 뒤에도 그대로
났다 — React가 아니라 WebKit 쪽이다. `.chat-hint`로 직접 그리고 **초점이
가는 순간 치운다**(글자가 들어왔는지가 아니라 **초점**이 기준이다 — 그래야
번쩍일 틈 자체가 없다).

**보내기 버튼은 `onMouseDown`을 막는다.** 안 막으면 버튼이 초점을 가져가
입력칸이 풀리고, 그러면 **키보드가 함께 내려가** 한 마디마다 다시 눌러야 한다.
`send()`가 `await` 앞에서 `focus()`를 한 번 더 부르는 것은 그래도 초점이
넘어가는 기기 때문이다 — 응답을 기다린 뒤에 부르면 iOS가 사용자 손짓으로
안 쳐서 키보드가 안 올라온다. `onComposerBlur`도 150ms 기다렸다 접는다
(잠깐 떴다 돌아올 때 화면이 껑충 뛰지 않게).

**채팅만 예외다.** 지난 대화는 바뀌지 않으므로 들어온 행을 그냥 덧붙인다.
내가 보낸 글은 **누르는 그 자리에서** 먼저 그리고, 실시간 이벤트가
뒤따라오면 `id`로 걸러 두 번 그리지 않는다.

**서버 답을 기다리지 않는다 — 누르면 그 자리에서 올라간다**(사용자 제보 —
`전송 버튼을 누르면 채팅이 올라가는데 딜레이가 생겨. 그래서 혹시 멈췄나
싶어서 다시 누르면 두 번이 올라갈 때가 있어`). 예전에는 서버에 넣고 **답이
온 뒤에야** 말풍선을 그리고 글칸을 비웠는데, 그 왕복이 곧 인터넷 한
바퀴(0.3~1초)라 그동안 **화면에 아무 일도 안 일어나고 친 글도 칸에 그대로
남아 있었다.** 자리는 `send()`(Chat.tsx)다.

- **먼저 그리는 줄은 `TEMP_ID`(`tmp:`)를 단다** — 화면에만 있는 줄이라
  **서버에 실어 보내면 안 된다.** 진짜 id는 uuid여서 한 줄만 섞여도 그
  조회가 400으로 막히는데, 반응 받아 오는 자리는 오류가 나면 **남은 묶음까지
  통째로 건너뛴다**(`if (err) return`). 그래서 거기서 걸러 낸다.
  **새로 글 id를 서버에 보내는 곳을 만들면 여기도 함께 볼 것.**
- **답이 오면 갈아 끼운다**(`swapRow`). **실시간 이벤트가 서버 답보다 먼저
  닿는 판이 있어**, 진짜 줄이 이미 목록에 있으면 먼저 그린 것을 빼기만
  한다 — 안 그러면 같은 말이 두 줄로 남는다.
- **실패하면 적어 둔 것을 그대로 되돌려 놓는다** — 글·고른 이모티콘·답장
  셋 다. 다시 누르면 되게 해야지 친 글이 사라지면 안 된다.
- **사진은 먼저 안 그린다.** 올리는 데 몇 초가 걸려 그동안 보여 줄 그림이
  없고, 사진을 고른 것 자체가 이미 '하는 중'으로 읽힌다(`사진 올리는 중…`).
- **두 번 눌러도 한 번만 나가는 것은 `sendBusy`(ref)가 막는다.**
  단추를 흐리게 하는 것(`disabled={sending}`)으로는 **못 막는다** — 앱에서
  보내기는 **네이티브 바의 단추**라(`ComposerBar.swift`) 그 막이 아예 안
  걸리고 `send` 신호를 그대로 다시 보낸다. state가 아니라 ref인 것도
  그래서다: state는 다시 그려진 뒤에야 바뀌어 연달아 누르는 그 순간을 못 잡는다.
- `.dev/behave.mjs`의 `보내면 바로 올라간다` 칸이 붙들어 둔다 — **왕복을
  일부러 900ms로 늦춰** 답이 오기 전의 화면을 본다(빠른 서버에서는 고치기
  전후가 구별이 안 된다). **단추가 아니라 Enter로 보낸다**(그래서 창이
  700px이다) — 단추로 누르면 `disabled`에 막혀 **웹에서는 두 번째가 애초에
  안 눌려** 고치기 전 코드도 초록으로 뜬다. 되돌리면 세 줄이 빨갛게 뜬다.

**그래서 대화도 `useRefreshOnShow`가 한 벌로 붙는다.** 실시간은 **보고 있는
동안**만 맡는데, **대화 알림을 눌러 들어오는 길이 대개 접어 둔 것을 펴는
것**이라 그동안 끊겼던 연결이 다시 이어져도 **그 사이 온 글은 안 받아
온다** — 게다가 화면이 새로 안 만들어져 첫 묶음 조회(`[roomId]`)도 다시
안 돈다. **알림을 눌러 들어갔는데 정작 그 글이 없었다**(사용자 제보 —
`채팅배너알림이 와서 그걸 눌러서 들어가면 채팅으로 들어는가지는데 새로운
내용이 안나와. 그래서 투표눌렀다 다시 대화들어가면 그제서야 나와`).
탭을 옮겼다 오면 나오던 것은 **그때 화면이 새로 만들어져서**다 —
고쳐진 것이 아니라 딴 길로 돈 것이다.
- **통째로 다시 받지 말 것 — 마지막 글 뒤엣것만 덧붙인다.** 다시 받으면
  굴려 둔 자리도 `여기까지 읽으셨습니다` 줄도 잃는다. 겹치는 것은 `id`로
  거른다(실시간이 먼저 붙여 둔 것이 있을 수 있다).
- **검색으로 옛 글에 가 있으면 건너뛴다**(`windowedRef`) — 실시간 덧붙이기와
  같은 잣대다. 한참 지난 글 뒤에 오늘 글이 앉으면 안 된다.
- `.dev/behave.mjs`의 `접었다 펴면 그 사이 온 글` 칸이 붙들어 둔다 —
  거기서는 실시간이 아예 막혀 있어(`stubOutside`) 접어 둔 폰과 같은 상태가
  된다. 그 줄을 빼면 실제로 셋이 빨갛게 뜬다.

**답장은 왼쪽으로 밀어서 건다**(`messages.reply_to`). 카톡과 같은 손짓이다.

- 손짓은 `.chat-row`의 touch 이벤트로 집는다. **`preventDefault`를 부르지
  않는다** — React의 touchmove는 passive라 어차피 안 먹는다. 대신
  `touch-action: pan-y`로 세로 스크롤만 브라우저에 넘긴다.
- 미는 동안의 위치는 **state가 아니라 요소를 직접** 움직인다
  (`el.style.transform`). 손가락을 따라 매 프레임 다시 그리면 긴 대화에서
  눈에 띄게 끊긴다. 화살표가 짙어지는 것도 같은 이유로 CSS 변수(`--swipe`)다.
- 목록의 '아래로 끌어 키보드 내리기'와 부딪히지 않는다 — 그쪽은
  `dy > 40 && dy > dx`라 가로로 그은 것은 안 걸린다.
- **손가락이 없는 기기에서는 말풍선에 손을 얹으면 화살표가 나온다**
  (`@media (hover: hover)`). PC로도 답장할 수 있어야 한다.
- 원본이 지워져도 답장은 남는다(`on delete set null`). 지난 묶음에 있어
  아직 안 불러온 원본은 인용이 `지난 대화`가 되고 눌러도 안 움직인다.
- 인용을 누르면 원본으로 가고 1.3초 깜빡인다(`data-mid`로 찾는다).

**`@언급`은 따로 저장하지 않는다.** 글 안에 `@이름`으로 적히고, 그릴 때
회원 명단과 맞춰 본다(`lib/mention.ts`) — 칸을 두면 이름을 바꿨을 때 둘이
어긋나고, 백 명이라도 맞춰 보는 값이 싸다(이름 목록 한 번 훑는 것뿐이다).

- `@`는 **줄 처음이나 공백 뒤**에서만 목록을 띄운다. 안 그러면 메일 주소를
  칠 때마다 뜬다. 이름에 공백이 없으므로 뒤에 공백이 오면 접는다.
- **긴 이름을 먼저 맞춘다** — `김지`와 `김지명`이 함께 있으면 뒤엣것이다.
- 목록 항목도 `onMouseDown`을 막는다. 안 막으면 입력칸이 초점을 잃어
  **키보드가 내려간다** — 보내기 단추와 같은 이유다.
- **대화 알림을 꺼 둔 기기에도 언급은 간다.** 부르는 것은 '알아 두라'가
  아니라 '지금 봐 달라'라서, 그것까지 막히면 부를 이유가 없어진다.
  **내 글에 달린 답장도 같이 뚫는다** — 둘 다 한 사람을 콕 집은 글이라
  같은 규칙으로 다룬다(`alwaysFor()`). 발송기의 `Note.always`가 그
  사람들이고, 구독을 고를 때 `chat.eq.true` **또는**
  `user_id.in.(...)`로 푼다.
- **`@전체`는 운영진(운영자·부운영자)만 쓴다**(`ALL_MENTION` in lib/mention.ts).
  모집 마감이나 집합 시각이 바뀐 것을 서른 명에게 한 번에 알리는 자리다.
  이걸로 부르면 **대화 알림을 꺼 둔 기기까지 다 울리므로**, 아무나 쓰면
  그 스위치가 있으나 마나가 된다. 세 곳이 함께 걸린다:
  - 언급 목록에 `📢 전체`를 얹는 것은 `isAdmin`일 때만.
  - 말풍선에서 도드라지는 것은 **쓴 사람이 운영진일 때만**(`allowAll`).
    내가 운영진이라고 남이 친 `@전체`까지 도드라져서는 안 되고, 회원이
    손으로 쳐 넣은 것이 도드라지면 불렀다고 여기게 된다.
  - **발송기가 다시 확인한다** — 화면에서 목록을 감추는 것은 손으로 쳐
    넣는 것을 못 막는다. `mentionedIds()`가 쓴 사람의 등급을 보고,
    운영진이면 `@전체` 한 번에 회원 전부를 `always`로 돌려준다.
- **맞추는 규칙이 두 군데에 있다** — 화면의 `splitMentions`(lib/mention.ts)와
  발송기의 `mentionedIds`(functions/notify). 둘 다 긴 이름을 먼저 본다.
  `ALL_MENTION`(`전체`)도 양쪽에 같은 글자로 적혀 있다.
  **한쪽만 고치지 말 것** — 화면에는 도드라지는데 알림은 안 오게 된다.
- 발송기는 이름을 **글에서 찾는다**(칸에 담아 보내지 않는다). 그래서
  앱을 안 고쳐도 발송기만 고치면 규칙이 바뀐다.
- `내 정보`의 대화 스위치를 끄면 **`나를 @언급한 글은 그래도 옵니다`**라고
  적힌다. 안 적으면 껐는데 왜 오냐가 되고, 부르는 쪽은 왜 안 보냐가 된다.

#### 머리말은 카톡 오픈톡과 같은 배치다

**왼쪽에 제목과 사람 수, 오른쪽에 🔍와 ☰**(사용자 요청). 한동안 제목을
가운데 세우고 단추 둘을 양끝에 얹었는데(그래서 `position: absolute`였다),
카톡은 제목이 왼쪽에 붙고 누르는 것이 오른쪽에 모여 있다.

- **`←`(뒤로)는 안 둔다.** 카톡에서는 방을 나가는 자리지만 **이 앱에서
  대화는 탭**이라 뒤로 갈 데가 없다 — 눌러 봐야 앱이 통째로 닫힌다.
  방을 옮기는 일은 탭바가 맡는다. 되살리지 말 것.
- **사람 수는 제목 옆에 흐리게 붙인다**(`.chat-title-n`). ☰ 위에 얹던 것을
  옮긴 것이라 **두 군데에 적지 않는다.**
- 제목은 `flex: 1` + `min-width: 0`이 **한 쌍이다** — 없으면 방 이름이 길
  때 말줄임이 안 걸리고 단추를 밀어낸다.
- 단추 둘 다 흐름 안에 있다. 제목이 왼쪽이라 자리를 뺏길 일이 없어졌으니
  `position: absolute`로 되돌리지 말 것.

#### 말풍선 반응 — 카톡의 `😄 2`

한마디 한마디에 `네` `ㅋㅋ`로 답하면 하루 백 마디가 이백 마디가 된다.
카톡이 그래서 둔 자리이고, 여기서도 같은 몫이다(사용자 요청).
**다는 곳은 길게 누르는 창**이고, 붙는 곳은 말풍선 아래 칩 줄이다.

- **표를 새로 만들었다**(`message_reactions`). 글에 개수만 적어 두면 '내가
  눌렀나'를 알 수 없고 동시에 누를 때 셈이 어긋난다 — `settlement_shares`를
  JSON으로 안 담은 것과 같은 이유다. 기본키가 `(글, 사람, 그림글자)`라
  **한 사람이 한 글에 같은 것을 두 번 못 단다**(다른 것은 여럿 달 수 있다).
- **읽기는 회원 전부, 쓰고 지우는 것은 제 것만.** 남의 반응을 떼어 낼 수
  있으면 셈이 거짓말이 된다 — **운영진에게도 안 열었다**(가릴 글이 있으면
  글을 가리는 것이 맞다).
- **그림글자는 다섯이다**(`REACTIONS` in types.ts — 카톡과 같다).
  늘리면 창의 줄이 두 줄로 접혀 무엇을 누르는 중인지가 흐려진다.
- **알림은 안 간다.** 댓글과 같은 결이다 — 반응까지 폰을 울리면 대화
  알림과 다를 바가 없어진다. 웹훅을 걸지 말 것.
- **하나도 없으면 줄 자체를 안 그린다.** 빈 자리를 늘 비워 두면 말풍선
  사이가 성겨진다.
- **차례는 먼저 달린 순서다**(`countReacts`). 개수순으로 정렬하면 새 반응이
  들어올 때마다 칩이 자리를 바꿔 누르려던 것을 잘못 누른다.
- **뜻이 있는 색을 안 쓴다** — 흰 알약이고, 내가 누른 것만 분홍 테두리로
  갈라 둔다. 이 화면에서 '지금 눌러야 할 것'은 보내기 단추 하나다.
- **칩은 30px 아래로 내리지 말 것** — `node .dev/audit.mjs`가 그보다 작은
  누름 자리를 잡아낸다(창의 그림글자는 44px이다).
- **가린 글에는 안 붙인다** — 덮어 둔 글에 좋다고 누를 일이 없다
  (`복사`·`답장`을 안 붙이는 것과 같은 잣대다).
- **먼저 화면에 반영하고 나중에 보낸다.** 누르자마자 안 켜지면 안 눌린 줄
  알고 또 누른다. 실패하면 되돌리고 까닭을 알린다.
- **글 id를 나눠서 묻는다**(`REACT_CHUNK` 60). 밀린 사람은 한 번에 300개까지
  받는데 그 id를 한 줄에 다 실으면 주소가 11KB가 되어 중간에서 잘린다.
  보통은 50개라 한 번으로 끝난다.
- **실시간은 방을 안 가리고 받는다.** 거르개는 칸 하나만 볼 수 있는데 이
  표에는 방 번호가 없다 — 대신 **내가 든 글의 것만** 반영한다(`reactSeen`).
  **다시 불러오지 않는다**: 반응은 줄끼리 안 얽히므로 들어온 줄만 갈아
  끼우면 된다(읽음 표시와 같은 이유다).
- **`Bubble`이 `memo`라 넘기는 값이 매번 같아야 한다.** 반응은 `글 id →
  배열`로 담고 **바뀐 글의 몫만** 갈아 끼운다 — 통째로 새 객체를 만들면
  쉰 개가 다시 그려진다(`node .dev/type-bench.mjs`로 확인할 것. 지금 대화
  입력칸은 가운데값 0.8ms다).
- **오류를 그냥 삼킨다.** 표가 아직 없는 저장소에서 400이 나는데 그걸
  던지면 대화가 통째로 안 열린다. 못 받으면 반응만 안 뜬다.

#### 참여자 목록과 프로필 — 오픈톡의 ☰

방에 누가 있는지 볼 길이 없었고, **말풍선 옆 얼굴을 눌러도 아무 일이
없었다** — 100명 방에서 `83/신성호/광산구`만 보고는 누군지 떠올리기 어렵다.
머리말 오른쪽 ☰이 목록이고, 얼굴이나 그 줄을 누르면 프로필 카드가 뜬다.

- **명단을 새로 안 받아 온다.** 이 화면이 이미 `fetchPeople()`로 회원 전부를
  들고 있고(이름표와 얼굴 테두리에 쓴다) 방에 있는 사람이 곧 회원이라,
  여기서 거르는 값이 조회 한 번보다 훨씬 싸다. **거르는 잣대는 `mentionable`
  그대로다** — 부르는 목록과 있는 목록이 갈리면 안 된다(대기·추방은 대화를
  아예 못 보므로 둘 다에서 빠진다).
- **찾는 글자는 `PeopleList` 안에 있다. 위로 올리지 말 것.**
  대화 화면에 두었더니 한 글자마다 화면 전체가 다시 그려져 **글자 하나에
  64ms**가 걸렸다(`node .dev/type-bench.mjs`, 말풍선 120개). 조각을 갈라
  놓으니 2.3ms다. `useDeferredValue`는 **여기서도 안 쓴다** — 회원 명단에서
  첫 글자가 8 → 44ms로 나빠진 그 자리와 같다.
- **열둘을 넘으면 찾게 한다**(`FIND_AT` — 정산·회원 명단과 같은 잣대).
- **참석 횟수는 목록이나 카드를 처음 열 때 한 번만 받는다**(`attendTried`).
  대화를 보기만 하는 사람에게는 필요 없는 조회다. **함수가 없는 저장소에서는
  `null`로 두어 그 줄을 아예 안 적는다** — 0으로 넘기면 모두가 `올해 0회`가
  되어 거짓말이 된다(회원 명단과 같은 규칙).
- **카드에 전화번호·차량번호를 적지 말 것.** 그건 **회원 명단 하나에서
  운영진에게만** 보이기로 정해 둔 값이다(사용자 요청). `.dev/behave.mjs`가
  그 줄을 지킨다.
- **얼굴은 그림 29px 그대로 두고 누르는 자리만 넓힌다**(`.chat-face`에
  `padding: 4px; margin: -4px`). 29px은 카톡 스크린샷을 재서 얻은 값이라
  손대면 짜임이 어긋나는데, 그대로 두면 `node .dev/audit.mjs`가 '누름 자리
  작음'으로 잡는다(30px 아래). 음수 여백이라 배치는 한 픽셀도 안 밀린다.
- **`onFace`는 `useCallback`이다** — 말풍선이 `memo`라, 매번 새 함수를
  넘기면 쉰 개가 통째로 다시 그려진다(`openMenu`·`jumpTo`와 같은 자리다).
- **카드 생김새는 `.chat-menu`와 같은 값이다** — 아래에서 올라오는 창이
  둘인데 다르게 생기면 같은 손짓으로 안 읽힌다.
- **내 얼굴에는 `@언급하기`를 안 붙인다** — 나를 부를 일이 없다.
- **붙여넣을 SQL이 없다** — 표도 칸도 안 늘었다.

#### 방 공지 — 말풍선을 맨 위에 붙박는다

카톡 오픈톡에서 길게 눌러 맨 위에 붙박는 그것이다. 모임 규칙·계좌·집합
장소처럼 **늘 보여야 하는 한 줄**이 하루 백 마디에 밀려 사라지지 않게 한다.
**공지 탭(`posts`)과는 다르다** — 그쪽은 읽고 지나가는 글이고, 이건 대화
위에 붙박여 있는 쪽지다.

- **표를 새로 안 만들었다** — `messages.pinned_at`·`pinned_by` 칸 둘이다.
  공지가 되는 것이 이미 있는 말풍선이라, 표를 두면 글이 지워졌을 때 짝이
  어긋나는 자리만 새로 생긴다.
- **한 방에 하나다.** 화면이 `pinned_at`이 **가장 늦은 줄 하나**만 읽으므로
  새로 올리면 앞엣것이 저절로 물러난다 — **앞엣것을 손으로 내리지 말 것**:
  쓰기가 둘이 되면 그 사이에 공지가 없는 순간이 생기고, 실패했을 때
  되돌릴 것도 둘이 된다.
- **정책을 새로 안 만들었다 — 붙여넣을 SQL은 칸 둘뿐이다.**
  `messages_admin`이 이미 `for all`이고 회원에게는 update 정책이 아예 없다.
  화면이 감추는 것과 DB가 막는 것이 같은 잣대다.
- **가린 글에는 `공지로 올리기`를 안 붙인다** — 덮어 둔 내용이 맨 위로 샌다.
- **공지 줄은 대화 목록 밖에 있다**(머리말 바로 아래). 목록 안에 넣으면
  굴릴 때 함께 올라가 사라져서, 늘 보이라고 붙박은 뜻이 없어진다.
- **기본은 접힌 한 줄이다.** 긴 공지를 펴 놓고 시작하면 대화가 그만큼
  가려진다. 펴면 전문과 함께 `누가 올림`·`대화에서 보기`(·운영진에게는
  `공지 내리기`)가 나온다 — 앞뒤 이야기가 곧 공지의 뜻인 때가 많다.
- **카톡처럼 흰 쪽지다**(사용자 요청). 한동안 노란 띠가 화면 폭을
  가로질렀는데, 카톡의 그것은 대화 바탕 위에 얹힌 **모서리 둥근 흰 카드**다.
  바깥(`.chat-pin-wrap`)에 `--chat-bg`를 깔아 목록과 이어 보이게 하고 카드만
  흰색으로 띄운다. 무슨 줄인지는 📢가 말해 준다.
  **분홍을 안 쓰는 것은 그대로다** — 이 화면에서 '지금 눌러야 할 것'은
  보내기 단추 하나다.
- **✕는 내 화면에서만 치운다**(카톡과 같다. 사용자 요청). 운영진의
  `공지 내리기`와 **하는 일이 다르다** — 그건 DB를 고쳐 모두에게서
  사라지고, 이건 그 기기의 `localStorage`(`teetime:pin-x`)에만 남는다.
  **열쇠에 `pinned_at`을 함께 넣는다**(`m4@2026-…`) — 같은 글을 내렸다 다시
  올리면 새 공지로 보고 다시 떠야 한다(id만 보면 영영 숨는다).
  **`.dev/behave.mjs`에서 잴 때는 `page.reload()`를 쓸 것** — `#`만 바뀌는
  이동은 화면을 새로 안 만들어서 state가 그대로 살아 있고, 저장을 안 했어도
  통과해 버린다(`countPosts`와 같은 자리다).
- **실시간은 들어온 줄만 보고 판단한다 — 다시 물어보지 않는다.** 붙박는
  것이 곧 가장 늦게 붙박은 줄이 되는 일이라 그 줄만으로 답이 나온다.
  내린 줄은 **그것이 지금 공지일 때만** 치운다(남의 옛 글이 내려간 것일 수
  있다). 쓴 사람이 지우면(DELETE) 공지 줄도 함께 걷는다.
- **폰 알림은 안 간다.** 댓글과 같은 결이다 — 공지는 대화방을 열면 맨 위에
  늘 있으므로 밀어 줄 것이 아니다.
- **`.dev/rest.mjs`에 `not.`을 풀어 넣었다.** 조회가 `.not('pinned_at','is',
  null)`인데 안 풀면 아무 조건도 안 걸려 **아무 글이나 공지로 뜬다.**
  같은 자리에서 **`is null`이 칸이 없는 행도 맞게** 했다 — 고정 자료에는
  키 자체가 없어서, 안 맞추면 `not.is.null`이 모든 글을 공지로 만든다.
- **`.dev/behave.mjs`의 옛 스키마 칸은 이제 거르는 칸도 400을 준다.**
  공지는 `select('*')`로 받으면서 `pinned_at`으로 거르므로, 고르는 칸만
  보면 그 길을 아예 안 타서 **시험이 통과해도 대화 화면이 통째로 안 열린다.**
- **고정 자료의 공지는 `m4`다. `m3`에 두지 말 것** — 그 줄은 `지우기` 칸이
  지워 버려, 뒤에 오는 공지 칸이 찾을 때는 화면에 없다(실제로 걸렸다).

#### 대화 검색 — 오픈톡의 🔍

100명이 하루 100마디를 쌓으면 `무등산 몇 시라고 했지`를 되짚을 길이
**위로 계속 올리는 것 말고는 없었다.** 머리말 오른쪽 🔍이 그 자리다.

- **찾는 일은 서버가 한다**(`ilike`). 받아 둔 것만 뒤지면 `지난 대화 더
  보기`를 누른 만큼만 찾아져서, 정작 오래된 것을 못 찾는다.
- **가린 글은 화면에서 거른다.** 조회에 `hidden_at` 조건을 걸지 **않는
  것은 일부러다** — 그 칸이 아직 없는 저장소에서 400이 나면 검색이 통째로
  죽는다(스키마를 손으로 붙여넣는 사이가 있다). 40건만 받아 오므로
  화면에서 거르는 값이 싸다.
- **`%`·`_`를 막아 둔다.** 그냥 넘기면 찾기의 특수문자라 `100%`가 아무거나
  맞는다. `.dev/rest.mjs`의 흉내도 **같게 푼다** — 다르게 풀면 시험이
  통과해도 소용없다.
- **두 글자부터 찾는다.** 한 글자는 백 줄이 걸려 목록이 뜻이 없다.
  치는 동안 250ms 쉬면 그때 한 번 간다(글자마다 물어보지 않는다).
- **결과 목록은 머리말 아래를 덮는다**(`position: absolute`). 흐름에 넣어
  `flex: 1`로 두었더니 대화 목록과 자리를 반씩 나눠 가져 **결과 아래로
  보라색 대화가 그대로 보였다.** 덮어 두면 대화 목록은 손도 안 댄 채
  살아 있어(굴려 둔 자리·키보드 셈이 그대로다) 닫으면 보던 자리로 돌아온다.
- **닫는 단추는 `취소`라는 글자다**(카톡과 같다). ✕로 두면 칸 안에 브라우저가
  붙여 주는 '글자 지우기' ✕와 나란히 서서, 생김새가 같은 둘이 서로 다른
  일을 한다.
- **찾은 글이 지난 묶음에 있으면 그 글을 가운데 두고 앞뒤를 받아 온다**
  (`openHit`). `더 보기`를 몇 번씩 누르게 하면 찾아 준 뜻이 없다.
- **그때는 `최근 대화로` 단추가 뜬다**(`windowed`). 목록이 그 언저리만
  담고 있어 **아래로 끝까지 굴려도 최근 대화가 없다** — 이 단추가 없으면
  나갔다 다시 들어와야 한다. 같은 까닭으로 **그동안은 실시간으로 들어오는
  새 글을 안 붙인다**: 한참 지난 글 바로 뒤에 오늘 글이 앉기 때문이다.
  그 값은 **`ref`로도 들고 있어야 한다**(`windowedRef`) — 구독은 한 번만
  걸리므로 그 안에서 읽는 state는 처음 값에 굳는다.

**글 안의 주소는 눌리는 링크가 된다**(`lib/links.ts` · `.chat-link`).
카톡처럼 붙이면 그대로 눌러 들어간다. **`@언급`과 같은 결이다** — 칸에
저장하지 않고 그릴 때 찾으므로, 규칙을 바꾸면 예전 글에도 그대로 먹는다.

- **`http(s)://`나 `www.`로 시작하는 것만 잡는다.** 맨몸 도메인(`naver.com`)까지
  잡으면 `오전 9.30분` 같은 글이 주소로 둔갑한다 — 눌러 봐야 아무 데도 안 가는
  링크가 생기는 쪽이 훨씬 성가시다.
- **한글에서 끊는다.** `https://…/mudeung 에서 하시면` 처럼 주소 뒤에 한글이
  바로 붙는 일이 잦은데, 안 끊으면 문장 끝까지 통째로 링크가 된다. 진짜
  주소 속 한글은 대개 `%EB%AC%B4`로 바뀌어 오므로 손해가 없다.
  끝에 붙은 문장부호(`.` `,` `)`)도 뗀다.
- **새 탭으로 연다.** 홈 화면 앱에는 주소창도 뒤로 가기도 없어서, 같은
  창으로 나가면 **앱을 껐다 켜야 돌아온다.**
- **먼저 언급으로 자르고, 언급이 아닌 조각만 다시 주소로 자른다.** 순서를
  뒤집으면 이름이 주소 안에 들어 있을 때 링크가 쪼개진다.

#### 말풍선을 길게 누르면 — 누른 자리에 카톡과 같은 창이 뜬다

손짓 하나에 할 일이 여럿이라, 길게 누르면 먼저 **고르는 창**(`.chat-menu`)이
뜬다. **그 창은 화면 아래가 아니라 누른 말풍선 옆에 뜬다**(사용자 요청 —
`누른 자리에서 나오도록해줘` · 카톡 사진을 받아 맞췄다).

**줄은 사용자가 정해 준 차례 그대로다** — `복사 · 선택 복사 · 댓글 · 공유 ·
캡쳐`, 그 뒤에 운영진의 `가리기`·`공지로 올리기`, 쓴 사람의 `삭제`.

- **누구에게 무엇이 붙는지가 규칙의 전부다.** 앞 다섯은 **누구나**,
  `가리기`·`공지로 올리기`는 **운영진**(사용자 요청 — `가리기는 운영진만
  할수있도록`), `삭제`는 **쓴 사람**(제 글에만).
  가린 글에는 `복사`·`선택 복사`·`댓글`과 반응 알약을 안 붙인다 —
  덮어 둔 내용이 그리로 샌다.
- **`댓글`은 왼쪽으로 밀면 걸리는 그 답장과 같은 일이다**(사용자가 정한
  이름이다 — `댓글이 답장기능과 같은거고`). 카톡의 실 댓글처럼 말풍선
  아래에 `↳ 댓글 2`가 달리는 것이 아니다. **이름만 보고 새로 만들지 말 것.**
- **남의 글은 지우지 않는다.** 되돌릴 수 없는 일이라 운영진에게도 안 열었다 —
  급한 불은 `가리기`로 끄고, 그건 언제든 푼다. **`삭제`를 남의 글로
  넓히지 말 것.**
- **손짓은 길게 누르기다**(PC는 오른쪽 클릭). 왼쪽으로 미는 것은 댓글이
  이미 쓰고 있어 겹치면 안 된다 — 조금이라도 움직이면 길게 누르기는
  취소되고, 창이 떴으면 손을 뗄 때 댓글이 안 걸린다.
- **고르면 창을 닫고 그 일을 한다.** 둘이 겹쳐 있으면 무엇을 누르는
  중인지가 흐려진다. 삭제 쪽은 **되돌릴 수 없다고 적는다.**
- **창은 어느 글에서나 뜬다.** 남의 글이라고 막으면 정작 `복사`할 길이
  없어진다.

**자리는 `HoldAt`(Chat.tsx)이 재서 정한다.** 안에 든 것의 크기를 미리 알
수가 없어서다 — 줄 수가 사람마다 다르고(운영진은 여덟, 남의 글을 누른
회원은 다섯), 가린 글에는 알약이 아예 없다. 규칙 넷:

- **가로는 말풍선의 가까운 쪽에 붙인다** — 내 글은 오른쪽 끝을, 남의 글은
  왼쪽 끝을 맞춘다. 누른 자리에서 눈이 안 움직인다.
- **세로는 아래를 먼저 보고, 안 들어가면 위로 넘긴다.** 화면 가장자리에서
  8px은 띄운다.
- **높이는 `visualViewport`로 본다** — 키보드가 올라와 있으면 보이는 높이가
  그만큼 작다(`clientHeight`는 아이폰에서 안 줄어든다).
- **재기 전에는 `visibility: hidden`이다.** 안 그러면 첫 프레임에 왼쪽 위
  구석에서 한 번 번쩍인다. `useLayoutEffect`라 그리기 전에 자리가 잡힌다.

**미리보기 머리말(`.chat-menu-head`)은 없앴다** — 화면 아래에서 올라오던
때는 어느 글을 누른 것인지 창만 봐서는 몰라 그 줄이 필요했는데, 말풍선
옆에 뜨면 자리가 이미 말해 준다(카톡도 머리말이 없다).
**`닫기` 줄도 없앴다** — 바탕을 누르면 닫힌다.
**바탕은 옅게 덮는다**(`.chat-menu-back.soft`, 0.30) — 짙게 가리면 어느
글을 누른 것인지가 사라진다. 프로필 카드·선택 복사는 화면 가운데를
차지하는 창이라 예전대로 짙다.

- **창 크기는 카톡 화면을 픽셀로 재서 맞춘 값이다.** **배율을 몰라도 되는
  방법으로 잰다** — 같은 폰에서 우리 창과 카톡 창을 잇따라 찍어,
  **우리의 아는 값으로 카톡 쪽을 환산한다.** 우리 한 줄 34px이 사진에서
  77픽셀이고 카톡이 91.5픽셀이었으므로 `34 × 91.5 ÷ 77 ≈ 40px`이다.
  창 폭도 같은 방법으로 **296px**이 나와, 300px과 재는 오차 안이라 그대로
  두었다. **눈대중으로 고치지 말 것.**
  - 한 줄 **40px** · 글자 **15px** · 창 폭 **300px** · 모서리 **14px**.
    30px 아래로 내리지 말 것(`node .dev/audit.mjs`가 잡는다).
    (예전 값 34px은 **배율을 3.5로 잘못 잡아** 나온 것이다 — 실제로는
    3.0이었고, `34 × 3.5 ÷ 3.0 = 39.7`로 지금 값과 맞는다.)
  - **줄 사이를 띄우지 않고 선으로 가른다.** 카톡의 그 창은 알약이 여럿
    쌓인 것이 아니라 **종이 한 장에 그은 줄**이라 그것만으로 단출해 보인다.
    `.chat-menu`에 `overflow: hidden`이 한 쌍이다(선이 모서리를 넘는다).
    **테두리는 없앴다** — 카톡의 그 카드는 선 없이 그림자만 있다.
  - **글자는 왼쪽, 그림은 오른쪽이다**(카톡과 같다). 그림은
    `components/HoldIcons.tsx`의 선 SVG 여덟 개다 — **그림글자를 쓰지 말 것**:
    기기에 없으면 네모난 두부가 나온다(투표 결과 카드의 `🗳`에서 겪었다).
    색은 `currentColor`라 `삭제` 줄에서는 저절로 빨강이 된다.
    **프로필 카드(`.chat-card`)는 가운데 정렬 그대로이고 그림도 안 붙는다** —
    거기는 목록이 아니라 얼굴 아래 붙는 단추다.
  - **반응 알약은 창에 붙어 있지 않고 아래에 따로 뜬다**(사용자 요청 —
    `이모티콘도 카톡처럼 하단에 넣어줘`). **언제나 맨 아래**라, 창이
    말풍선 위에 뜰 때는 그 알약이 말풍선에 가장 가까운 쪽이 된다.
    칠은 안 깐다(카톡도 그림글자만 있다).
    **카톡의 `+`(더 고르기)는 안 만든다** — 우리 반응은 다섯으로 못박혀
    있어(`REACTIONS`) 더 고를 것이 없다.
  - **`.chat-card`와 확인창(`Confirm`)의 모서리·폭도 함께 맞춰 두었다** —
    창에서 고르면 곧바로 확인창이 뜨는데 모서리가 다르면 다른 앱의 창처럼
    보인다. `.dev/behave.mjs`의 `길게 누른 창의 크기`·`창이 누른 자리에서
    뜬다`·`메뉴 줄과 선택 복사` 칸이 붙들어 둔다.

##### 선택 복사 · 공유 · 캡쳐

- **`선택 복사`는 말풍선에서 못 하는 일을 되돌리는 자리다.** 말풍선에는
  `user-select: none`이 걸려 있어(아래 참고) **글의 일부만 가져갈 길이
  아예 없다.** 그래서 글을 펼쳐 놓고 **거기서만** 글자 고르기를 켠다
  (`.chat-pick-body`). 글자 16px도 그대로 둘 것 — 그보다 작으면 iOS가
  글을 칠 때 화면을 확대한다.
- **`공유`·`캡쳐`는 `lib/share.ts` 하나에 있다.** 둘 다 **폰이 해 주는 일을
  부르는 것뿐**이라 서버도, 붙여넣을 SQL도 없다.
- **되는지는 기기마다 다르다.** `navigator.share`는 아이폰에서는 되지만
  PC 크롬에는 없다. **그때 조용히 돌아서면 안 된다** — 공유는 복사로
  물러나고 그 사실을 토스트로 알린다(아무 일도 안 일어나면 고장으로 보인다).
- **캡쳐는 `html-to-image`가 화면에 그려져 있는 그 줄을 그대로 찍는다** —
  말풍선 모양을 두 번 만들지 않으려는 것이다.
  - **누를 때 받아 온다**(`import()`). 한 번도 안 누르는 사람이 대부분인데
    30KB를 늘 안고 다닐 이유가 없다(따로 12.9KB 조각으로 빠진다).
  - **`[data-mid]`가 아니라 그 안의 `.chat-row`를 찍는다.** 바깥 칸에는
    날짜 칸과 `여기까지 읽으셨습니다` 줄이 함께 들어 있어 그대로 딸려 온다.
  - **여백을 주면 캔버스도 그만큼 키울 것**(`width`/`height`를 직접 넘긴다).
    `style`로 padding만 주면 **아래가 잘린다**(시각 줄이 반쯤 잘려 나왔다).
  - **글꼴은 안 심는다**(`skipFonts`). 우리 Pretendard는 화면에 나온 글자만
    받아 오는 방식이라, 심으려 들면 CDN에 수십 번 다녀오느라 몇 초가 걸린다.
    폰의 기본 한글 글꼴로 그려지는데 그림으로는 그게 더 자연스럽다.
  - **파일 이름만은 영문이다**(`kkakkung-0908-214305.png`). `까꿍-…`으로
    두었더니 **브라우저가 한글 이름을 버리고 `download`로 저장했다**
    (크로미움에서 확인했다) — 그러면 여러 장이 서로 덮어쓴다.
    내려받는 `<a>`는 **문서에 붙였다 눌러야** 그 이름이 먹는다.
  - 아이폰에서는 공유창(`navigator.share`)으로 가고, 없는 기기에서는
    내려받는다. **사파리는 그 길에서 새 탭으로 그림을 열어 준다** — 거기서
    길게 눌러 저장하면 된다. 그것까지가 웹이 할 수 있는 전부다.
- **글자 고르기는 손가락으로 쓰는 기기에서만 막는다**(`@media (hover: none)`).
  iOS는 글자를 길게 누르면 제 선택 손잡이를 띄워 우리 손짓을 덮는다.
  **마우스가 있는 기기는 그대로 둔다** — 거기서는 길게 누를 일이 없고
  글 일부만 끌어 복사하는 것이 더 편하다. 폰에서 잃는 것은 창의 `복사`와
  **`선택 복사`**가 대신한다(그 줄이 있는 까닭이 이것이다).
- **그 막는 규칙을 이모티콘·사진에도 함께 걸 것**
  (`.chat-sticker`·`.chat-image`·`.chat-photo-link`). 사용자 제보로 잡은
  자리다(`이모티콘은 그런 기능들이 안되네`) — 한동안 `.chat-bubble`에만
  걸려 있었는데, **그림에는 iOS가 제 `이미지 저장 / 복사 / 공유` 시트를
  띄워** 우리 길게 누르기를 통째로 덮는다(사진은 `<a>`라 링크 미리보기까지
  뜬다). 그래서 폰에서만 **이모티콘·사진의 반응·답장·지우기가 아예 안
  먹었다.** 사진 저장은 눌러서 연 새 창에서 하므로 여기서 막아도 잃는 것이
  없다.
  **`behave`에서 창이 열리는지를 봐야 소용없다** — 크로미움에는 그 시트가
  없어서 고치기 전 코드도 초록으로 뜬다(깜빡임에서 얻은 교훈 그대로다).
  그래서 **막는 값 자체를 잰다**(`폰에서 그림을 길게 누르기` 칸).
  손가락 기기에서만 거는 규칙이라 `hasTouch`인 창이 따로 필요하다.
- **정책을 새로 안 만들었다 — 붙여넣을 SQL이 없다.** 가리기는
  `messages_admin`이 이미 `for all`이고(회원에게는 update 정책이 아예 없다),
  지우기는 `messages_own`이 처음부터 `user_id = auth.uid()`인 줄만 열어 두었다.
  화면이 감추는 것과 DB가 막는 것이 같은 잣대다.
- **실시간은 갈래가 둘이다.** 대화는 들어온 줄을 덧붙이기만 하는데,
  가리기는 **고치기**라 그 줄만 갈아 끼우고(UPDATE) 지우기는 그 줄만
  빼낸다(DELETE) — 다시 불러오면 굴려 둔 자리를 잃는다.
- **지운 글에 달린 답장은 남는다**(`reply_to`가 `on delete set null`).
  인용이 `지난 대화`가 되고 눌러도 안 움직인다.
- **인용에도 새면 안 된다** — `preview()`가 가린 글을 `가려진 메시지`로
  바꾼다. 사진에 함께 적은 글도 함께 덮는다.
- **가리기는 화면에서 덮는 것이지 서버에서 지우는 것이 아니다.** 글은 행에
  그대로 남아 있어 마음먹고 파 보면 읽힌다. 칸 단위로 가리려면 `select('*')`가
  통째로 막혀 앱이 조용히 깨지므로(위 `profile_private` 참고) 그 길을
  안 갔다. **정말 없애야 할 글은 쓴 사람이 지우는 것이 맞다.**
  사용자에게도 그렇게 알렸다.

대화에는 **사진 한 장**을 함께 보낼 수 있다. 파일은 Storage의 `chat-photos`
통(공개)에 넣고 `messages.image_url`에는 주소만 남는다. 올리기 전에
`lib/image.ts`가 긴 변 2560px·JPEG 82%로 줄인다 — 폰 사진 원본은 3~5MB라
그냥 올리면 저장 공간도, 받는 사람 데이터도 금방 축난다.
**사진이 없을 때는 `image_url`을 아예 보내지 않는다** — DB에 그 칸이 없는
저장소(스키마를 다시 안 돌린 곳)에서도 글은 오가게 하려는 것이다.
사진이 늦게 뜨면서 목록을 밀어내므로 `onLoad`에서 다시 바닥에 붙인다.

**늦게 뜬 사진이 읽던 자리를 밀어내지 않게 우리가 메운다**(`onImageLoad`).
**"앱에 처음 들어가서 채팅창을 위로 올리면 끊기면서 올라간다. 한 번 올리고
난 뒤에는 괜찮다"**는 제보에서 나온 것이다. 사진은 화면에 보일 때가 되어야
받아 오고(`loading="lazy"`) 받기 전에는 높이가 거의 0이라, 위로 훑는 도중에
**화면 위쪽 사진이 도착하면 그만큼 글이 통째로 아래로 밀린다** — 두 번째
스크롤이 멀쩡한 것은 그때는 이미 받아 둬서 높이가 안 변하기 때문이다.
헤드리스로 재 보니 **사진 한 장에 125px씩 네 번** 튀었고, 사진이 없는 방에서는
한 번도 안 튀었다.

- **크로미움은 이걸 알아서 메워 주고(scroll anchoring) 사파리는 안 한다.**
  그대로 두면 아이폰에서만 나는 자국이 된다.
- **브라우저 것을 끄고 우리가 메운다**(`.chat-list`의 `overflow-anchor: none`).
  켜 둔 채로 메우면 크로미움에서만 **두 번** 메워져 이번엔 반대로 튄다
  (실제로 250px씩 튀었다). 꺼 두면 어느 브라우저에서나 같게 돌고,
  **헤드리스로 확인한 것이 폰에서도 맞는다** — 이게 이 방식을 고른 까닭이다.
- 자란 만큼은 `listH`(마지막으로 잰 목록 높이)와의 차이로 안다. 그래서
  `onScroll`과 `pinBottom`이 굴릴 때마다 그 값을 다시 적어 둔다.
- **화면 위쪽에서 자란 것만 메운다.** 보고 있는 자리 아래에서 자라는 것은
  원래 그렇게 밀리는 것이 맞다(크로미움도 그렇게 둔다). 사진의 **윗변**으로
  가리는 것은, 자라는 쪽이 아래라 윗변은 안 움직이기 때문이다.
- **사진 높이를 미리 잡아 두는 길로 가지 않았다** — 어떤 사진이 올지 모르는데
  칸을 정해 버리면 세로 사진이 잘리거나 여백이 남는다. 지금 방식은 사진이
  보이는 모양을 한 픽셀도 안 바꾼다.
- `.dev/behave.mjs`의 `늦게 뜬 사진` 칸이 붙들어 둔다 — 사진을 150ms 늦게
  주는 방을 띄워 놓고 한 프레임에 60px씩 올리며, 붙박아 둔 말풍선이 딱
  60px씩 내려오는지 본다. 메우는 줄을 빼면 거기서 125px씩 네 번 튄다.
  **그 방은 사진이 예순 장이고 130프레임을 훑는다** — 아래 '미리 받아 두기'가
  가까운 사진을 먼저 받아 버려서, 사진이 적거나 짧게 훑으면 목록이 아예
  안 자라 **검사가 헛돈다**(실제로 그렇게 됐다). 첫 줄이 '자라기는 하는가'를
  먼저 보는 것이 그 때문이다.

**그런데 이것만으로는 사용자가 느끼는 끊김이 안 없어졌다**(`똑같네`).
자리가 튀는 것과 **화면이 버벅이는 것**은 다른 자국이었다 — 아래를 볼 것.

#### 미리 받아 두기 — 끊김의 진짜 몫

`node .dev/jank.mjs`로 폰만큼 느리게(CPU 12배) 재 보니 갈렸다.
**첫 스크롤에 밀린 프레임**(세 번 평균):

| | 미리 받아 두기 전 | 넣은 뒤 |
|---|---|---|
| 글만 | 1.0 | 1.0 |
| 사진 | 6.0 | 2.0 |
| 움짤(움직이는 이모티콘) | 6.3 | 1.0 |
| 섞임 | **12.0** (가장 긴 프레임 133ms) | **3.7** (50ms) |

**글만 있는 방은 원래 안 끊긴다.** 그림을 **언제** 받느냐가 전부였다 —
`loading="lazy"`라 화면에 보일 때가 되어야 받아 오는데, 그 바람에 받아
오고 푸는 일이 **손가락이 움직이는 바로 그 순간에** 벌어진다. 두 번째
스크롤이 멀쩡한 것은 이미 받아 둬서 할 일이 없기 때문이다.

그래서 **사람이 읽고 있는 동안**(=아무 일도 안 하는 동안) 위쪽 그림을
조금씩 미리 켜 둔다(`Chat.tsx`의 `미리 받아 두기` 효과).
넷을 지킬 것 — 하나씩 다 재서 얻은 것이다:

- **진짜 요소의 `loading`을 `eager`로 바꾼다.** 따로 만든 `new Image()`로
  받아 두는 길도 해 봤는데 **전혀 안 줄었다**(15개 그대로) — 그렇게 받은
  것은 화면에 걸린 그림과 따로 논다.
- **그림 자리를 재서 고르지 말 것.** '화면에서 얼마나 떨어졌나'로 골랐더니
  **되레 나빠졌다**(12.0 → 14.7). `getBoundingClientRect()`가 그때마다
  배치를 다시 잡게 하는데 하필 굴리는 중에 돈다. 자리를 안 보고 **아래에서
  부터 차례로** 켠다 — 대화는 위로 훑어 올라가므로 그것이 곧 가까운 순서다.
- **굴릴 때 부르지 말 것.** 같은 이유다. 이 일은 사람이 읽는 동안 끝나야 한다.
- **한 번에 세 장씩 나눠서 켠다.** 한꺼번에 켜면 그 자체로 한 프레임을
  먹어 미리 받는 뜻이 없어진다.
  **틈은 200ms에서 80ms로 줄였다** — 실기기에서 **첫 스크롤만** 여전히
  끊긴다는 제보가 남았고(`처음 한번만 나와`), 그건 곧 **아직 다 못 받아 둔
  것이 굴리는 도중에 받아진다**는 뜻이다. 200ms에 세 장이면 쉰 장을 다 켜는
  데 3.3초인데 대화를 열고 3초를 가만히 있는 사람은 없다. 80ms면 1.3초다.
  **`한꺼번에 켜지 말 것`은 그대로다** — 그때 잰 것은 '한 번에 다' 대
  '나눠서'였지 200이라는 값이 아니었다. **더 줄이지는 말 것**(한 프레임
  아래로 내려가면 나눈 뜻이 없어진다).
- **받아 두는 것만으로는 모자란다 — `decode()`로 푸는 일도 미리 시킨다.**
  `eager`는 파일을 받아 오게 할 뿐이고 **압축을 푸는 것은 그릴 때** 벌어진다.
  사파리는 그 일을 화면 그리는 갈래에서 하므로(아래 '이모티콘 그림' 꼭지)
  굴리는 도중에 걸리면 그대로 끊긴다. **기다리지 않는다** — 기다리면 다음
  묶음이 그만큼 늦어진다.
- **멈춘 이모티콘 몫을 30 → 80으로 올렸다.** 우리 대화방은 이모티콘이
  대부분인데 서른에서 몫이 떨어지면 그 위쪽 것이 결국 굴리는 도중에
  받아진다. 한 장 9KB라 여든 장이 720KB이고 1년 캐시다.
  **움직이는 것(6장)과 사진(8장)은 그대로 둔다** — 거기는 진짜로 무겁다.

**몫을 정해 둔 것은 통신량 때문이다.** 사진은 `WARM_PHOTOS`(8장)까지만 —
Supabase에서 오고 무료 통신량이 월 5GB다. 한 번 받으면 1년 캐시라
(`cacheControl: '31536000'`) 다시 들어올 때는 안 받으므로, 더 드는 것은
**끝내 안 볼 사진 몇 장**뿐이다. 몫은 방을 옮길 때와 `지난 대화 더 보기`를
누를 때 새로 준다.

**이모티콘은 멈춘 것과 움직이는 것의 몫이 다르다** — 멈춘 것은 한 장 7KB라
`WARM_STICKERS`(30장)까지 넉넉히 켜지만, **움직이는 것은 평균 239KB로 서른
배가 넘어** `WARM_ANIM`(6장)뿐이다. 같은 몫으로 두면 대화를 열 때마다 7MB를
받아 **미리 받아 두려다 폰이 더 느려진다**(LTE에서는 더). 가르는 잣대는
`img.src`가 `.webp`로 끝나는가다. 6장으로 줄여도 밀린 프레임은 그대로였다
(움짤 1.0 → 1.3 · 섞임 3.7 → 3.0).

#### 이모티콘 그림은 서비스워커가 캐시한다 — 캐시 규칙의 유일한 예외

**GitHub Pages는 `max-age=600`으로 보낸다.** 그래서 움직이는 이모티콘
(열여덟 장 4.3MB)이 **10분마다 다시 받아졌다.** 대화를 하루 몇 번만 열어도
그만큼 폰 데이터가 나가고, 하필 훑는 순간에 받아 오면 화면이 끊긴다.

`public/sw.js`가 **`/stickers/` 아래만** 캐시에 남긴다(`STICKER_CACHE`).
**다른 주소로 넓히지 말 것** — index.html이 캐시에 남으면 고쳐도 옛 화면이
그대로 뜬다(위 '주의사항' 첫 줄이 그 자리다). 이모티콘 그림은 한 번 만들면
안 바뀌므로(바꿀 일이 생기면 새 id로 넣는다) 이 예외가 안전하다.
정말 갈아 끼우면 `STICKER_CACHE`의 번호를 올리면 옛 캐시가 통째로 버려진다.
**실패한 답(404)은 캐시에 안 넣는다** — 그림을 새로 넣었을 때 '없다'가
굳으면 영영 안 뜬다.

**아이폰 쪽은 여기서 못 잰다.** `/opt/pw-browsers`에는 크로미움뿐이라
`jank.mjs`도 크로미움으로 돈다. **크로미움은 움직이는 그림을 딴 갈래에서
푸는데 사파리(WebKit)는 화면을 그리는 그 갈래에서 푼다** — 그래서 여기서
`움짤 1.0`으로 나와도 아이폰에서는 더 나쁠 수 있다. 깜빡임에서 얻은 교훈
그대로다(**흉내로 초록이 떴다고 고쳐진 것이 아니다**). 이모티콘이 남은
원인으로 지목되면 다음 손은 **그림 자체를 가볍게 하는 것**이다 —
프레임 수(12fps)·크기(256px)·`loop=3`을 줄이면 파일도 푸는 값도 함께 준다.
**보이는 그림이 달라지는 일이라 사용자에게 묻고 할 것.**

**`loading="lazy"`를 아예 빼지 말 것.** 그렇게 하면 숫자는 가장 좋지만
(섞임 3.0) 대화를 열 때마다 그 방의 사진을 통째로 받아 **통신량이 월
5GB를 훌쩍 넘긴다.** 미리 받아 두기는 그 사이를 고른 것이다.

#### 이모티콘 — 사진과 같은 칸을 쓴다

그림은 `public/stickers/`에 있고 목록은 `lib/stickers.ts`에 있다.

**백여든여섯 장이고 여덟 묶음이다** — `✨ 움직임`(18) · `⛳ 골프공`(27) ·
`🧢 골퍼`(24) · `🐻 골프친구`(24) · `🐧 펭귄골프`(22) · `💙 펭귄`(28) ·
`🐱 고양이`(24) · `😀 이모지`(18). 예전 백여든일곱 장은 배경을 벗겨 내도
흰 카드나 모눈 자국이 남아 **다 지웠고**, 배경이 처음부터 투명한 것으로
다시 채웠다. 넣는 방법은 `lib/stickers.ts` 머리말에 세 걸음으로 적어 두었다.

**`⛳ 골프공`은 앱 아이콘과 같은 캐릭터다** — 선글라스 낀 골프공. 시트가
석 장 왔는데 `까꿍day`·`Good!`·`한잔해요!`처럼 **거의 같은 것이 시트마다
되풀이돼서**, 한 장을 뼈대로 삼고 나머지 둘에서 **겹치지 않는 것만** 더했다
(사용자 요청 — 중복은 뺀다). 펭귄도 두 시트에서 겹치는 넷을 뺐다.
**`🧢 골퍼`도 골프공이지만 다른 세트다** — 모자에 `GOLF`가 적히고 남색
셔츠를 입었다. 그림이 다르므로 묶음을 따로 뒀다(같은 캐릭터로 묶으면
`나이스샷!`이 어느 탭인지 매번 헷갈린다). 그 시트는 겹치는 것이 없어
스물넷을 그대로 넣었다.
**`🐻 골프친구`는 캐릭터가 여럿 섞인 시트다** — 소년·소녀·펭귄·곰·강아지·
고양이가 함께 라운딩한다. **캐릭터가 아니라 세트로 묶는 규칙 그대로다** —
곰과 펭귄을 각자의 묶음에 흩어 넣으면 한 시트가 세 탭으로 쪼개진다.
**중복인지는 눈으로 가른다** — 그림이 같은 캐릭터라 비슷함을 숫자로 재면
겹치지 않는 것까지 나란히 붙는다(48px 썸네일 코사인으로 재 봤더니 상위
마흔 쌍이 거의 다 중복이 아니었다). **자동으로 거르려 들지 말 것.**

**시트를 자르는 법.** 예전에는 '바탕 띠'로 갈랐는데, 이 시트들은 글자가
위아래로 삐져나와 **띠가 붙어 버려 스무 칸이 아홉 칸으로 뭉쳤다.**
지금은 **격자와 덩어리를 함께 쓴다**: 시트를 칸 수(5×4 · 6×4 · 6×5)로 나눠
놓고, 알파의 **연결 덩어리마다 무게중심이 든 칸**에 넣는다. 하트·반짝임·
글자처럼 떨어져 있는 조각도 제 칸으로 들어가고, 옆 칸과 붙지도 않는다.
**덩어리를 부풀려 묶지 말 것** — 9px만 부풀려도 옆 칸과 붙는다.

**배경이 체커 무늬로 그려진 시트도 온다**(`😀 이모지`). 알파가 없는 그냥
PNG인데 **체커가 곧 '투명'이라는 뜻**이라, 영상 때와 같이 벗겨서 쓴다.
오래 헤맨 자리라 얻은 것을 적어 둔다:

- **`rembg`가 주인공을, 색 판정이 나머지를 맡는다.** rembg는 얼굴·손은 깨끗이
  따지만 옆에 떨어져 있는 글자(`안녕`·`boo`)·반짝임·동전은 **주인공이 아니라고
  보고 통째로 뺀다.** 그 자리만 바싹 잘라 rembg를 한 번 더 돌리고, 그래도
  안 나오면 체커 판정으로 직접 더한다.
- **체커인지는 '22px 이웃과의 차이'로 본다.** 바탕이면 칸이 뒤바뀌어 크게
  다르고(≈92), 그림 안이면 거의 같다. 무채색(채도 ≤13)까지 함께 본다.
- **글로우가 덮인 바탕이 제일 어렵다.** 얼굴 둘레에서 무늬가 흐려져
  '아직 안 딴 알맹이'로 잡히는데, 그걸 더하면 **얼굴 둘레에 네모난 체커가
  통째로 살아난다.** 두 가지로 거른다 — ① 격자 판(밝은 칸 +1 / 어두운 칸 −1)
  과의 상관이 크면 무늬가 아직 있는 것이다 ② 글로우 띠는 주인공에 딱 붙어
  있다(둘레의 40%가 이미 딴 자리면 버린다). **한 잣대만으로는 못 가른다** —
  파란 글로우처럼 센 것은 무늬가 거의 씻겨 ①에 안 걸리고, `안녕`처럼 옆에
  붙은 글자는 ②에 걸릴 뻔한다.
- **채우기(`floodFill`)는 1px 테두리를 덧대고 할 것.** 덩어리가 칸 모서리에
  닿아 있으면 (0,0)에서 시작한 채우기가 막혀 **칸 전체가 알맹이가 된다.**
  실제로 그래서 열여덟 칸이 다 체커로 나왔다.
- **못 딴 것은 못 딴 대로 두고 알린다.** `boo`(엄지 아래)처럼 흰 글자에
  옅은 테두리만 있는 것은 끝내 안 나왔다 — 억지로 넣으면 체커가 함께 온다.

**영상에서 잘라 넣은 것이다**(`✨ 움직임`). 사용자가 준 영상 두 편
(24fps·1280×720)에 장면이 여럿 들어 있고, **배경이 체커 무늬로 그려진
장면이 곧 '투명'이다** — 파스텔 바탕(노랑·분홍·초록…)이 깔린 장면은
규칙대로 뺐다. 스무 장면 가운데 열둘이 체커였다. 만든 순서는 이랬다:

- **장면은 프레임끼리의 차이로 가른다.** 컷이 뚜렷해 한 번에 갈린다.
- **체커인지는 두 색만으로 이뤄졌는지로 본다** — 41px(두 칸) 창 안에서
  `226~244`와 `248~255` 두 무리가 각각 30% 넘고 그 둘이 90%를 넘으면 체커다.
  **창을 한 칸(21px)으로 잡지 말 것** — 셀 경계에서 구멍이 뚫린다.
- **오려 내는 것은 `rembg`(isnet-general-use)가 한다.** 체커를 재구성해
  빼는 길을 오래 팠는데(배경 판을 세워 차이를 보고, 격자를 맞춰 보고,
  rank-1로 가운데를 메우고) **캐릭터가 흰데 체커의 밝은 칸도 흰색**이라
  끝내 깨끗해지지 않았다 — 발밑 그림자가 체커를 비쳐 얼룩으로 남았다.
  `rembg`는 캐릭터와 글자를 **한 번에** 따 주고 그림자도 저절로 빠진다.
  **다음에도 여기부터 시작할 것.**
- 잘라 낼 자리는 **그 장면 모든 프레임의 알파를 더해** 잡는다(한 프레임에만
  스치는 색종이 조각까지 다 담으면 캐릭터가 손톱만 해진다). 정사각으로
  넓히고 여백을 조금 준 뒤 256px로 줄인다.
- **12fps**(원본 두 프레임에 한 장) · `loop=3` · webp 품질 80.
  품질을 34까지 내려 봐도 파일이 10%밖에 안 줄었다 — **알파와 움직임이
  크기를 정하므로 품질을 깎아 봐야 그림만 나빠진다.** 한 장 85~610KB다.

**갈색 곰 영상 여섯도 같은 묶음에 넣었다**(`mvblove`·`mvbfight`·`mvbthanks`·
`mvbcry`·`mvbyay`·`mvbsleep`). 한 편에 장면 여섯이 이어져 있고 컷이 흐릿해
프레임 차이로는 안 갈려서, **눈으로 보고 경계를 적어 두었다**(`mv/buildC.py`의
`SCENES`). 세 가지가 이 영상에서 새로 걸렸다:

- **체커의 두 톤 차이가 10밖에 안 난다**(135 대 145). 시트에 쓴 '22px 이웃과의
  차이'가 안 먹어서, **바탕이 거의 완전한 무채색 회색(채도 ≤4, 밝기 116~151)**
  이라는 것만으로 갈랐다. **영상마다 체커가 다르니 먼저 재 볼 것.**
- **글자가 다 뜬 구간만 담는다.** 장면 앞뒤로 글자가 흐려지는 프레임이 있어,
  그대로 담으면 `loop=3`이 끝났을 때 **글자 없는 그림에서 멈춘다.**
- **자를 자리는 색종이까지 담지 말 것.** 색종이 대포가 화면 폭을 다 써서
  그대로 담았더니 한 변이 1356px이 되어 **곰이 손톱만 해졌다.** 지금은
  780px로 막아 둔다.

**묶음(카테고리)이 곧 서랍의 탭이다**(사용자 요청 — 카카오톡처럼).
한 줄로 늘어놓았더니 백예순 장 가운데 아래쪽 것은 아무도 끝까지 굴려 보지
않았다. **다시 채울 때도 묶음으로 나눌 것** — 백 장이 넘어가면 한 줄로는
아래쪽이 아무에게도 안 보인다. 예전에는 여덟 묶음이었다(`✨ 움직임` ·
`⛳ 골프` · `🐻 곰돌이` · `🦊 동물` · `🐨 코알라` · `😀 표정` ·
`🌙 아침·밤` · `🍔 먹을 것`). **`.dev/behave.mjs`의 '탭을 옮기면' 두 줄은
묶음이 셋 이상일 때만 돈다** — 한 묶음뿐이던 때 저절로 건너뛰게 해 둔 것이다.

- **묶음은 뜻이 아니라 '그림이 나온 세트'로 가른다.** 카톡이 이모티콘
  세트마다 탭을 두는 것과 같다 — 뜻으로 나누면 `화이팅!`이 어느 탭에
  있는지를 매번 헷갈린다(같은 말이 세트마다 하나씩 있다).
- **탭은 위에 둔다.** 카톡은 아래에 두지만 이 앱은 화면 맨 아래가
  탭바(홈·공지·…) 자리라, 서랍이 그 밑으로 깔려 가려진다.
- **그림글자만 두지 않고 이름을 함께 적는다.** 그림 하나로는 무슨 묶음인지
  알 수 없고, 그림글자가 없는 기기에서는 네모난 두부만 남는다
  (투표 결과 카드의 `🗳`에서 겪은 것과 같다). 일곱 개가 320px에서 넘치므로
  **탭 줄만 옆으로 굴러가게** 두었다.
- **눌린 탭에 분홍을 쓰지 않는다** — 이 화면에서 '지금 눌러야 할 것'은
  보내기 단추 하나다. 바탕을 카드색으로 올려 종이가 앞으로 나온 것처럼 한다.
- **그림 칸의 `key`가 묶음이다** — 굴려 둔 자리가 남으면 장수가 적은 묶음으로
  옮겼을 때 빈 칸만 보인다.
- **고른 묶음을 기억해 두지 않는다** — 나갔다 오면 첫 묶음으로 돌아간다
  (회원 명단의 차례 고르기와 같은 결이다).

- **DB에 칸을 새로 만들지 않았다.** 사진이 쓰던 `messages.image_url`에
  `sticker:<id>`로 넣고, 그렇게 시작하면 이모티콘으로 본다(`isSticker`).
  칸을 하나 더 두면 **사용자가 손으로 붙여넣어야 하는 SQL이 늘고**,
  스키마를 아직 안 돌린 저장소에서 또 갈라진다 — 이 방식은 사진과 똑같이
  동작한다. 발송기도 같은 잣대로 가른다(`이모티콘을 보냈습니다`).
- **글에 남는 값이 주소가 아니라 id다.** 그림을 다시 만들거나 자리를
  옮겨도 예전 글이 안 깨진다 — 주소는 `stickerSrc()`가 그때그때 만든다
  (`import.meta.env.BASE_URL`을 거치므로 저장소 이름이 붙은 주소에서도 맞다).
  **모르는 id도 그냥 넘긴다**(`stickerLabel`) — 그림만 안 뜨고 화면은 산다.
- **그림은 Supabase가 아니라 GitHub Pages가 내준다.** 회원이 올린 것이
  아니라 앱에 딸린 붙박이라, 무료 통신량(월 5GB)을 대화 사진과 나눠 쓸
  이유가 없다. **한 장은 256px 정사각·64색 팔레트 PNG로 20KB 안쪽**으로
  맞춘다(지금 평균 7KB). 움직이는 것만 webp라 85~610KB로 크고, 백여든여섯
  장이 다 합쳐 6.1MB다 — **크기는 사실상 움직이는 열여덟이 정한다.**
  서랍은 `loading="lazy"`라 화면에 보이는 것만 받는다.
- **고르면 곧바로 나가지 않는다**(사용자 요청). 입력칸 위에 **미리보기로
  물려 두고**(`.sticker-peek`), 글을 마저 적어 **한 마디로 함께 보낸다** —
  `messages` 한 줄에 `image_url`은 `sticker:…`, `body`는 그 글이다.
  한동안 누르는 즉시 나가게 두었는데, 그러면 `나이스 샷!`에 한마디
  덧붙이려면 두 마디로 갈라 보내야 했다. **되돌리지 말 것.**
  미리보기를 누르면 글 없이 그대로 나가고, `✕`로 뗀다.
- **그래서 말풍선에 글이 붙는다.** `caption`이 사진과 같은 잣대를 쓴다
  (`image_url`이 있고 `body`가 있으면 그림 아래 한 줄). 이모티콘만 보낸
  글은 예전처럼 그림 하나다.
- **서랍은 입력칸 아래, 키보드가 서던 자리에 뜬다**(사용자가 보여 준 카톡
  모양이다). 위에 두었더니 대화가 가려졌고, 무엇보다 **글칸이 서랍에 밀려
  올라가** 이모티콘을 고르면서 글을 이어 칠 수가 없었다. **되돌리지 말 것.**
  아래에 두면 `.chat-input`이 두꺼워지고 `.chat-list`가 `flex: 1`이라 저절로
  줄어든다 — 대화는 가려지는 게 아니라 접힌다.
- **키보드와 자리를 맞바꾼다.** 열 때 키보드를 내리고(`toggleTray`), 글칸에
  초점이 가면 서랍을 닫는다(`onComposerFocus`). 그래서 이 단추만은
  **`onMouseDown`을 막지 않는다** — 보내기·언급 목록과 반대다(그쪽은 키보드가
  내려가면 안 된다). 열려 있는 동안 단추 그림이 **자판으로 바뀐다**:
  얼굴 그대로 두면 눌러도 또 이모티콘이 나올 것처럼 보인다.
- **서랍을 열면 대화를 맨 아래로 붙인다.** 목록이 서랍만큼 줄어들어 방금
  읽던 글이 위로 밀려나기 때문이다. **`pinBottom`으로는 안 된다** — 목록이
  줄어드는 순간 브라우저가 스크롤 이벤트를 던지고 `onScroll`이 '맨 아래가
  아니다'로 내려 버려서 아무 일도 안 한다. 그리고 **높이가 한 번에 정해지지
  않는다**(중간값 492px → 최종 434px). 몇 프레임 따라 내리는 것으로도 안
  잡혀서, 지금은 `ResizeObserver`로 **목록이 다시 재어질 때마다** 따라
  내리고 0.6초 뒤에 손을 뗀다 — 그 뒤는 사람이 굴리는 것이라 건드리면 안 된다.
- **여는 단추는 입력칸 안 오른쪽 끝에 얹혀 있다**(사용자가 보여 준 모양이다).
  왼쪽 `+` 옆에 나란히 두었더니 눌러야 할 것이 왼쪽에 둘로 몰렸다.
  흐름에서 빠져 있으므로 `.textarea`의 **오른쪽 여백(40px)과 한 쌍이다** —
  줄이면 글자가 단추 밑으로 지나간다. `bottom`으로 붙여 두어 여러 줄로
  늘어나도 아래쪽 끝을 따라 내려간다. **30px 아래로 내리지 말 것**
  (`node .dev/audit.mjs`가 그보다 작은 누름 자리를 잡아낸다).
- **말풍선을 두르지 않는다**(이모지만 보낸 글과 같은 결이다). 사진과 달리
  **누르는 곳도 아니다** — 원본을 띄워 봐야 같은 그림이다.
- **움직이는 것은 `.webp`이고 그 밖은 `.png`다.** **글에 남는 값은 그대로
  `sticker:<id>`이고** 확장자는 `stickerSrc()`가 붙이므로, 나중에 형식을
  또 바꿔도 예전 글이 안 깨진다.
  - **가르는 잣대는 id의 머리글자 `mv`다**(`ANIM_PREFIX`) — **목록에서
    찾지 않는다.** 한동안 목록(`Sticker.anim`)을 봤는데, 그러면 **그 판이
    모르는 id에서 깨진다**: 남이 새로 만든 움직이는 이모티콘을 보냈을 때
    받는 사람 폰이 아직 옛 묶음을 들고 있으면 `.png`로 찾아 404가 나고,
    그림 자리에 `이모티콘`이라고만 뜬 채 **새로고침해야 나왔다**(실기기
    제보). GitHub Pages가 index.html을 10분쯤 물고 있어 흔한 일이다.
    머리글자는 글에 남는 값이라 판이 달라도 같게 풀린다. 그래서
    **움직이는 것은 id가 반드시 `mv`로 시작해야 하고, 그 밖의 것은
    `mv`로 시작하면 안 된다.**
  - **움직이는 것에도 `<id>.png` 한 장을 함께 둔다**(가운데 프레임).
    위 고침이 닿기 전의 판은 여전히 목록을 보므로, 그 폰에서 안 깨지려면
    `.png`가 있어야 한다. **지우지 말 것** — 이 예비본이 곧 그 기기들의
    유일한 길이다.
  - 그래도 못 찾으면 **다른 확장자로 한 번만 다시 해 본다**
    (`StickerImg`·`otherExt` in Chat.tsx). 앞으로 규칙을 또 바꿀 때의
    예비 길이다.
  - **모르는 id의 이름은 `이모티콘`이다**(`stickerLabel`) — 그림을 지운
    뒤에도 예전 글이 열려야 한다. 보낸 글에서는 둘 다 실패했을 때
    그 이름이 작은 조각(`.chat-sticker-gone`)으로 그려진다.
    **그 조각도 자리는 118×118로 똑같이 차지한다** — 그림이 없다고 자리가
    줄어들면, 아래 '미리 받아 두기'가 그것을 알아내는 순간(80ms마다 세 장씩)
    목록이 그만큼 짧아져 **읽던 자리가 위아래로 튄다.** 실기기 진단에서
    대화방에 들어갈 때 내용 높이가 **272px씩 네 번** 줄었고, 그것이 딱
    이모티콘 세 장 몫(96×3)이었다(사용자 제보 — `처음 채팅 들어갈때 위아래로
    순간 움직이는것`). 알약은 그 안에 넣는다.
  - **APNG로 하지 말 것.** `.png`라 코드를 한 줄도 안 고쳐도 되는 대신
    **한 장이 1MB**였다(움직이는 webp는 85KB다). 열두 배를 코드 한 줄과
    바꿀 수는 없다.
  - **무한 반복이 아니라 세 번만 돌게 만든다**(`loop=3`). 늘 켜져 있는
    그리기 비용이 안드로이드에서 화면을 끊기게 한다는 규칙이 여기에도
    걸린다 — 한 사람이 움직이는 것을 열 장 보내면 그게 열 개다.
    카톡의 움직이는 이모티콘도 몇 번 돌고 멈춘다.
  - 240fps짜리 원본을 **두 프레임에 한 장씩**(12fps) 뽑는다. 24fps 그대로
    담으면 파일이 두 배인데 눈에는 거의 같다.
- **배경은 반드시 투명이어야 한다.** 이 규칙 하나 때문에 있던 것을 다 지웠다
  (사용자 요청). 대화 바탕이 보라가 되면서 흰 카드나 옅은 모눈 자국이
  **네모난 자국으로 통째로 도드라졌다.** 영상·시트에서 잘라 온 것들이라
  배경이 붙어 있었고, `rembg`와 `cv2`로 벗겨 보았지만 그림엽서 꼴이나
  글자와 배경이 같은 색인 것(`잘 자!`)은 끝내 깨끗해지지 않았다.
  **처음부터 투명한 그림으로 만들어 넣는 것이 답이다** — 벗기는 도구는
  저장소에 두지 않았고, 다시 짜야 할 이유도 없다.
  - 이 그림체는 무엇이든 **검은 윤곽선으로 닫혀 있다.** 그림 안의 흰 면
    (고양이 배·골프공 몸통)까지 뚫리면 잘못 벗긴 것이다 — 흰 판과 흰
    골프공을 색으로 가르려다 공 몸통을 통째로 날린 적이 있다.
  - 팔레트를 48~64색으로 줄이면 눈에는 그대로인데 파일이 1/3이 된다.
    **줄인 뒤 테두리에 잡티가 없는지 볼 것** — 한 점만 남아도 흰 바탕
    위에서 네모난 자국으로 보인다(앱 아이콘에서 겪은 것과 같다).
  - **움직이는 것은 무한 반복이 아니라 세 번만 돌게 만든다**(`loop=3`).
    늘 켜져 있는 그리기 비용이 안드로이드에서 화면을 끊기게 한다는 규칙이
    여기에도 걸린다 — 한 사람이 열 장 보내면 그게 열 개다. 12fps면 충분하고
    (24fps는 파일만 두 배다), **APNG로 하지 말 것**(한 장이 1MB였다.
    움직이는 webp는 85KB다).
- **거르는 잣대는 배경이 투명한가 하나뿐이다**(사용자 요청).
  **주는 것을 임의로 빼지 말 것** — 사용자가 먼저 하나씩 보고 고른 것들이라,
  이쪽에서 다시 골라내면 검수한 뜻이 없어진다.
  - **글자가 한글이 아니어도 그대로 넣는다.** 예전에 그림을 만드는 쪽이
    한글을 헛그린 것(`Belfle tesppick`)을 빼던 규칙이 있었는데
    **사용자가 지웠다.** 영어든 무슨 글자든 상관없다.
  - **눈에 걸리는 것이 있으면 빼지 말고 알릴 것.** 배경이 안 지워졌거나
    남의 상표(`Titleist` 같은 것)가 보이면 **넣기 전에 한 줄로 말하고
    사용자가 정하게 한다** — 상표는 스토어 반려 사유라 알릴 값이 있지만,
    뺄지 말지는 사용자 몫이다.
  - **사과 상표는 두 번 나왔고 두 번 다 지웠다** — 노트북 앞에 앉은 펭귄
    (`pnwork`)과 고양이(`ctwork`). 사용자가 `로고만 지워줘`로 정한 방식이라
    **다음에도 그림은 살리고 상표만 지운다.** `cv2.INPAINT_TELEA`로 메운다
    (밝은 획만 골라 3×3으로 두 번 부풀린 자리) — 뚜껑의 그라디언트가
    그대로 남아 지운 자국이 안 보인다.
  - 배경이 투명하지 않은 것만 이쪽에서 돌려보낸다. 대화 바탕이 보라라
    흰 네모가 통째로 도드라진다 — 그게 있던 것을 다 지운 이유다.

승인은 남의 기기에서 일어나므로 `lib/auth.tsx`가 내 `profiles` 행을 따로 구독한다 —
대기 화면에 앉아 있던 사람이 새로고침 없이 들어오는 게 이것 때문이다.

### 알림은 Edge Function 하나가 보낸다

앱에는 서버가 없으므로 **미는 일은 `supabase/functions/notify` 한 곳에서만**
한다. DB 웹훅이 새 행을 그 함수로 보내고, 함수가 `push_subscriptions`의
기기들로 밀어 준다. 누구에게 갈지는 `planFor()` 하나에 모여 있다.

- **받는 곳은 사람이 아니라 기기다.** 한 사람이 폰과 PC로 켜면 두 줄이 된다.
- **대화만 따로 끌 수 있다** (`push_subscriptions.chat`, 기본 켜짐).
  모집·공지·투표는 하루 몇 번이지만 대화는 종일 울려서, 그것 때문에 알림을
  통째로 꺼 버리면 정작 라운드 소식을 놓친다. 이것도 **사람이 아니라 기기**
  단위다 — 폰에서만 끄고 PC로는 받을 수 있다.
  발송기는 `Note.channel === 'chat'`일 때만 `chat = true`인 기기를 고른다.
  갈래를 더 나눌 일이 생기면 칸과 `channel`을 함께 늘린다.
  **읽는 쪽은 켜짐으로 기운다**(`chatPush()`) — 못 받는 쪽으로 틀리면
  사람이 알아채지 못한 채 소식이 끊긴다.
  `enablePush()`의 upsert에 `chat`을 안 넣는 것도 일부러다. upsert는 보낸
  칸만 고치므로, 껐다 켜도 대화만 꺼 둔 것이 그대로 살아남는다.
- 못 쓰게 된 구독(404·410)은 발송할 때 지운다. 안 그러면 같은 실패가 쌓인다.
- 끌 때는 `disablePush()`를 쓴다 — 행만 지우면 브라우저 구독이 남아
  켜진 것처럼 보이는데 발송 목록에는 없는 상태가 된다.
- **iOS는 홈 화면에 추가한 앱에서만 된다.** 사파리 탭에서는 권한 요청이
  아예 안 뜨므로 `pushState()`가 그것부터 보고 안내 문구를 돌려준다.
- **`pushState()`는 절대 던지지도, 멈춰 서지도 않는다.** 화면(`Me.tsx`)은
  답이 오기 전까지 `확인 중…`을 적어 두는데, 그 약속이 던져지거나 답을
  안 주면 **그 자리에 영영 멈춘다** — 알림을 켤 길이 통째로 사라지고
  무엇이 막힌 것인지도 알 수 없다(사용자 제보 — `이 기기로 받기
  확인중.....`). 그래서 try/catch와 **6초 시간 제한**을 함께 두르고,
  알 수 없을 때는 **`꺼짐`으로 답한다** — `못 함`이 아니라. 켜기 단추가
  살아 있어야 눌러 볼 수 있고, 그러면 `enablePush()`가 **진짜 까닭을
  토스트로** 보여 준다. 알 수 없다고 길을 막으면 실마리까지 없어진다.
  - **던지던 자리는 `앱에 플러그인이 안 실린 판`이었다.** `import()`는
    웹 묶음에서 오므로 **늘 성공하는데**, 그 판으로 만든 앱에는 네이티브
    쪽이 없어 `checkPermissions()`가 `not implemented`로 거절한다.
    `notThere()`(native-push.ts)가 그것만 골라 **`unsupported`**로
    돌려주고, 화면은 앱일 때 그것을 `앱을 최신 판으로 받으면 켤 수
    있습니다`로 적는다 — 실제로 그것이 맞는 안내다.
    `askToken()`의 거절도 갈 데가 없어 **15초를 꼬박 기다리던 것**을
    함께 고쳤다.
  - `.dev/behave.mjs`의 `알림 칸이 확인 중…에 안 멈춘다` 칸이 붙들어
    둔다 — `navigator.serviceWorker`를 **던지는 것**과 **답 없는 것**으로
    갈아 끼워 둘 다 본다(보호 줄을 빼면 실제로 둘이 빨갛게 뜬다).
- **알림 플러그인은 `import()`(동적)로 부르지 말 것 — 실기기에서 그 약속이
  안 끝났다.** 사용자 제보(`플러그인 찾는중에서 안넘어가`)로 잡은 자리다.
  걸음이 `1 플러그인 찾는 중`에 멈춰 있고 `1 플러그인 없음`도 안 뜬다는
  것은 **거절도 아니고 답이 아예 안 온다**는 뜻이다 — 앱 안에서는 문서가
  `capacitor://localhost`라 조각(chunk)을 받아 오는 길이 웹과 다르고,
  거기서 막히면 그대로 굳는다.
  지금 `native-push.ts`는 **static import**다. 그 꾸러미의 알맹이는
  `registerPlugin(...)` 한 줄뿐이고(웹용 구현은 그 안에서 따로 늦게
  불러온다) `registerPlugin`은 `lib/composer.ts` 때문에 어차피 묶음에
  들어 있어 **묶음이 0.03KB 늘었다.** 플러그인이 안 실린 옛 앱은 예나
  지금이나 **2번 걸음에서** `not implemented`로 갈린다(`notThere()`).
  **`lib/native.ts`의 `@capacitor/browser`·`@capacitor/app`은 그대로
  둔다** — 그쪽은 실제로 잘 돌고 있다(카카오 로그인이 오간다).
- **앱을 부르는 자리에는 시간 제한을 둘 것**(`soon()`). 플러그인 호출은
  앱 쪽으로 한 번 건너갔다 오는 것이라, 저쪽이 답을 안 주면 그 자리에서
  굳어 화면이 `켜는 중… 2 권한 보는 중`에 멈춘다. 권한 창을 띄우는
  `requestPermissions()`만 60초다 — 거기는 **사람이 창을 보고 누르는
  시간**이라 5초로 자르면 멀쩡한 판을 실패로 만든다.
- **켜는 동안 어느 걸음인지 화면에 적는다**(`pushStep`·`watchPushStep`).
  알림을 켜는 일은 **폰에서만 도는 네 걸음**이라(플러그인 찾기 · 권한 묻기 ·
  애플/구글에서 토큰 받기 · 서버에 남기기) 어디서 막혔는지 밖에서는 알 길이
  없다 — `안 켜져요`만 듣고 짐작하면 한 바퀴를 헛돈다(`ncStatus()`와 같은
  자리다). 도는 동안 `켜는 중… 3 토큰 받는 중`처럼 적고, 실패하면
  **그 걸음과 까닭을 줄에 그대로 남긴다**(`.switch-desc.err`).
  - **토스트로만 알리지 말 것** — 몇 초 뒤 사라져 **사진으로 찍어 보낼 수가
    없다.** 폰에서만 갈리는 자리는 값을 화면에 남겨 두는 편이 결국 빠르다.
  - 도는 동안 아무 말이 없으면 **안 눌린 줄 알고 또 누른다** — 토큰을
    기다리는 15초가 특히 그렇다.
  - 웹도 같은 걸음을 적는다(`enablePush`) — 화면은 앱인지 웹인지 몰라도
    되게 `lib/push.ts`가 그대로 내보낸다.
- **밀어 준 알림은 `notifications` 표에도 남는다 — 그것이 알림함(🔔)이다.**
  사용자 제보 — 아이콘에 `12`가 떠 있어도 **그중 둘이 정산인지 자리가 난
  것인지 앱 안에서 알 길이 없었다**(`앱에서는 알수있는 방법이 없어`).
  미는 것으로 끝내면 배너를 놓쳤을 때 되짚을 데가 아예 없다.
  화면은 `screens/Alerts.tsx`, 받는 곳은 `lib/alerts.ts`, 문은 홈 머리말의 🔔이다.
  - **대화는 안 넣는다.** 하루 100마디가 그대로 쌓이는 데다 **대화방이 곧
    목록**이라 여기 또 적을 것이 없다. 그래서 **종의 숫자와 대화 안 읽은
    수는 갈린다** — 폰 아이콘의 숫자만 둘을 더한 값이다.
  - **기기가 아니라 사람에게 남긴다.** 발송기가 미는 목록은 알림을 켜 둔
    **기기**인데, 그걸로 넣으면 **알림을 안 켠 사람은 앱을 열어도 못 본다** —
    알림함은 켜고 끄고와 상관없이 있어야 하는 자리다.
  - **넣는 정책이 아예 없다** — 발송기(service_role)만 넣는다. 회원에게
    열면 남에게 가짜 알림을 꽂을 수 있다(`signups`에 직접 insert를 안 여는
    것과 같은 잣대다). 회원은 제 것을 보고·읽음 찍고·지우기만 한다.
  - **여는 순간 다 읽음으로 본다.** 줄마다 눌러 읽게 하면 아이콘의 뱃지를
    지울 길이 없어진다 — 종을 눌렀다는 것이 곧 '무엇이 왔나 보자'다.
    **읽음은 화면당 한 번만 찍는다**(`once`) — 실시간 이벤트로 다시 그려질
    때마다 찍으면 그 쓰기가 다시 이벤트가 되어 되돌아온다.
  - **읽은 것과 안 읽은 것을 색으로 가른다**(사용자 요청 — `내가 확인한건
    색상을 다르게해서 확인한건지 안한건지 알수있게해줘`). 정산 알림 둘이
    나란히 서면 어느 것이 새로 온 것인지 알 길이 없었다.
    안 읽은 줄은 **흰 카드 + 왼쪽 빨간 띠 + `N` 표**, 읽은 줄은
    **`--surface-2` 바탕에 흐린 제목**이다. **라운드·투표의 `모집중`/`마감`과
    같은 방식이다** — 테두리나 투명도만 만지면 훑을 때 여전히 다 같은 흰
    카드로 보인다. 빨강인 것은 이 앱에서 빨강이 '내가 안 본 것'이기
    때문이다(**분홍을 쓰지 말 것** — 그건 '지금 눌러야 할 것' 자리다).
  - **무엇이 안 읽은 것인지를 `read_at`으로 정하지 말 것.** 바로 위 규칙대로
    **여는 순간 다 읽음으로 찍으므로**, 그 칸을 보고 그리면 들어가는 순간
    **넷이 다 '읽음'이 된다.** 화면이 `fresh`(id 집합)에 얼려 둔다 —
    대화의 `lastSeen`을 첫 렌더에서 한 번만 집는 것과 같은 결이다.
    담기는 길이 **둘이고 둘 다 필요하다**: ① 그릴 때 `read_at`이 빈 줄을
    보면 담고 ② `markAlertsRead()`가 **방금 찍은 id를 돌려주므로** 그것도
    담는다(목록이 읽음 찍기보다 늦게 닿는 판을 메운다).
    **id마다 한 번만 판단한다**(`judged`) — 실시간 이벤트로 다시 그릴 때마다
    보면 읽음으로 바뀐 줄이 그때 뒤집혀 색이 되돌아간다.
  - **`.dev/rest.mjs`의 흉내는 고치기를 받아도 고정 자료를 안 건드린다.**
    그래서 알림함 칸에서는 **PATCH를 직접 받아 진짜로 찍어 준다** — 안
    그러면 다시 받아 온 줄이 여전히 `read_at`이 빈 채로 와서 **`read_at`으로
    그리는 옛 코드도 초록으로 뜬다**(초록을 위한 검사가 되어 버린다).
    고쳐 놓고 되돌려 **실제로 셋이 빨갛게 뜨는 것을 확인했다.**
  - **제목의 그림글자를 두 번 그리지 말 것**(`splitAlertTitle`). 알림 제목에는
    이미 붙어 있어(`💰 정산`) 아이콘을 따로 그리면 `💰 💰 정산`이 된다 —
    헤드리스로 찍어 보고 잡았다. 제목이 들고 있는 그것을 아이콘 자리로 옮기고,
    없으면 갈래로 고른다(`ALERT_ICON`, 모르면 `🔔`).
  - `kind`는 발송기가 넣는 **표 이름 그대로**다(`rounds`·`settlement_shares`…).
    갈래를 새로 만들 때 적을 것이 없게 한 것이고, 화면은 모르는 값도 그린다.
  - **90일이 지나면 알림함을 연 사람의 화면이 지운다**(`purge_my_notifications`).
    대화 사진 청소와 같은 결이다 — 정해진 시각에 도는 것(pg_cron)을 안 켠다.
  - **`useRealtime`만으로는 종에 숫자가 안 붙는다 — `useRefreshOnShow`와
    한 쌍이다**(`lib/db.ts`). 앱을 접으면 실시간 연결이 끊기고 **그동안
    들어온 것은 다시 이어져도 되받아 오지 않는데**, 폰 알림을 눌러 들어오는
    길이 대개 **껐다 켜는 게 아니라 접어 둔 것을 펴는 것**이라 화면도 새로
    안 만들어진다 — 조회가 한 번도 다시 안 돌아 **알림은 왔는데 🔔이 0**이었다
    (사용자 제보 — `알림 후 앱에 들어가서보면 앱 종에 뱃지알림이 안생겨`).
    줄은 멀쩡히 들어와 있었고 화면만 모르고 있었다.
    **탭바의 빨간 숫자는 같은 것을 이미 하고 있어 멀쩡했다** — 그래서 한
    곳으로 모았다(탭바·홈·알림함이 같이 쓴다).
    **실시간으로 받는 화면을 새로 만들면 이것도 함께 걸 것.**
  - `.dev/behave.mjs`의 `알림함 (🔔)` 칸이 붙들어 둔다(종의 숫자 · 가이드가
    옮겨 간 것 · **접었다 펴면 그 사이 온 것이 붙는가** · 읽음이 한 번만
    나가는 것 · 줄을 누르면 그 건으로 가는 것). 거기서는 실시간이 아예
    막혀 있어(`stubOutside`) 폰에서 접어 둔 것과 같은 상태가 된다 —
    `useRefreshOnShow`를 빼면 실제로 빨갛게 뜬다.
  - **이 화면은 새 앱 판이 있어야 폰에 닿는다.** 만들면서 `앱은 다시 안
    만들어도 된다 — 화면도 숫자도 웹에서 온다`고 적었는데 **틀린 말이다**
    (사용자가 홈 화면 사진을 보내 줘서 잡혔다 — 종은 없고 `📖 앱 가이드`가
    그대로였다). 서버에서 오는 것은 **뱃지 숫자와 알림함 줄**뿐이고,
    **화면은 `dist`에 담겨 앱 안에 들어간다**(위 '앱 (Capacitor)' 꼭지).
    **화면을 건드리는 고침에는 언제나 새 판이 필요하다** — 웹만 밀어 놓고
    `됐습니다`라고 하지 말 것.
- **아이콘 위의 빨간 숫자(뱃지)는 서비스워커가 센다**(`sw.js`의 `bumpBadge`).
  **`registration.getNotifications()`로 세면 안 된다** — 대화 알림은 같은
  `tag`로 묶여 여섯 줄이 와도 알림창에는 하나뿐이라 늘 1이 된다. 그래서
  IndexedDB에 따로 담는다. 화면 쪽(`lib/badge.ts`)은 "봤다"고 알려 주기만
  한다 — **세는 곳을 하나로 두려는 것이다.**
  보고 있는 창이 있으면(`isWatching`) 올리지 않고 0으로 맞춘다. 그 자리에서
  읽는 것이라 숫자가 붙었다 바로 지워지는 깜빡임만 남고, 겸사겸사 앱이 미처
  못 지운 옛 숫자도 거기서 바로잡힌다.
  **홈 화면에 추가한 앱에서만 보이고, 안드로이드는 런처마다 다르다** —
  삼성은 숫자, 순정은 점만. 안 되는 기기에서도 알림은 그대로 떠야 하므로
  전부 조용히 지나간다.
  **그래서 안 뜰 때 어디가 막힌 것인지 밖에서는 알 길이 없었다.** 한동안
  `내 정보` 맨 아래에 `뱃지 화면 O · 워커 O`로 적어 두었는데(`badgeSupport()`),
  뱃지가 확인된 뒤 사용자 요청으로 걷어냈다. **그 자리는 이제
  `앱제작: 악마제리` · `버전 1.0` 두 줄이다**(사용자가 정한 문구다).
  함수는 남겨 두었으니 다시 볼 일이 생기면 그 줄만 도로 붙이면 된다.
  **`워커` 쪽이 진짜다** — 숫자를 얹는 건 알림이 올 때 도는 서비스워커라,
  화면에서 된다고 되는 게 아니다.
- **알림음은 웹에서 못 정한다.** `Notification`의 `sound`는 어느 브라우저도
  구현하지 않았고 서비스워커는 오디오를 못 재생한다 — **알림으로 오는 소리는
  폰의 기본음이고, 웹에서 바꿀 길이 아예 없다.**
  **앱을 보고 있는 동안에만 `까꿍` 소리를 낸다**(`lib/sound.ts` +
  `TabBar`의 `useDing`). 대화 실시간 이벤트를 듣다가 남의 글이 오면 울린다 —
  내 글·안내 줄(`system`)·대화 화면에 있을 때·창이 숨어 있을 때는 안 울리고,
  1.5초 안에 두 번은 안 울린다(쉰 명이 떠들면 소리가 겹친다).
  파일은 `public/kkakkung.wav`다.
  **iOS는 소리를 잠가 둔다 — 첫 손짓에서 풀어야 한다.** 아이폰 사파리는
  *사람이 누른 순간에 한 번이라도 울려 본 적 없는* `Audio`를 나중에 코드로
  재생하지 못하게 막는다. 화면 어딘가를 눌렀는지가 아니라 **그 요소가
  손짓 안에서 울려 봤는지**를 본다. 그래서 알림이 올 때 처음 `play()`를
  부르면 언제나 거절당했고, 그 실패를 조용히 넘기고 있어서 **아무 일도
  안 일어난 것처럼 보였다.** 지금은 모듈을 불러오는 순간부터 창에 귀를
  대고 있다가 **첫 손짓에서 소리를 끈 채 한 번 재생해 잠금을 푼다**(`unlock`).
  **`pointerdown`은 `click`보다 먼저 온다.** 그래서 `소리 시험`을 누르면
  잠금 풀기가 **먼저** 돌고 단추의 진짜 재생이 뒤따르는데, 잠금 풀기의
  `.then`이 무심코 `pause()`를 부르면 **방금 난 소리를 스스로 끈다.**
  실제로 그렇게 나서, `tried`(동기 표시)를 보고 그때는 안 멈추게 했다.
  `testDing()`이 `unlock()`을 따로 안 부르는 것도 같은 이유다 — 단추를
  누른 것이 이미 손짓이라 그 재생 자체로 풀린다.
  **`unlock`에서는 음소거를 절대 되돌리지 않는다.** 예전에는 `pause()` 뒤에
  풀었는데, **앱에서 처음 켜고 대화를 누르면 `까꿍`의 뒷부분(`꿍`)이 실제로
  났다**(사용자 제보). iOS는 `play()` 바로 뒤에 부른 `pause()`를 **아직
  재생이 시작되지 않았다고 보고 무시할 때가 있다** — 소리는 계속 흐르는데
  다음 줄에서 음소거가 풀려 중간부터 들린 것이다(들린 것이 앞이 아니라
  뒤였던 것이 그 증거다). 지금은 **`playDing()`만 소리를 되살리고**
  `unlock`은 `muted`와 `volume`을 둘 다 걸어 두기만 한다 —
  `pause()`가 무시돼도 끝까지 안 들린다. **되돌리는 줄을 다시 넣지 말 것.**
  **그래도 무음 스위치는 못 이긴다.** 폰이 무음이면 사파리 소리는 통째로
  안 난다 — 웹에서 넘을 수 있는 벽이 아니다. 이것도 앱으로 감싸면 없어진다.
  (`내 정보`에 `소리 시험` 단추를 뒀다가 **사용자 요청으로 걷어냈다.**
  잠금 푸는 일은 첫 손짓에서 저절로 도니 단추가 있어야 되는 것이 아니었다.)
  **앱을 보고 있을 때는 폰 소리를 죽인다**(`sw.js`의 `options.silent = seen`).
  안 그러면 소리가 **두 번** 난다 — 폰의 기본 알림음과 우리 `까꿍`이 같이
  울린다(아이폰 홈 화면 앱에서 실제로 그랬다). 보고 있을 때 들려야 할 것은
  `까꿍`이므로 그쪽을 남긴다. **알림 자체는 그대로 띄운다** — 웹푸시는 밀어
  준 건마다 눈에 보이는 알림을 하나 띄우기로 되어 있어, 아예 안 띄우면
  브라우저가 '백그라운드에서 갱신됨' 같은 제 문구를 대신 띄운다.
  **`silent`는 안드로이드에서만 먹는다 — 실기기로 확인했다.**
  안드로이드는 지켜 줘서 `까꿍`만 나고, **아이폰(홈 화면 앱)은 무시해서
  여전히 두 번 난다.** 웹에서 더 팔 자리가 아니다 — 앱으로 감쌀 때
  없어진다(네이티브는 앱이 떠 있을 때 알림을 어떻게 보여 줄지 앱이 정한다).
  `docs/출시-전-할일.md` 1-1번에 적어 두었다.
  **알림으로 오는 소리까지 바꾸는 것은 앱으로 감쌀 때 한다**
  (`docs/출시-전-할일.md` 1번).
- **알림창 맨 윗줄은 폰이 붙인다.** 홈 화면 앱이면 manifest의 `name`,
  브라우저 탭이면 사이트 주소다 — 주소는 브라우저가 출처를 밝히는 것이라
  끌 수 없고, 설치하면 이름으로 바뀐다. 그래서 사람 눈에 보이는 이름은
  전부 `까꿍`으로 맞춰 두었다: manifest의 `name`/`short_name`,
  `apple-mobile-web-app-title`, `<title>`, `sw.js`의 예비 제목, 로그인 화면,
  `내 정보` 아래 표기. 저장소 이름만 `teetime`으로 남아 있다.
  **앱스토어 목록 이름(`까꿍데이`)만 다르다 — 여기에 맞추지 말 것**
  (위 '프로젝트 개요' 참고. 사용자가 정한 것이다).
  **`teetime:seen:` 같은 저장 키와 로그 꼬리표는 소문자 그대로 둘 것** —
  바꾸면 안 읽음 표시가 초기화된다.
  **iOS는 이 이름을 홈 화면에 추가할 때 한 번만 읽는다** — 바꿨으면 아이콘을
  지우고 다시 추가해야 알림창에 반영된다.
- **알림 제목 첫머리에 종류를 세운다**(`💬 대화` · `⛳ 새 모집` · `🗳 새 투표` ·
  `📢 새 공지` · `🙋 가입 신청` · `🙋 대기 신청` · `💰 정산` · `🎉 자리가 났습니다` ·
  `🚩 조 편성` · `💰 입금 부탁드립니다` · `⛳ 내일 라운드` ·
  `🎯 2시간 뒤 스크린` · `⛳ 라운드 공유` · `🎯 스크린 공유`).
  알림창에는 제목 한 줄만 보이는 때가 많아, 이름만
  있으면 무엇인지 열어 봐야 안다.
  (`🎯 내일 스크린`도 코드에 남아 있는데, **줄을 넣은 뒤에 종류를 스크린으로
  바꾼 라운드** 하나 때문이다 — 제목을 그 라운드의 지금 종류에서 뽑으므로
  그때도 말이 맞는다. 평소에는 안 나온다.)
- **본문은 기기마다 다를 수 있다**(`Note.bodyBy`). 라운드 알림이 사람마다
  자기 조와 조 시각을 실어 보내기 때문이다 — 나머지는 지금까지처럼 다 같다.
  (정산은 몫 행 하나가 사람 하나라 이게 필요 없었다.)
- **`tag`는 '서로를 밀어내도 되는가'로 정한다.** 알림창에서 같은 `tag`는
  뒤엣것이 앞엣것을 덮는다. 그래서 입금 독촉은 정산 알림과 **같은** `settle-`
  tag를 쓰고(같은 건이라 두 줄이 쌓일 이유가 없다), 대기 승격은 모집 알림과
  **다른** `signup-` tag를 쓴다(그 모집과 별개로 읽어야 할 소식이다).
- **다시 열린 것은 웹훅이 `after update`다**(`🗳 투표 다시 열림` ·
  `⛳ 모집 다시 열림` · `🎯 스크린 다시 열림`). 새로 올린 것과 **말이 갈려야
  한다** — `새 투표`라고 오면 새것이 생긴 줄 알고 들어갔다가 아까 그것을 본다.
  **무엇을 알릴지 고르는 곳이 발송기가 아니라 트리거의 `when` 절이다**
  (`docs/설치.md` 3번): 마감했다 푼 것만 함수를 부르고, 제목을 고치거나
  마감할 때는 아예 안 부른다. 그래서 발송기는 `UPDATE`가 오면 그게 곧
  '다시 열림'이라고 본다 — `old_record`를 안 봐도 되고, 실제로 `notify_push()`가
  그 칸을 null로 보내기도 한다. **한쪽만 고치면 엉뚱한 때 알림이 간다.**
  **다시 열 때는 아무도 안 뺀다.** 웹훅이 주는 것은 바뀐 행뿐이라 누가
  눌렀는지 알 길이 없다 — `created_by`로 빼면 남이 다시 열었을 때 **정작
  만든 사람만 소식을 못 듣는다.** 누른 사람에게 한 번 더 울리는 쪽이 낫다.
- **알림을 누르면 그 건으로 간다.** `note.url`이 갈 곳이고, **목록이 아니라
  그 건이어야 한다** — 정산은 `#/rounds/{round_id}`(라운드 안에 있다),
  투표는 `#/polls/{id}`다. 목록으로 보내면 어느 건이었는지 또 찾아야 한다.
  가는 길이 **셋**인데, 기기마다 통하는 게 달라 셋 다 둔다:
  1. 앱이 열려 있으면 `client.navigate()` — **iOS는 이걸 지원하지 않기도 한다.**
  2. 그때를 위해 `postMessage({type:'navigate'})`도 같이 보낸다
     (`lib/push.ts`가 받아 해시를 옮긴다).
  3. **앱이 꺼져 있으면 `openWindow`에 주소를 주는데, 아이폰은 그걸 무시하고
     첫 화면으로 띄우기도 한다.** 그래서 `sw.js`가 갈 곳을 IndexedDB에 적어
     두고(`putNav`), 앱이 켜질 때 `take-nav`로 가져가 옮긴다(`takePendingNav`).
     **가져가면 지운다** — 안 지우면 다음에 그냥 켤 때도 옛 알림 화면으로 끌려간다.
  **남의 탭은 건드리지 않는다** — `client.url`이 스코프로 시작하는 창만 쓴다.
  - **켤 때 한 번만 묻던 것이 이 셋을 다 헛되게 하고 있었다**(사용자 요청 —
    `알림을 누르면 해당화면으로 이동할수있게`). 아이폰 홈 화면 앱은 알림을
    눌러도 대개 **껐다 켜는 게 아니라 접어 둔 것을 도로 펴 준다** — 그때는
    `takePendingNav()`가 다시 돌 일이 없고, 1번은 iOS가 지원하지 않기도 하고,
    2번은 잠들어 있던 화면이 놓칠 수 있다. 지금은
    **화면으로 돌아올 때마다 다시 묻는다**(`visibilitychange`, 그리고
    되살아난 `pageshow`).
  - **그래서 적어 둘 때 시각도 함께 적는다**(`putNav`의 `at` ·
    `NAV_FRESH` 2분). 돌아올 때마다 묻게 되면서, 지난주에 적히고 안 지워진
    값이 오늘 화면을 엉뚱한 데로 끌고 갈 자리가 생겼다. 시각이 없는 값은
    **예전 판이 적은 것**이라 그대로 따라간다(켤 때 한 번만 묻던 때의
    것이라 오래됐을 리가 없다).
  - **`pageshow`를 그냥 걸지 말 것** — 처음 열 때도 불려 켜자마자 두 번
    묻는다. `e.persisted`인 것만 본다.
  - `.dev/behave.mjs`의 `알림을 누르면 그 화면으로 간다` 칸이 붙들어 둔다 —
    헤드리스에는 서비스워커도 알림도 없지만 **주고받는 말은 흉내 낼 수
    있다**(`navigator.serviceWorker`를 가짜로 갈아 끼우고 `take-nav`에
    답해 준다). 돌아올 때 다시 묻는 줄을 빼면 세 줄이 빨갛게 뜬다.
- **크롬의 설치 신호(`beforeinstallprompt`)는 앱이 뜨자마자 한 번 온다.**
  화면 하나에서 듣고 있으면 이미 지나간 뒤라 못 잡는다 — 실제로 `내 정보`
  에서만 듣다가 설치 버튼이 영영 안 나왔다. `lib/install`이 **모듈을 불러오는
  순간부터** 창에 귀를 대고 붙잡아 두고, 화면들은 그걸 구독만 한다.
  `main.tsx`에서 가장 먼저 불러오는 이유가 그것이다.
- **서비스워커는 앱을 열 때 바로 등록한다**(`registerServiceWorker`).
  알림을 켤 때 등록해도 알림은 되지만, 안드로이드 크롬이 '앱 설치'를
  띄우려면 처음부터 등록돼 있고 `fetch` 핸들러가 있어야 한다. 그래서
  `sw.js`의 `fetch`는 캐시도 안 하면서 남아 있다 — 지우지 말 것.
- 공개키는 `src/lib/push.ts`에 그대로 있다(공개돼도 되는 값). **비밀키는
  Edge Function의 Secret에만 있다** — 저장소에 넣지 말 것.
- **함수의 `Verify JWT`는 꺼 둔다.** 켜 두면 트리거가 부를 때 관문이 401로
  막아 알림만 조용히 안 온다(실제로 여기서 막혔다). 문은 `x-notify-secret`이
  지킨다. 안 올 때는 `net._http_response`를 보면 이유가 바로 나온다 —
  진단 쿼리를 `docs/설치.md` 7번에 적어 두었다.

### 라우팅은 해시(`/#/rounds`)로 고정

바꾸지 말 것. 두 가지가 걸려 있다.

1. GitHub Pages 같은 정적 서버는 `/rounds`로 직접 들어오면 404를 낸다.
2. 나중에 **Capacitor로 감싸 스토어에 낼 때** 앱 안에서는 파일을 직접 띄우므로
   경로 라우팅이 깨진다. 해시는 그대로 동작한다.

같은 이유로 `vite.config.ts`의 `base: './'`(상대 경로)도 그대로 둔다.

### 스토어가 요구하는 웹페이지 둘 — `개인정보 처리방침`과 `지원`

`public/privacy.html` · `public/support.html` · `public/doc.css`.
GitHub Pages가 그대로 내주므로 주소가 곧
`…github.io/teetime/privacy.html`이고, **애플·구글 모두 이 주소가 없으면
등록 화면에서 막힌다**(`docs/출시-전-할일.md` 0-7번).

- **앱 화면과 따로 논다 — 일부러다.** 심사자가 스토어에서 링크로 여는
  페이지라 리액트도 로그인도 글꼴 CDN도 거치지 않고 **한 번에 떠야** 한다.
  그래서 색만 앱에서 가져오고 글꼴은 폰에 있는 것을 쓴다.
  **`index.html`에 끌어들이지 말 것** — 묶음만 커지고 얻는 것이 없다.
- **적힌 내용이 앱과 어긋나면 안 된다.** 받는 칸을 늘리거나 맡기는 곳
  (Supabase·카카오·Apple·FCM·Open-Meteo)이 바뀌면 **개인정보 처리방침도
  함께 고칠 것** — 스토어에 낸 약속이라 거짓이 되면 반려 사유이고,
  무엇보다 회원에게 거짓말이 된다.
- **지원 페이지의 버튼 이름은 실제 화면의 말이다** — `앱 사용자 가이드`
  (`screens/Help.tsx`)와 같은 규칙이라, 화면 문구를 바꾸면 여기도 고친다.
- 문의 이메일이 적혀 있다. **공개되는 페이지**라 바꾸려면 두 파일의
  그 줄만 고치면 된다.

### 앱 (Capacitor) — 이제 화면을 앱 안에 담는다 (출시용)

`capacitor.config.ts` · `ios/` · `.github/workflows/ios.yml` 셋이다.
절차와 사용자가 손으로 할 일은 **`docs/출시-전-할일.md` 0-3번**에 있다.

- **`server` 한 덩어리를 지웠다**(사용자 요청 — `앱 출시용으로 진행해줘`).
  그전까지는 그 자리에 GitHub Pages 주소가 있어 **화면을 웹에서 가져다
  띄우는 확인용 껍데기**였다. 이제 `npm run build`가 만든 `dist`가 앱 안에
  담긴다 — 이모티콘 그림(`public/stickers/`)까지 함께 들어가므로 앱에서는
  그림을 밖에서 안 받아 온다.
  - **고치는 속도가 그 대가다.** 껍데기이던 동안에는 웹만 밀면 폰에서 바로
    보였는데, 이제 앱을 쓰는 사람에게 닿으려면 **한 바퀴가 30분**이다
    (`빌드 → TestFlight → 애플이 처리 → 폰에서 갱신`). 화면을 여러 번
    고쳐 보며 다듬어야 하는 일이 생기면 **`server`를 잠시 도로 붙여** 그
    동안만 껍데기로 쓰면 된다 — 지운 자리에 그 한 줄을 주석으로 남겨 두었다.
  - **`vite.config.ts`의 `base: './'`가 이 자리를 위한 것이다.** 앱에서는
    문서가 `capacitor://localhost`에 있어 절대 경로(`/teetime/…`)로는
    아무것도 못 찾는다. 바꾸지 말 것.
    확인은 **`dist`를 루트로 띄워** 해 볼 수 있다(`python3 -m http.server`로
    `dist` 안에서 띄우고 열어 본다) — 앱과 같은 자리가 된다. 그렇게 재서
    로그인 화면이 뜨고 · 이모티콘이 200으로 풀리고 · 404도 오류도 0인 것을
    확인했다.
  - **웹(GitHub Pages)은 그대로 돈다** — 홈 화면에 추가해 쓰던 회원은 예전과
    똑같고, **알림도 거기서만 온다**(아래). 앱은 그 위에 하나 더 생기는 길이다.
- **`ios/`는 저장소에 있다.** 손으로 고쳐 둔 것이 셋이라 CI가 다시 만들면
  날아간다: `Info.plist`의 **URL 스킴**(`kkakkung`)과 **수출 규정 답**
  (`ITSAppUsesNonExemptEncryption`), 그리고 **공유 스킴 파일**
  (`xcshareddata/xcschemes/App.xcscheme`).
  - **공유 스킴이 없으면 CI가 `scheme not found`로 통째로 멈춘다** —
    Xcode가 만드는 기본 스킴은 `xcuserdata/`에 들어가는데 그건 무시 목록에
    있다. Capacitor 템플릿이 안 넣어 주므로 우리가 넣었다.
  - **앱 아이콘에 알파가 있으면 애플이 업로드를 거절한다.** `public/icon-512`을
    흰 바탕에 눌러 붙여 넣었다(첫 화면 그림도 같이). 아이콘을 바꾸면
    **여기까지 여섯 자리**가 된다(웹 넷 + 인라인 하나 + iOS).
- **아카이브는 서명 없이 만들고, 올릴 때 서명한다.** 워크플로에서 제일
  헤맨 자리이고, 까닭은 하나다 — **Xcode의 자동 서명은 아카이브할 때 늘
  개발용 프로비저닝을 받으러 가는데, 개발용은 기기를 미리 등록해 둬야
  만들어진다.** TestFlight로만 쓰는 우리 팀에는 등록된 기기가 없다.
  두 번 죽고 나서야 갈렸으니 되돌리지 말 것:
  1. 그냥 두면 `Your team has no devices from which to generate a
     provisioning profile`.
  2. 앱 타깃 Release에 `CODE_SIGN_IDENTITY = "Apple Distribution"`을
     못박으면 이번엔 `App has conflicting provisioning settings` —
     자동 서명이 고른 개발용과 어긋난다고 거절한다. **그래서 그 줄은
     지금 없다**(`project.pbxproj`에 다시 넣지 말 것).
  **앱스토어 배포용 프로비저닝은 기기가 필요 없다.** 그래서 아카이브는
  `CODE_SIGNING_ALLOWED=NO`로 서명을 아예 끄고, `-exportArchive`가
  `-allowProvisioningUpdates`로 인증서와 배포용 프로비저닝을 스스로 만들어
  받아 그때 서명한다. **앱에 entitlements 파일이 없어서**(푸시·특수 권한을
  안 쓴다) 서명을 미뤄도 잃는 것이 없다 — 나중에 푸시를 붙이면 이 전제가
  깨지므로 그때 다시 볼 것.
- **카카오 로그인만 갈래가 둘이다**(`signInWithKakao` in supabase.ts).
  앱에서는 웹뷰를 안 옮기고(`skipBrowserRedirect`) 주소만 받아
  `@capacitor/browser`로 연다 — **앱 안 웹뷰에서 들어오는 로그인을 카카오가
  막기 때문이다.** 돌아오는 것은 `lib/native.ts`가 받는다.
  **카카오 콘솔은 안 건드린다** — 카카오가 보는 것은 늘 Supabase의 콜백
  하나뿐이고, 커스텀 스킴은 그 뒤에서만 쓰인다.
- **`resize: 'native'` 한 줄이 아이폰 키보드 밀림의 답이었다 — 실기기에서
  확인했다**(사용자 확인 — `메시지입력창을 잡고 위로 올릴때 밀리는 증상은
  없어졌어`). 웹으로 넉 대를 헛짚었던 그 자리가 이 한 줄로 끝났다.
- **다만 앱에서는 키보드에 맞춰 화면을 움직이는 일을 우리가 맡아야 한다.**
  웹용 보정을 그대로 두었더니 **키보드가 먼저 뜨고 대화창이 뒤따라 뜨는**
  두 단계가 보였고, 그걸 끄고 웹뷰에 맡겼더니 **이번엔 웹뷰가 엇박자로
  움직였다**(사용자 제보 — `내려갈 때도 뭔가 엇박자야`).
  **원인은 우리 코드가 아니라 플러그인의 타이밍이다.** `Keyboard.m`에
  그대로 적혀 있다 — 열 때는 `키보드 애니메이션 시간 + 0.2초` 뒤에 창을
  줄이고, 닫을 때는 `0.01초` 만에 늘린다. 키보드는 0.25초에 걸쳐 움직이는데
  창은 0.45초 뒤에 툭 줄고, 내릴 때는 창이 먼저 늘어난다.
  지금은 앱이면 `html`에 **`native`** 클래스가 붙고(`lib/native.ts`의
  `IS_NATIVE`) 이렇게 갈린다:
  - `Chat.tsx`의 `applyKeyboard`는 **앱에서 통째로 물러난다.**
  - 대신 `앱: 키보드와 같은 박자로` 효과가 플러그인의
    `keyboardWillShow`/`Hide`를 듣는다. 그 신호는 **애니메이션이 시작하는
    그 순간** 오므로(늦는 것은 창을 줄이는 일뿐이다), **아직 창이 안 먹은
    만큼만 우리가 먹는다**: `메울 몫 = 키보드 높이 − (원래 높이 − 지금 창 높이)`.
    창이 줄어드는 순간 몫이 0이 되어 **높이가 한 픽셀도 안 바뀐다.**
  - 높이는 **한 값(`--chat-h`)으로만 몬다.** `100dvh`와 `--kb`를 함께 쓰면
    창이 줄어드는 그 프레임에 둘이 따로 바뀌어 한 번 튄다.
  - `Chat.css`의 `html.native .chat`이 **0.25초·iOS 곡선**으로 그 높이를
    편다. 그 시간과 곡선이 곧 키보드의 것이다 — **눈대중으로 바꾸지 말 것.**
  - **그런데 같은 시간·같은 곡선인데도 늦게 따라온다**(사용자 제보 —
    `키보드 나오고 내려갈때 채팅배경이 좀 늦게 따라와`). **시작이 늦어서다** —
    소식이 앱에서 웹으로 다리를 건너오느라 한두 프레임이 이미 지났는데,
    거기서 0.25초를 **다 쓰면** 그만큼 늦게 도착한다.
    그래서 3판 바가 **보낸 시각을 함께 실어 보낸다**(`kb` 신호의 `at`,
    1970년부터 ms). `Date.now()`와 견주면 늦은 만큼이 그대로 나오므로
    남은 시간만큼만 움직여 **키보드와 같이 끝낸다**(`Chat.tsx`의 `kbBeat` →
    `--chat-anim`). **0.25초라는 값은 그대로다** — 눈대중으로 줄인 것이 아니고,
    그 신호가 없는 판에서는 250ms 그대로 돈다.
    `kb`는 `keyboardWillShow`/`Hide`와 **같은 일을 하는 또 하나의 신호**다 —
    먼저 닿은 쪽이 하고 나중 것은 같은 값이라 그냥 지나간다.
  - **그래도 늦었다 — 그래서 4판은 곡선을 흉내 내지 않는다.** 진단 줄로 재니
    늦게 받은 것은 **9~10ms**뿐이었고, 대신 iOS가 알려 준 키보드 시간이
    **0.383초**였다(`움373`). **위의 `0.25초`는 이 폰에서 틀린 값이다** —
    그 값과 곡선을 맞춰도 실기기에서는 `아직 늦다`가 남았다(두 번 제보).
    바는 iOS가 키보드와 한 움직임으로 옮기므로 **바의 실제 자리가 곧 키보드
    자리**다. 4판 바가 `CADisplayLink`로 **그려지는 자리**(`layer.presentation()`)
    를 프레임마다 보내고(`frame` 신호: `bottom`·`h`·`p`·`end`), 웹은
    `kbFrame`이 그 값을 `--chat-h`·`--composer`에 그대로 적는다 —
    `--chat-anim`은 0으로 두고, 그동안 `html.kb-follow`로 입력칸 여백을
    `--composer` 하나로 몬다(홈 인디케이터 몫은 `p`로 섞어 넣는다).
    **따라가는 동안은 `flush`도 `height` 신호도 높이를 안 적는다** —
    목표값을 먼저 적으면 툭 뛴다. 끝(`end`)에 여느 셈(`paint`)으로 돌아간다.
    옛 앱(`frame`이 없는 판)은 위 셈이 그대로 돈다.
  - **바의 그려지는 높이는 쓰지 말 것 — 자리 하나에서 둘 다 셈한다**(5판의
    `chatH`·`pad`). 4판은 `bottom`(자리)과 `h`(높이)를 따로 보냈는데, 자리는
    iOS가 키보드와 함께 옮기고 높이(탭바 자리를 되붙이는 것)는 우리
    `UIView.animate`라 **곡선이 다르다** — 자리가 먼저 닿고 높이가 뒤늦게
    자라 목록이 34px 넘쳤다 돌아왔다(진단 `L652 → 618` · 사용자 제보 —
    `내려갔다가 다시 올라와`). 자리에서 비율 `p`를 내고 탭바 몫·홈
    인디케이터 몫을 그 비율로 섞으면 한 곡선을 탄다(`ComposerBar.tick`).
  - **6판부터 바가 두 값의 주인이다 — 웹은 다른 셈을 아예 안 쓴다.**
    5판까지는 **움직이는 동안만** 바가 적고 그 밖에는 웹이 셈했는데, 그
    경계에서 둘이 엇갈렸다: iOS가 키보드 알림을 여러 번 던져 따라가기가
    끊겼다 다시 시작하는데, 그 틈에 웹 값(바 높이 116)과 앱 값(여백 150)이
    섞여 **`--composer`가 116↔150을 오가며 목록이 618↔652로 흔들렸다**
    (실기기 진단 · 사용자 제보 — `내려갔다가 다시 올라와`).
    이제 바가 `layoutSubviews`에서도 `report()`로 알리므로 `frame` 신호가
    **늘** 오고, 웹은 `kb-follow`를 붙인 채로 그 값만 적는다.
    **`end`에서 웹 셈으로 돌아가지 말 것** — 그 자리가 곧 엇갈리는 자리다.
  - **`--chat-anim`을 끄는 것도 그 자리에서 할 것.** 켜 두면 앱이 준 값은
    곧바로 들어가는데 **화면만 CSS로 0.25초 더 끌려간다** — 그것이 마지막까지
    남은 `늦게 따라옴`이었다(진단 — `h874 b150`은 첫 줄부터 끝값인데 `L`만
    348→618로 235ms에 걸쳐 자랐다). 지금은 `kbFrame`이 바에게서 첫 값을
    받는 그 자리에서 끈다. 아래 '판 번호를 값으로 잡지 말 것'과 **같은 실수**로
    한 번 놓쳤다(`const follows = ncLog.v >= 4`).
  - **판 번호를 값으로 잡아 두지 말 것 — 쓸 때마다 볼 것**(`owns6()`).
    `ncLog.v`는 `composerReady()`가 앱에 물어봐야 채워지는데 그 답은 화면
    효과가 도는 **뒤에** 온다. `const owns = ncLog.v >= 6`으로 잡아 뒀더니
    늘 거짓이라 **6판 코드가 통째로 안 돌았다** — 앱은 6판인데 진단 줄은
    `틱70`이 돌면서도 `b116↔150`이 그대로였다(고친 줄 알고 한 판을 헛돌았다).
  - **`--composer`의 뜻이 판마다 다르다 — 섞으면 34px씩 흔들린다.**
    5판까지는 **바의 높이**(116)였고 CSS가 홈 인디케이터 몫을 더했는데,
    6판부터는 **바가 가리는 자리**(150, 그 몫까지 든 값)다. 그래서 6판에서는
    **그 값을 적는 곳이 `frame` 하나뿐이어야 한다** — 웹 글칸을 재는
    `ResizeObserver`가 `ncH`(바 높이)를 다시 적고 있었고, 그것이 초점이
    오갈 때마다 깨어나 목록을 흔들었다.
  - **`kb-follow`는 값을 적은 뒤에 붙일 것.** 그 표가 곧 '이 값은 가리는
    자리다'라는 뜻이라, 먼저 붙이면 34px 작은 옛 값으로 한 프레임이 그려진다.
  - **값을 다 적기 전에 배치를 읽지 말 것.** `kb-follow`를 붙인 뒤
    `--composer`를 적기 전에 `settleList()`가 끼어 있었는데, 그 함수가
    `scrollHeight`를 읽으며 **반쯤 적힌 값으로 한 프레임을 그렸다**
    (`b116`인데 `L652`). 적는 것을 다 끝내고 부를 것.
  - **4판부터 `--chat-anim`은 늘 0이다.** 움직임은 `frame`이 맡으므로 CSS가
    부드럽게 할 것이 없고, 들어올 때 여백(`--composer`)이 정해지는 것까지
    250ms에 걸쳐 옮기면 **글이 내려갔다 올라오는 것**으로 보인다. 같은 까닭으로
    지난번 바 높이(`lastBarH`)를 들어오자마자 먼저 적어 둔다.
  - `.chat`·`.chat-input`을 화면에 붙이는 웹용 규칙과 목록 아래 여백은
    `html:not(.native)`로 걸려 있다.
  - **줄어드는 동안 목록을 따라 앉힌다**(`settleList`를 여닫을 때 둘 다).
    빠뜨리면 목록이 330px쯤 짧아지는데 굴러간 자리는 그대로라 **맨 아래
    글이 잘려 안 보인다**(실제로 그렇게 나갔다 · 사진으로 제보받았다).
  - **초점이 갈 때 지난번 높이로 먼저 시작한다**(`kbHint`·`MEMO`).
    `keyboardWillShow`는 네이티브에서 한 번 건너오느라 한두 프레임 늦는데,
    0.25초짜리 움직임에서 그 30ms가 곧 `살짝 따로 노는` 느낌이다.
  - **탭바는 따로 여닫는다**(`kb-bar`). `kb-open`을 내리는 순간 탭바가 도로
    튀어나오는데 입력칸은 아직 내려오는 중이라 둘이 겹쳐 보인다.
    **그런데 이 늦추기는 1판 규칙이다 — 2판에서는 내릴 때 바로 내놓는다**
    (`keyboardWillHide`에서 `bar(false)`). 2판의 네이티브 바는 키보드가
    내려가기 시작하는 그 순간 탭바 자리(`tabH`)를 도로 걷어 가는데, 웹
    탭바는 `keyboardDidHide`까지 감춰져 있어 **그 사이가 빈 띠로 남았다가
    툭 나타난다**(실기기 제보 — `키보드 내릴때 탭바가 사라졌다가 나타나고`).
    1판은 바가 키보드와 함께 내려가므로 그대로 미룬다.
  **한쪽만 고치지 말 것** — 이 여섯이 한 벌이다.

**그래도 카톡만큼 부드럽지는 않다. 그 까닭을 알고 있을 것**(사용자 질문 —
`카카오톡은 엄청 부드럽게 되는데 이거는 왜 그렇게 못 만들어?`).

**카톡의 입력칸은 키보드에 붙어 있는 네이티브 뷰다**(`inputAccessoryView`).
iOS가 키보드와 그 칸을 **한 번의 움직임으로 함께** 옮기므로 맞출 것이
애초에 없다 — 둘이 한 몸이다.

우리는 셋이 따로 논다: **iOS의 키보드** · **플러그인이 줄이는 웹뷰** ·
**그 안에서 반응하는 웹 화면**. 게다가 화면은 키보드가 **움직이기 시작한
뒤에야** 그 사실을 전해 듣는다. 위의 여섯은 그 틈을 좁히는 것이지 없애는
것이 아니다 — **웹 화면인 한 완전히 같아질 수는 없다.**

**진짜로 같아지려면 입력칸만 네이티브로 만들어야 한다.** 그리고 그것이
**천지인 깜빡임의 답과 같다** — 남은 아이폰 문제 둘이 고칠 자리가 하나였다.
**그래서 만들었다**(아래 `글칸은 앱에서 네이티브다` 꼭지). 아래 여섯은
**플러그인이 없는 판**(웹, 그리고 아직 새 판을 안 깐 앱)에서 그대로 도므로
지우지 말 것 — 네이티브 바를 쓰는 판에서는 `applyKeyboard`처럼 저절로
물러난다.
- **웹은 한 줄도 안 바뀐다** — 플러그인은 `Capacitor.isNativePlatform()`일
  때만 불러오고, 묶음은 8.7KB만 늘었다.
- **앱에도 알림이 온다**(사용자 요청 — `FCM으로 옮겨서 앱에서도
  알림받을수있게해줘`). 앱 안에는 웹푸시도 서비스워커도 없어서 한동안
  **앱으로 옮겨 간 회원만 소식이 조용히 끊겨 있었다.** 지금은
  `lib/native-push.ts`가 따로 받는다 — 아래 '앱 알림' 꼭지를 볼 것.
  **다만 사용자가 열쇠를 넣어야 켜진다**(`docs/설치.md` 7-1·7-2번).

### 앱 알림 — 아이폰은 애플에 바로, 안드로이드는 구글을 거친다

**앱 안에는 웹푸시가 없다.** 서비스워커도 안 돌아서, 앱으로 옮겨 간 회원은
소식을 한 건도 못 받았다. 받는 쪽은 `src/lib/native-push.ts`, 보내는 쪽은
`supabase/functions/notify`의 갈래 둘이다.

- **받는 코드는 한 벌이다.** `@capacitor/push-notifications`가 아이폰에서는
  APNs 토큰을, 안드로이드에서는 FCM 토큰을 그대로 주므로 **표시만 갈린다**
  (`MARK` — `apns:` / `fcm:`).
- **아이폰에 FCM을 안 쓴다.** 그러려면 파이어베이스 프로젝트·앱 등록·
  `GoogleService-Info.plist`·APNs 키 올리기가 딸려 오는데, 애플에 바로
  보내면 **`.p8` 열쇠 하나**로 끝난다. 안드로이드는 구글 말고 길이 없어
  그쪽만 파이어베이스를 쓴다. **한쪽으로 통일하려 들지 말 것** — 얻는 것
  없이 아이폰에 짐만 는다.
- **표를 새로 안 만들었다.** 웹 구독이 쓰던 `push_subscriptions`에
  `endpoint`를 `apns:<토큰>`·`fcm:<토큰>`으로 넣는다(이모티콘이 사진 칸을
  `sticker:<id>`로 같이 쓰는 그 방식이다). 덕분에 **받는 사람을 고르는
  규칙**(`only`·`except`·대화 스위치)과 **죽은 구독을 걷는 자리**가 웹과
  한 코드로 남고, 붙여넣을 SQL도 없다. `p256dh`·`auth`는 웹 암호용이라
  앱에는 없어 빈 글자로 둔다(그 칸이 `not null`이다).
- **갈 곳(`note.url`)은 웹과 같은 값이다** — 앱은 `data.url`로 받는다.
  그래서 **발송기는 어디로 가는지 신경 쓸 필요가 없다.**
- **비밀값이 없으면 그 갈래만 건너뛴다**(행은 안 지운다). 아이폰 열쇠가
  없다고 웹·안드로이드 알림까지 막히면 안 된다.
- **표(JWT)를 다시 쓴다** — 애플은 20분에 한 번보다 자주 새로 만들면
  막고(`TooManyProviderTokenUpdates`), 구글 표는 한 시간짜리다. 둘 다
  45분 동안 모듈 값으로 들고 있는다.
- **토큰이 바뀌면 갈아 끼운다**(`refresh`). 앱을 지웠다 다시 깔거나 폰을
  새로 사면 다른 값이 온다 — 안 갈아 끼우면 옛 토큰이 죽은 것으로 걷혀
  **알림이 조용히 끊긴다.** 지웠다 넣지 않고 **고친다**(지우면 대화 알림만
  꺼 둔 것이 함께 날아간다).
- **앱을 보고 있을 때도 알림은 띄우되 소리는 안 낸다**
  (`presentationOptions: ['alert']`). 소리가 두 번 나는 것을
  막는 자리가 여기다 — 폰 기본음과 `까꿍`이 같이 울린다(웹에서 `silent`로
  막았지만 **아이폰이 그걸 무시하던** 그 자리다. `docs/출시-전-할일.md`
  1-1번). **`sound`를 넣지 말 것.**
  - **`badge`도 넣지 말 것.** 아이콘 위 숫자는 앱을 **안 보고 있을 때만**
    뜻이 있는데, 지우는 자리가 `applicationDidBecomeActive`(=앱을 열 때)
    하나뿐이라 보고 있는 동안 붙이면 **앱을 쓰는 내내 `1`이 남는다.**
  - **한동안 빈 배열(`[]`)이라 아예 안 띄웠는데 그것이 지나쳤다.**
    안 띄우면 **그 알림이 통째로 사라진다** — 알림창에도 안 남아 되짚을
    수도 없다. 앱을 열어 두고 다른 탭을 보고 있었으면 `💰 정산`·
    `🚩 조 편성`·`🎉 자리가 났습니다`를 그냥 놓친다. `까꿍` 소리는
    **대화에서만** 나므로(`useDing`이 대화 이벤트만 듣는다) 그 밖의
    알림은 소리도 배너도 없이 증발했다. **웹은 이럴 때도 띄운다**
    (`sw.js`가 `silent`만 걸고 알림 자체는 늘 띄운다) — 같은 앱인데
    갈릴 이유가 없다.
  - **알림이 안 온다는 제보를 쫓다 여기서 한 번 걸렸다.** 애플 열쇠를
    고친 뒤에도 **앱 안에서 시험하면 아무것도 안 보여서**, 고쳐 놓고도
    아직 안 된 줄 알았다. 앱 알림을 시험할 때는 **앱을 나간 상태**로
    받아 보거나, 이 값을 먼저 볼 것.
- **그 가운데 대화만 배너를 안 띄운다**(사용자 제보 — `화면을 보면서
  채팅을 하고 있는데 채팅이 올라오면 배너 알림이 계속 떠`). 대화방을
  열어 놓고 주고받는 내내 제 화면 위로 제 배너가 덮였다.
  자리는 `ios/App/App/MainViewController.swift`의 **`QuietChatPush`**다.
  - **`presentationOptions`로는 못 가른다** — 그건 앱 전체에 한 번 정하는
    값이라, 거기서 `alert`을 빼면 바로 위의 그 자리로 되돌아간다
    (정산·조 편성·자리가 났습니다가 통째로 사라진다). **알림 하나하나를
    보고 정할 수 있는 곳은 `willPresent` 하나뿐이다.**
  - **원래 것을 갈아 끼우지 말고 감쌀 것.** 그 답을 정하는 것은 Capacitor의
    `notificationRouter.pushNotificationHandler`인데, 우리 것으로 바꾸기만
    하면 `pushNotificationReceived`·`pushNotificationActionPerformed`가
    안 나가 **알림을 눌러도 그 화면으로 안 옮겨진다**(`lib/native-push.ts`).
    먼저 `inner.willPresent`를 부르고 **답만** 우리가 고른다.
  - **라우터가 그 자리를 약하게(weak) 들고 있다.** 씌운 껍데기를
    `MainViewController`가 붙잡아 두지 않으면 그 자리에서 사라져
    **아무 일도 안 한 것이 된다.**
  - **대화라는 것은 발송기가 실어 보낸다**(`chat` 한 칸). 예비로
    `thread-id`도 본다 — 앱은 새로 깔아야 바뀌고 발송기는 밀면 바로
    올라가 둘이 어긋나는 사이가 늘 있는데, 대화 알림의 `tag`는 예나
    지금이나 `chat` 하나다.
  - **탭을 가리지 않는다** — 다른 탭을 보는 중에 와도 안 띄운다. 그때는
    `까꿍` 소리와 **탭바의 빨간 숫자**가 이미 알려 주므로, 앱을 보고 있는
    사람에게 그 위에 배너까지 덮을 이유가 없다. **대화는 배너를 안 띄워도
    잃는 것이 없다** — 대화방이 곧 목록이고 알림함에도 원래 안 들어간다.
    정산·조 편성처럼 **되짚을 데가 알림뿐인 것**과 갈리는 자리다.
  - **안드로이드는 아직 안 막는다.** 거기서 배너를 띄우는 것은 플러그인
    안(`PushNotificationsPlugin.fireNotification`)이라 끼어들려면 FCM 서비스를
    통째로 갈아 끼워야 하고, 잘못하면 **알림이 통째로 안 뜬다.** 발송기는
    `chat` 표를 안드로이드에도 똑같이 실어 두었으니, 손댈 때 거기부터 볼 것.
  - **헤드리스로는 확인할 수 없다** — 알림도 앱도 없는 자리다. TestFlight로
    받아 대화방을 열어 놓고 남이 한 마디 보내 보는 것이 유일한 확인이다.
- **앱 알림음은 `까꿍`이다**(아이폰. 사용자 제보 — `까꿍이라고 안하고
  진동으로 오네`). 소리 파일이 **앱 번들 맨 위**에 들어 있고
  (`ios/App/App/kkakkung.wav` — 웹이 쓰는 것을 그대로 복사한 16비트 PCM ·
  0.59초. 아이폰은 30초를 넘거나 PCM이 아니면 안 받는다) 발송기가
  `aps.sound`에 **파일 이름만** 적는다.
  - **`public/` 아래에 두면 못 찾는다** — iOS는 번들 맨 위와
    `Library/Sounds`만 본다. `npx cap sync`가 옮겨 주는 `App/public/`은
    거기가 아니라, `project.pbxproj`의 Resources에 따로 등록해 두었다.
  - **못 찾으면 폰 기본음으로 울릴 뿐 알림은 그대로 뜬다** — 그래서 이
    파일이 없는 옛 앱에서도 탈이 없다.
  - **되돌리는 길을 남겨 두었다** — Secrets에 `APNS_SOUND=default`를
    넣으면 폰 기본음이다(앱을 다시 안 만들어도 된다).
  - **앱을 보고 있을 때는 안 난다**(위 `presentationOptions`에 `sound`가
    없다). 그 자리는 `lib/sound.ts`의 `까꿍`이 맡는데 **대화에서만** 난다.
  - **폰이 무음이면 진동만 온다.** 어느 앱이나 같다 — `까꿍`이 안 난다는
    말이 나오면 **무음 스위치부터 볼 것.**
- **안드로이드 알림음도 `까꿍`이다 — 다만 소리가 `채널`에 박힌다**
  (사용자 요청 — `안드로이드 알림도 이제하자`). 아이폰처럼 payload 한 줄로
  안 끝나는 것이 여기 차이다: 안드로이드 8부터 알림음은 보내는 쪽이 아니라
  **앱이 미리 만들어 둔 알림 채널**의 것으로 울린다.
  - **만드는 곳은 `MainActivity.createNotifyChannel()` 하나다.**
    플러그인의 `PushNotifications.createChannel()`(JS)로 하지 말 것 —
    그건 **웹 화면이 떠서 그 줄이 돌아야** 생기는데, 앱을 새로 깐 사람이
    앱을 열기 전에 알림이 먼저 닿으면 채널이 없어 **기본음**으로 뜬다.
    `onCreate`는 화면보다 먼저, 알림을 켰든 안 켰든 늘 도는 자리다.
  - **글자는 `android/app/src/main/res/values/notify.xml`에만 있다** —
    `MainActivity`와 `AndroidManifest.xml`이 같이 읽는다. 발송기의
    `FCM_CHANNEL`(`supabase/functions/notify`)이 **같은 글자여야 한다** —
    한쪽만 고치면 안드로이드가 모르는 채널로 보고 기본 채널로 떨어뜨린다.
  - **한 번 만들어진 채널의 소리는 앱이 못 바꾼다.** 사람이 설정에서
    바꿔 둔 것을 앱이 덮지 못하게 막아 둔 자리라 이름·설명만 갱신된다 —
    **소리를 바꾸려면 채널 id를 올리는 수밖에 없다**(`kkakkung_v1` → `_v2`).
  - **매니페스트의 `meta-data` 셋이 한 벌이다**(기본 채널·아이콘·색).
    앱이 꺼져 있을 때 오는 알림은 우리 코드가 아니라 파이어베이스 SDK가
    직접 띄우므로, 없으면 제 `Miscellaneous` 채널을 만들어 **기본음**으로
    뜬다.
  - **상태 막대 아이콘을 따로 그려 둔 까닭**(`res/drawable/ic_stat_kkakkung.xml`) —
    안 두면 런처 아이콘을 쓰는데 그 자리는 색을 버리고 **알파만 남겨
    흰색으로** 그린다. 우리 런처 아이콘은 알파가 없는 흰 정사각 PNG라
    **알림마다 흰 네모**가 뜬다. 선글라스 낀 골프공을 `evenOdd`로 뚫은
    한 덩어리이고(조각을 따로 그려서는 못 판다), **24px으로 줄여 놓고
    볼 것** — 처음에는 렌즈가 커서 다리가 1px이 되어 사라졌다. 헤드리스로
    가운데 가로줄의 알파를 훑어 `테두리 : 렌즈 : 다리`를 맞췄다(17 : 17 : 8).
  - **`presentationOptions: ['alert']`는 안드로이드에도 그대로 먹는다** —
    이름만 보면 아이폰 것 같지만 아니다. 앱이 앞에 떠 있으면 FCM이 알림을
    **아예 안 띄우고** 앱에 넘기는데, 플러그인이 그 값이 있을 때만 제가
    대신 띄운다(`PushNotificationsPlugin.fireNotification`). **빼면
    안드로이드에서도 그 알림이 통째로 사라진다.**
  - **빌드가 결과물을 열어 확인한다**(`android.yml`) — AAB 안의
    `res/raw/kkakkung`과 매니페스트의 기본 채널·아이콘. 빠지면 **초록인데
    폰에서만 조용히 기본음**이 나는 그 자리다.
  - 되돌리는 길은 아이폰(`APNS_SOUND`)과 같이 비밀값에 있다 —
    `FCM_CHANNEL_ID` · `FCM_SOUND`.
- **아이콘 위 빨간 표시는 앱에서 따로 붙인다**(사용자 제보 —
  `뱃지로 표시가 안돼`). **앱 안에는 서비스워커가 없어서** 웹에서 숫자를
  세던 `sw.js`의 `bumpBadge`와 `lib/badge.ts`가 **한 줄도 안 돈다** —
  그래서 앱은 표시가 붙지도 지워지지도 않았다.
  - **붙이는 것은 발송기**(`aps.badge`), **지우는 것은 앱**이다
    (`AppDelegate`의 `applicationDidBecomeActive`에서 0으로). 앱을 열었다는
    것이 곧 봤다는 뜻이라, 웹에서 보고 있는 창이 있으면 0으로 맞추는 것과
    같은 잣대다. `setBadgeCount`는 **iOS 16부터**라 갈래를 둘로 두었다.
  - **숫자는 카톡처럼 '안 읽은 대화 개수'다**(`unread_chat_counts` in
    schema.sql). 한동안 `1`(개수가 아니라 '새 소식 있음' 표)이었는데,
    그렇게 둔 까닭은 **사람마다 세면 100명 방의 한 마디에 조회가 100번**
    나가서였다 — `room_reads`를 한 번 훑어 **사람별 개수를 한 조회로**
    돌려주면 그 걱정이 없다.
    - **세는 잣대는 탭바의 빨간 숫자와 같게 맞췄다** — 내 글은 빼고
      안내 줄(`system`)까지 다 센다. 다만 **보는 시계가 다르다**:
      탭바는 이 기기의 `teetime:seen:`을, 뱃지는 서버의
      `room_reads.last_read_at`을 본다. 몇 마디쯤 어긋날 수 있고,
      **폰 아이콘 하나에 붙는 값이라 서버 쪽이 맞는 잣대다.**
    - **못 세도 알림은 그대로 보낸다.** 그 함수가 없는 저장소에서는
      숫자만 안 붙는다 — 숫자 하나 때문에 소식이 끊기면 안 된다.
    - **`0`이면 iOS가 표시를 지운다.** 다 읽은 사람에게 알림이 갈 때
      저절로 깨끗해진다. **다만 대화가 아닌 알림에는 못해도 `1`을 붙인다**
      (`badgeFor`) — 정산·조 편성에는 '안 읽음'이라는 것이 애초에 없어서,
      대화를 다 읽어 둔 사람에게는 `0`이 실려 **알림은 왔는데 아이콘은
      깨끗한** 꼴이 된다. 그건 앱을 열 때 지워지는 것으로 끝낸다.
    - **알림을 알림센터에서 지워도 뱃지는 안 지워진다**(사용자 제보 —
      `알림센터에서 삭제했는데 뱃지 1이 안사라지네`). iOS에서 그 숫자를
      지울 수 있는 것은 **앱 자신과 `badge: 0`을 실은 다음 알림**뿐이다 —
      그래서 **지우는 코드가 없는 판에서는 영영 남는다**(1.41이 그랬다.
      정산 화면에 들어가도 안 지워진다 — 화면과는 아무 상관이 없는 값이다).
      급하면 **설정 → 알림 → 까꿍 → `배지` 껐다 켜기**로 지운다.
      **뱃지를 붙이는 판을 낼 때는 지우는 코드가 같은 판에 있어야 한다.**
    - 안드로이드도 함께 싣는다(`notification_count`)지만 **런처마다
      다르게 그린다** — 삼성은 숫자, 순정은 점만이거나 아예 안 그린다.
- **`AppDelegate`에 등록 답을 넘기는 자리가 있어야 한다**(아이폰).
  Capacitor 기본 틀에는 있는데 **우리 파일에는 없었다** — 없으면
  `registration` 이벤트가 영영 안 와서 `알림 서버에서 답이 없습니다`로만
  보인다. `lib/native-push.ts`와 한 벌이니 한쪽만 지우지 말 것.
- **아이폰은 `aps-environment` 권한이 앱에 실려야 한다.** 빠지면 빌드는
  초록이고 TestFlight에도 올라가는데 **폰에서 알림 등록만 거절당한다**
  (`유효한 aps-environment 인타이틀먼트 문자열을 찾을 수 없습니다`).
  실기기에서 잡기까지 **여덟 판(34~41)** 을 돌았으니 아래를 꼭 읽을 것.
  - **먹은 방법은 하나다 — 아카이브 안의 앱에 임시 서명(ad-hoc)으로 권한을
    실어 둔다**(`아카이브에 권한을 실어 임시 서명`). `-exportArchive`가
    권한을 **앱에서** 읽기 때문이고, **임시 서명은 인증서가 없어도 된다**
    (`codesign -f -s -`)는 것이 이 자리의 열쇠다. 그 뒤 진짜 서명이 그
    권한을 그대로 물고 간다.
  - **바탕 넷도 함께 적는다** — `application-identifier` ·
    `com.apple.developer.team-identifier` · `get-task-allow` ·
    `beta-reports-active`. Xcode가 이 목록을 **그대로** 쓰는 판을 대비한
    것이다(`aps-environment` 한 줄만 두면 `application-identifier`가 없는
    앱이 되어 설치가 통째로 막힌다). 앱 쪽 값의 원본은 그대로
    `ios/App/App/App.entitlements`이고 거기서 꺼내 쓴다.
  - **`archived-expanded-entitlements.xcent`를 넣는 길은 안 먹는다 — 지금
    Xcode는 그 파일을 아예 안 본다.** 한 줄짜리로도, 바탕 넷을 다 갖춘
    것으로도 넣어 봤지만(37·38판) 서명된 앱의 권한은 매번 프로파일에서
    뽑은 넷 그대로였다. **다시 넣지 말 것** — 두 판을 헛돌았다.
  - **`export`로 뽑은 앱에 권한이 이미 있으면 아무것도 안 한다.**
    없을 때만 예비 길로 간다 — `.ipa`를 풀어 `codesign -f -s "…
    Distribution"`으로 겉만 다시 서명하고 `ditto`로 도로 묶는다.
    **속에 든 Frameworks는 안 건드린다**(제 서명으로 봉해져 있고, 겉을
    다시 서명하면 `CodeResources`만 새로 셈된다).
    그 길의 권한 목록은 `embedded.mobileprovision`에서 통째로 꺼내 쓴다.
  - **그 예비 길은 인증서를 못 찾아 막힐 수 있다.** 40판에서
    `security find-identity`가 `0 valid identities found`인데도 앱은 멀쩡히
    서명돼 있었다 — Xcode가 제 키체인에 넣고 쓴다. 그래서 키체인 파일을
    하나씩 다 뒤진다. **이것이 예비 길이고 본길이 아닌 까닭이다.**
  - **인증서 이름을 `grep`으로 찾지 말 것 — 못 찾으면 그 자리에서 죽는다.**
    `ID=$(… | grep …)`은 grep이 빈손일 때 1을 돌려주고, `set -e`가 그걸 보고
    **다음 줄의 `[ -n "$ID" ]`에 닿기도 전에** 끝낸다 — 39판에서 우리가 적어
    둔 문구도 못 보고 조용히 멈췄다. `sed`는 빈손이어도 0이라 갈래가 산다.
    (같은 이유로 워크플로에서 **값을 잡는 자리마다** `grep` 대신 `sed`거나
    `|| true`를 둘 것.)
  - **올리는 것은 `altool`이다**(`xcrun altool --upload-app`).
    `-exportArchive`로 올리면 **아카이브에서 다시 서명해** 방금 붙인 권한이
    도로 날아간다. 열쇠는 `~/private_keys/AuthKey_<KEY_ID>.p8`에 두면
    `altool`이 스스로 찾는다.
  - **앱에 실어 두는 것만으로는 안 된다 — 권한을 실제로 주는 것은 애플이
    내주는 프로비저닝 프로파일이다.** `com.kkakkung.app`의 App ID에서
    **Push Notifications를 켜 두지 않으면** 그 프로파일에 `aps-environment`가
    없어 서명할 때 통째로 빠진다. `-allowProvisioningUpdates`는 프로파일을
    만들어 줄 뿐 **App ID의 기능을 켜 주지는 않는다.** 사람이 한 번 켜야
    하는 자리라 `docs/설치.md` 7-1번 맨 앞에 적어 두었다.
  - **우리가 넣은 것을 우리가 다시 보는 검사는 아무 뜻이 없다** — 늘 통과한다.
    실제로 그렇게 두었다가 빌드가 내내 초록인데 폰에서만
    `유효한 aps-environment 인타이틀먼트 문자열을 찾을 수 없습니다`가
    떴다(1.34판). 지금은 `codesign -d --entitlements`로 **서명된 결과**를
    읽어 보고, 없으면 무엇을 켜야 하는지 적어 빨갛게 세운다.
  - **그래서 `destination: upload`를 안 쓴다.** 그건 `exportPath`에
    아무것도 안 남기고(올리고 지운다) 곧바로 올려 버려서, 검사를 그 뒤에
    붙였더니 `서명된 파일이 없어 건너뜁니다`로 조용히 지나갔다(35판) —
    **검사를 넣었는데 안 도는 것이 안 넣은 것보다 나쁘다**(초록이 거짓말을
    한다). 지금은 `export`로 파일만 뽑아 확인하고, **올리기 전에** 붙잡아
    빌드 번호를 헛되이 안 태운다.
  - **`embedded.mobileprovision`도 함께 찍는다 — 갈래가 둘이라서다.**
    앱에 권한이 없을 때 그것만으로는 `App ID를 안 켰다`인지 `프로파일엔
    있는데 우리가 안 붙였다`인지 못 가른다. 실제로 앞엣것으로 짐작하고
    사용자에게 시켰는데 **뒤엣것이었다**(37판). 지금은 그 줄을 보고
    오류 문구도 갈라 적는다 — 사람이 할 일인지 아닌지가 거기서 갈린다.
  - 폰 화면에도 사람 말로 적는다(`whyToken()` in native-push.ts) —
    애플이 주는 영문·직역 문구가 그대로 뜨면 어디를 고쳐야 하는지 알 수 없다.
- **안드로이드는 `POST_NOTIFICATIONS` 권한과 `google-services.json`이
  있어야 한다.** 앞엣것이 없으면 권한 창이 아예 안 뜨고, 뒤엣것이 없으면
  앱은 멀쩡히 만들어지는데 **폰이 알림 주소(토큰)를 아예 못 받는다.**
  - **`GOOGLE_SERVICES_JSON`이 없으면 빌드를 세운다** — 예전에는 노란 줄만
    남기고 만들었는데, 그러면 알림이 빠진 앱이 조용히 나간다. 아이폰에서
    그 '초록인데 실은 안 되는' 앱을 여덟 판(34~41) 쫓고 나서 여기도 막았다.
    **서명 열쇠와는 다르다** — 그건 돈을 내야 생기므로 없어도 만들지만,
    이건 공짜라 없을 이유가 없다.
  - **패키지 이름(`com.kkakkung.app`)이 맞는지도 그 자리에서 본다.**
    파이어베이스에 앱을 등록할 때 제일 자주 틀리는 자리이고, 한 글자만
    달라도 토큰을 못 받는다.
  - **다 만든 뒤에 결과물을 열어 확인한다**(`알림이 진짜 실렸는지`) —
    플러그인이 만들어 낸 `google_app_id` · AAB 안의 `POST_NOTIFICATIONS` ·
    알림음(`res/raw/kkakkung`) · 매니페스트의 기본 채널·아이콘.
    **우리가 넣은 것을 우리가 다시 보는 검사는 하지 말 것**(아이폰에서
    늘 통과해 두 판을 헛돌았다) — **빌드가 뱉어 놓은 것**을 본다.
  - **그 검사가 거꾸로 세 판을 죽였다. 두 가지가 겹쳤다:**
    - **`값을 잡는 자리`마다 `|| true`를 붙일 것.** `find`·`grep`·`ls`는
      못 찾으면 1을 돌려주는데, `pipefail`이 걸린 `bash -e`에서는 그것이
      **다음 줄에 닿기도 전에** 스크립트를 끝낸다 — 우리가 적어 둔 오류
      문구도 못 보고 `exit code 1` 한 줄만 남는다. 아이폰 쪽에서 `grep`으로
      겪고 적어 둔 그 함정을 안드로이드에서 `find`로 되풀이했다.
    - **google-services가 만들어 내는 자리를 못박지 말 것.** 4.4부터
      `build/generated/res/google-services`가 아니라 **task 이름이 들어간
      자리**(`.../res/processReleaseGoogleServices/values/`)다 — 못박아
      두었더니 알림이 멀쩡히 실린 앱에서도 이 검사만 빨갛게 섰다.
      지금은 `google_app_id`가 적힌 파일을 **찾는다.**
  - **매니페스트는 눌러 담긴 모양(AXML)이라 `grep -a`만으로는 헛짚는다.**
    글자가 UTF-16으로 들어 있으면 사이사이 `\0`이 끼어 안 걸린다 —
    **널을 걷어내고(`tr -d '\0'`) 본다.**

### 안드로이드 앱

`android/`도 저장소에 있다(아이폰과 같은 이유 — 손으로 고쳐 둔 것이 있다).

- **카카오 로그인이 돌아오는 문**(`kkakkung://`)이 매니페스트에 있다.
  아이폰의 `CFBundleURLSchemes`와 **한 쌍이고**, 둘 다 `lib/supabase.ts`의
  `NATIVE_REDIRECT`와 같은 글자여야 한다.
- **아이콘 자리가 또 늘었다.** 아이콘을 바꾸면 이제 **웹 넷 + 인라인 하나 +
  iOS 하나 + 안드로이드(적응형 앞장·옛 런처·둥근 것 × 다섯 밀도)**다.
  **적응형 아이콘의 안전 구역은 66%**(72/108)로 **웹 maskable(80%)보다
  좁다** — 그대로 옮기면 런처가 동그랗게 자를 때 가장자리가 잘린다.
  뒷장은 흰색 한 칸이라 앞장은 배경 없이 그림만 둔다.
- 첫 화면(splash)은 흰 바탕에 스티커 하나로, `index.html`의 `.boot`와
  같은 모양이어야 이어 보인다.
- **판 번호는 워크플로가 준다**(`appVersionCode`·`appVersionName`).
  `versionCode`는 **올릴 때마다 커져야** 구글이 받아 준다 — 손으로 적지 말 것.
- **서명 열쇠가 없어도 만들어진다**(`ANDROID_KEYSTORE_PATH`가 비면 서명을
  안 붙인다). 등록비를 내기 전에도 **앱이 제대로 만들어지는지** 확인할 수
  있게 그렇게 두었다 — 아이폰은 애플 열쇠가 없으면 아카이브 자체를 못 만든다.
- **Play에 저절로 올리는 단계는 일부러 안 넣었다.** 앱이 아직 없어서 넣어
  두면 돌릴 때마다 빨갛게 실패한다. 첫 판은 결과물(`kkakkung-aab`)을 받아
  손으로 올린다 — 그래야 Play에 앱이 만들어진다.
- **결과물이 둘이다 — `kkakkung-aab`와 `kkakkung-apk`.**
  앞엣것이 Play에 올리는 것이고, 뒤엣것은 **폰에 바로 깔리는 디버그 판**이다
  (`assembleDebug`를 함께 돌린다).
  **서명 안 된 AAB는 깔 수도 올릴 수도 없어**, 열쇠를 넣기 전에는 만들어
  놓고도 확인할 길이 아예 없었다 — 디버그 판은 안드로이드가 늘 들고 있는
  디버그 열쇠로 저절로 서명되므로 파일만 보내면 깔린다.
  **`applicationIdSuffix`를 붙이지 말 것** — 지금은 디버그도 패키지 이름이
  `com.kkakkung.app` 그대로라 `google-services.json`이 그대로 먹고
  **알림과 까꿍 소리까지 그 판으로 확인된다.** 붙이는 순간 토큰을 못 받아
  이 길의 뜻이 없어진다.
  **Play에 디버그 판을 올리지 말 것** — 스토어가 안 받는다.
- **`google_app_id` 검사는 `release` 자리를 먼저 고른다.** 디버그도 함께
  만들면서 `processDebugGoogleServices` 자리가 같이 생기는데, 그걸 집으면
  **정작 올릴 판을 안 보고 통과한다**(아이폰에서 두 판을 헛돈 '초록인데
  실은 안 되는' 자리와 같다).

### 글칸은 앱에서 네이티브다 — 대화와 댓글이 같은 바를 쓴다

**웹 글칸으로는 못 고치는 것이 둘이라 만든 것이다**(사용자 요청 —
`댓글창이랑 채팅창 네이티브로 수정하자`):

1. **천지인 깜빡임.** 아이폰 천지인은 웹 글칸에서 조합을 안 쓰고 글자를
   바꿀 때 **칸을 7~13ms 비웠다 채운다.** 맨 칸(자바스크립트 한 줄 없는
   `<textarea>`)에서도 같아서 웹에서 바꿀 것이 없었다(위 `깜빡임의 진짜 원인`).
2. **키보드와 따로 노는 움직임.** 카톡의 입력칸은 키보드에 붙은 네이티브
   뷰(`inputAccessoryView`)라 iOS가 **한 번의 움직임으로 함께** 옮긴다.

파일은 **넷**이다 — `ios/App/App/ComposerBar.swift`(바) ·
`ios/App/App/NativeComposerPlugin.swift`(다리) ·
`ios/App/App/MainViewController.swift`(**등록**) · `src/lib/composer.ts`(웹 쪽).

**Capacitor는 앱 안에 넣어 둔 Swift 플러그인을 스스로 찾지 않는다 —
손으로 등록해야 한다.** 첫판이 여기서 통째로 막혔다: 앱은 멀쩡히 빌드돼
TestFlight까지 올라갔는데 폰에서는 **고친 것이 하나도 없어 보였다**
(천지인 깜빡임도 키보드 엇박자도 그대로). 되물러남 규칙대로 예전 웹 글칸이
조용히 그대로 쓰였기 때문이다.

- `CapacitorBridge.registerPlugins()`는 **런타임을 훑지 않고**
  `capacitor.config.json`의 `packageClassList`만 읽는다. 그 목록은
  `npx cap sync`가 **npm으로 깐 플러그인 꾸러미**에서 뽑아 적는 것이라,
  우리가 앱 폴더에 넣은 Swift는 `CAPBridgedPlugin`을 따라도 거기 없다.
  (`node_modules/@capacitor/ios/.../CapacitorBridge.swift`에서 확인했다.)
- 그래서 `CAPBridgeViewController`를 물려받은 `MainViewController`가
  `capacitorDidLoad()`에서 `bridge?.registerPluginInstance(...)`로 넣는다.
  그 자리는 **다리가 만들어진 직후·웹 화면을 열기 전**이라 첫 화면부터 있다.
  플러그인이 `CAPPlugin`이 아니라 **`CAPInstancePlugin`인 것도 이것 때문이다**
  (다리가 스스로 만들지 않는 갈래).
- **`Main.storyboard`가 그 클래스를 가리켜야 한다**(`customClass`·`customModule="App"`).
  스토리보드와 클래스는 한 쌍이라 **한쪽만 고치면 다시 조용히 안 불린다.**
- **`npx cap sync`가 `capacitor.config.json`을 다시 만든다** — 그 파일에
  손으로 이름을 적어 넣는 길로 가지 말 것. CI가 돌 때마다 지워진다.

**그래서 한동안 `내 정보` 맨 아래에 한 줄을 적어 두었다**(`ncStatus()`) —
`글칸 앱O·플러그인O·바O`. 폰에서만 갈리는 자리라 **물어볼 값이 없으면
짐작만 하게 되고, 실제로 그래서 한 바퀴를 헛돌았다.** 셋이면 충분했다:
앱인가 · 플러그인이 있나 · 바가 화면에 섰나.
**출시용으로 넘어가면서 걷어냈다**(사용자 요청 — 그 자리는 이제
`앱제작: 악마제리` · `버전 1.0` 두 줄이다). **함수는 남겨 두었으니 다시 팔
일이 생기면 그 한 조각만 도로 붙이면 된다**(`badgeSupport`·`화면 판` 줄과
같은 자리다).

- **바는 화면 아래에 늘 서 있는 보통 뷰이고, 아래를 `keyboardLayoutGuide`에
  묶는다**(iOS 15+ · 그 아래는 안전 영역에 묶고 키보드 알림으로 손으로
  올린다, `ComposerBar.bottomC`). 키보드가 오르내리면 iOS가 그 안내선을
  **키보드와 같은 움직임으로** 옮기므로 바가 붙어서 함께 간다.
  - **처음에는 `inputAccessoryView`였다 — 되돌리지 말 것.** 그 방식은
    first responder가 곧 생명줄이라 눈에 안 보이는 `ComposerHost`가 늘 그
    자리를 쥐고 있어야 했는데, **대화 바탕을 한 번 누르기만 해도 웹뷰가
    first responder를 가져가 바가 통째로 사라졌다**(실기기 제보 —
    `채팅 배경을 누르면 아예 사라져`). 되받으면 이번엔 **탭바 밑에서 다시
    솟아오르는 것**이 그대로 보였다(`탭바를 덮으면서 올라와`). 게다가
    세운 직후에는 바가 창에 아직 안 붙어 있어 `becomeFirstResponder()`가
    조용히 실패했다(댓글 칸에서 키보드가 안 뜬 자리). 보통 뷰로 세우면
    셋이 다 없다. 그때 있던 `pause`/`resume`은 이제 아무 일도 안 한다
    (옛 웹이 부르므로 이름만 남겼다).
  - **`ready()`가 돌려주는 `v`가 앱 쪽 판 번호다**(지금 8). 판마다 는 것은
    이렇다 — 3판 초점 붙들기(`holdFocus`)·키보드 시각 알림(`kb`) ·
    4판 프레임마다 자리 알림(`frame`) · 5판 한 자리에서 `chatH`·`pad`를
    함께 셈 · 6판 그 두 값의 주인이 바로 옮겨감(늘 알림) · 7판 감추기
    (`hidden`) · 8판 사진 고르기·저장·공유(`pickPhoto`·`savePhoto`·`sharePhoto`) ·
    9판 고르는 창을 `+` 옆에 작게 붙이고 저장은 끝난 뒤에 답한다 ·
    10~11판 사진을 곱게 줄인다 · 13판 크기를 2560px으로 ·
    12판 고르는 창이 **닫힌 뒤에** 보관함·카메라를 띄운다(`afterSheet`).
    웹이 그걸 보고
    `html.nc2`를 붙이고, 여백 셈을 옛 판과 가른다(아래). `내 정보`의
    진단 줄에도 `(2판)`으로 적힌다 — **새 앱을 깔았는지가 폰에서는 이걸로만
    갈린다.** 판을 바꾸는 고침을 하면 이 번호를 올릴 것.
- **바가 맡는 것은 한 줄뿐이다** — `+` · 글칸 · 이모티콘 · 보내기.
  인용(답장)·언급 목록·이모티콘 미리보기·서랍은 **그대로 웹이** 그 위에
  그린다. 바가 가리는 만큼만 비운다(`--composer`는 **바가 알려 준다**).
- **바가 웹뷰를 덮으므로 키보드가 올라와 있어도 그만큼 비운다**(2판).
  `resize: 'native'`는 키보드 높이만큼만 웹뷰를 줄이고(바는 안 들어 있다)
  바는 그 위에 얹히므로 `.chat-input`의 아래 여백이 늘 `--composer`다.
  키보드가 내려가 있을 때는 바 아래에 홈 인디케이터 자리(`--safe-b`)가
  더 있고, 올라오면 웹뷰가 키보드 위에서 끝나 그 값이 0이 된다 — 그런데
  웹뷰가 줄어드는 것이 0.45초 늦어 그 사이 `env()`가 옛 값을 주므로
  **`kb-open`에서는 아예 안 더한다**(`Chat.css`의 `html.nc2` 두 줄).
  댓글도 같다(`global.css`의 `html.nc2 body.nc-typing`).
  **1판(옛 앱)은 반대였다** — 바가 `inputAccessoryView`라 키보드 프레임에
  바까지 들어 있어 웹뷰가 이미 바 위에서 끝났고, 그래서 `kb-open`에서
  여백을 0으로 뒀다. 그 규칙은 `html.nc:not(.nc2)`로 남아 옛 앱을 돌본다.
  - **`--composer`의 주인은 하나여야 한다 — 지우는 곳이 따로 있으면 안 된다.**
    웹 글칸을 재는 `ResizeObserver` 효과가 `[roomId, focused]`로 걸려 있어
    **초점이 오갈 때마다 뒷정리로 `--composer`를 지웠는데**, 다시 적는 일은
    그 효과가 `ncOn`에서 되돌아서므로 **영영 안 채워졌다.** 그러면 CSS의
    예비값(60px)이 쓰이는데 실제 바는 116px이라 **목록 아래 56px이 바 뒤로
    숨어 마지막 글이 안 보인다**(실기기 · 진단 줄에 `b0 L674`로 찍혔다).
    **키보드를 내릴 때만 걸린 것이 이 자국의 열쇠였다** — 올릴 때는
    `setFocused(true)` 뒤에 바가 높이(58)를 알려 와 도로 채워지는데, 내릴
    때는 `onComposerBlur`가 150ms 기다렸다 `setFocused(false)`를 부르므로
    **바가 알려 준 116이 먼저 오고 그다음에 지워진다.**
    지금은 ① 네이티브 바를 쓰는 동안에는 그 뒷정리가 안 지우고
    ② 마지막으로 알려 온 높이를 `ncH`에 담아 두었다 다시 적는다.
    화면을 떠날 때는 네이티브 효과의 뒷정리가 지운다(`ncOn`을 내린 뒤에
    지우므로 순서도 맞는다).
- **탭바 자리를 바가 비워 준다**(`tabH`). 바는 안전 영역 바로 위에 서는데
  그 자리가 우리 탭바다. 그 높이만큼 바를 키우고 **칠하지도 손짓을 받지도
  않아**(`hitTest`가 nil) 밑의 웹 탭바가 그대로 보이고 눌린다. 키보드가
  올라오면 탭바가 감춰지므로 이 몫도 0이 된다.
  - **그 몫은 키보드 알림에서 걷되, 키보드와 같은 시간·곡선으로 편다**
    (`setKb`가 알림의 값으로 `UIView.animate`). 그냥 바꾸면 키보드가
    움직이기 시작하는 순간 바가 탭바 높이만큼 툭 내려앉았다 올라간다.
    (1판에서 `textViewShouldBeginEditing`에서 미리 걷던 것은 키보드
    프레임 셈 때문이었다 — 이제 그 셈이 없으므로 걷어냈다.)
  - **댓글 바는 `tabH: 0`이다.** 거기는 적는 동안(=키보드가 올라와 있는
    동안)만 뜨고 그때는 탭바가 이미 감춰져 있다.
  - **바가 자라면 목록을 다시 앉힌다**(`height` 신호에서 `settleList(true)`).
    키보드가 내려갈 때 바가 탭바 몫만큼 자라 목록이 그만큼 짧아지는데,
    거기서 안 앉히면 **맨 아래 글이 잘려 안 보인다**(실기기 제보).
    그 사이 `onScroll`이 '맨 아래가 아니다'로 내려 버리므로 **값을 바꾸기
    전에 집어 두었다가** 넘긴다 — `settleList`에 `force` 인자가 있는 까닭이다.
    - **그것만으로는 모자랐다 — 앉히는 동안에는 `onScroll`이 `atBottom`을
      아예 안 고친다.** 키보드가 오르내리면 목록 높이가 여러 단계에 걸쳐
      바뀌고 그때마다 스크롤 이벤트가 오는데, **그 한 번이 먼저 도착하면
      뒤따르는 신호가 전부 '맨 아래가 아니었다'로 읽힌다.** 집어 두는
      자리를 아무리 앞당겨도 그 앞에 또 한 번이 올 수 있으니, 잣대를
      **얼려 두는 것**이 답이다(`settling.current.ro`가 살아 있는 600ms).
      손을 뗄 때 한 번 다시 재어 맞춘다.
- **색은 `fg`로 보낸다. `text`가 아니다.** `text`는 **글 내용**이 이미
  쓰는 이름이라(댓글 바가 적어 둔 글을 그렇게 실어 보낸다), 겉모습만
  보내는 `attach`가 **글칸에 색 코드(`#1b1f19`)를 써 넣었다** — 실기기
  글칸에 그 글자가 그대로 보였다. 이름이 겹치면 조용히 이런 자국이 난다.
- **초점은 세우면서 함께 준다**(`attach`의 `focus: true` · `grabFocus`).
  바가 보통 뷰라 세우자마자 창에 붙어 있어 대개 한 번에 되는데,
  `becomeFirstResponder()`가 거절하는 판이 있어 다시 해 본다.
  **웹에서도 같은 되풀이를 한다**(`openBar`의 `for`) — 1판 앱에서 창에 아직
  안 붙은 바에 초점을 주다 실패하던 자리를 웹만 밀어서 메운 것이다.
  **한쪽만 고치지 말 것.**
  - **아이폰은 손을 떼는 순간 웹 화면이 first responder를 도로 가져간다.**
    웹 칸을 누른 것이 아니어도 그렇고, `preventDefault`로도 못 막는다 —
    그래서 초점을 받아 키보드가 올라오려다 그다음 프레임에 뺏겨
    **아무 일도 안 일어난 것처럼 보였다**(실기기 제보 —
    `댓글은 키보드 자체가 안나와`).
  - **되찾지 말고 놓지 말 것.** 처음에는 0.08초마다 살펴보다 초점이 없으면
    다시 잡게 두었는데, 그러면 이번엔 **키보드가 올라오다 내려갔다 다시
    올라온다**(실기기 제보 — `키보드가 나오다 중간에 다시 내려갔다가
    다시 올라와`). 뺏긴 뒤에 되찾으면 그 오르내림이 그대로 보인다.
    3판은 `ComposerBar.holdFocus`로 **놓는 것 자체를 막는다**
    (`textViewShouldEndEditing`이 `false`를 돌려준다) — 뺏길 일이 없으니
    되찾을 일도 없다. 붙드는 것은 초점을 준 뒤 0.8초뿐이고, 그 뒤는 사람이
    내리는 것이라 그대로 놓아 준다. **우리가 내릴 때는 먼저 풀 것**
    (`blur`·`detach`의 `release()`) — 안 풀면 우리 것까지 막힌다.
  - **웹 쪽 되풀이는 '주는 것'만 한다**(`openBar`의 `for`) — `holdFocus`가
    없는 옛 앱 몫이다. 왔다가 뺏기는 것은 되풀이로 보지 않고 `focus`
    듣는 곳에서 **그 자리에서** 도로 잡는다(`grabUntil`). 기다렸다 잡으면
    그 사이가 곧 위의 오르내림이다.
- **댓글은 둘이 다르다** — 바가 **적는 동안만** 뜨고 `+`·이모티콘 단추가
  없다. 웹 칸은 `readOnly`인 **누르는 자리**가 되고, 적은 글은 거기에도
  옮겨 적어 둔다(바를 닫아도 남아야 한다).
  - **그래서 바가 떠 있는 동안에는 웹 칸을 감춘다**(`body.nc-typing
    .comment-form { visibility: hidden }` · 사용자 제보 — `댓글쓰는데가
    2군데야`). 옮겨 적어 둔 글과 `등록`이 그대로 한 벌 더 보여, 어느
    쪽에 적는 것인지 헷갈렸다.
    **`display: none`으로 지우지 말 것** — 그만큼 자리가 없어져 읽던
    글이 통째로 밀리고, `openBar`가 끌어 올릴 자리(`scroll-margin`)도
    함께 사라진다. `.dev/behave.mjs`가 **자리는 그대로인지**까지 잰다
    (헤드리스에는 바가 없으므로 `nc-typing`만 손으로 붙여 규칙을 본다).
  덤으로 **`키보드가 댓글 칸을 가린다`가 없어진다** — 적는 자리가 키보드에
  붙어 있어 가릴 것이 없다(`lib/keyboard.ts`의 `reveal`은 웹 칸일 때만 돈다).
  2판에서는 바가 본문을 덮으므로 `.page` 아래를 `--composer`만큼 비우고,
  누르는 자리(`.comment-form`)에 같은 값의 `scroll-margin`을 둔다.
  - **댓글 칸을 끌어올리는 것은 창이 줄어든 뒤에도 한 번 더 한다**(`openBar`의
    `resize` 듣기). 웹뷰는 키보드보다 0.45~0.8초 늦게 줄어드는데(진단 —
    `c512`가 800ms에 찍혔다) `scrollIntoView`를 그 전에만 불러서, 줄기 전
    크기로 굴린 자리가 키보드 뒤로 들어갔다 — **키보드와 바는 뜨는데 댓글
    칸은 안 올라오던 자리다**(사용자 제보 — `댓글창이 안올라와`. 처음엔
    바가 안 선 줄 알았는데 진단 줄이 `y454 fr1 kb1`로 바가 제자리에 있음을
    보여 줬다). 창이 줄면 `resize`가 오므로 그때 굴리면 맞다.
  - **바를 버리지 않는다**(6판의 `detach`). `UITextView`를 만드는 것이
    만만치 않아서, 누를 때마다 새로 만들면 **바가 뜨기까지 50ms**가 걸렸다
    (진단 — `누름 0` → `바 51`. 사용자 제보 — `뜨는게 텀이있는데`).
    화면에서 떼어 두기만 하고 다음 `attach`에서 다시 붙인다. 그러면 높이가
    지난번과 같아 `tellHeight`가 아무 말도 안 하게 되므로 — 웹은 그것을
    '바가 안 섰다'로 보고 1.5초 뒤 되돌린다 — **`announce()`로 한 번 못을
    박는다.** 같은 까닭으로 `composerSkin()`도 한 번만 읽고 기억한다
    (`getComputedStyle`을 열두 번 부르는 일이라 누르는 그 순간에 하면 늦다).
  - **그것으로도 모자라 바를 미리 세워 둔다**(6판 · `Comments.tsx`의 `warm`).
    댓글이 있는 화면이 뜨면 바를 세워 **감춰 두고**(`hidden`), 누를 때는
    `setState({hidden:false, focus:true})`로 **다리를 한 번만** 건넌다.
    닫을 때도 떼지 않고 도로 감춘다. **감춘 뷰는 초점을 못 받으므로**
    내보이는 것과 초점 주는 것의 차례를 바꾸지 말 것(`apply` → `grabFocus`).
    `hidden`을 모르는 앱은 미리 세우면 바가 그대로 보이므로 **7판부터만** 한다.
  - **`composerSkin()`은 `hidden: false`를 늘 함께 보낸다.** 앱은 **보낸 값만**
    고치므로 안 보내면 지난 화면 값이 남는다 — 댓글이 감춰 둔 바가 대화까지
    따라가 **입력칸이 통째로 안 보였다**(사용자 제보 — `메시지창 자체가 안보여`).
    **상태를 남기는 값을 새로 만들 때는 기본값도 함께 보낼 것.**
  - **사진 고르는 숨은 칸에 `hidden`을 쓰지 말 것**(`.file-anchor`).
    iOS는 `Photo Library / Take Photo / Choose File` 창을 **그 칸이 있는
    자리**에 붙이는데, `display: none`이면 자리가 없어 **화면 한가운데에
    뜬다**(사용자 제보 · 사진). `+` 자리에 겹쳐 `opacity: 0`으로 둔다.
    **1px로 두지도 말 것** — 그렇게 작은 자리는 iOS가 무시하고 제 맘대로
    띄운다(사용자 제보 — `팝업뜨는 위치가 지 맘데로야`). `+`만 한 44px로 둔다.
    - **이 규칙은 사진을 고르는 자리 전부에 걸린다 — 대화만이 아니다.**
      `내 정보`의 프로필 사진이 `hidden`인 채로 남아 있어, 앱에서 얼굴을
      눌러도 **아무 일도 안 일어났다**(사용자 제보 — `앱으로 올렸는데
      프로필사진이 없어`). 대화에서 겪고 고쳐 놓고 여기만 빠뜨린 것이다.
      지금은 `.avatar-slot .file-anchor`가 얼굴에 겹쳐 있다(56px).
      **새로 사진 고르는 자리를 만들면 이 두 가지를 함께 볼 것** —
      숨은 칸에 자리가 있는가 · 앱이면 `pickNativePhoto()`로 가는가.
      `.dev/behave.mjs`의 `프로필 사진 고르기` 칸이 붙들어 둔다.
  - **그래도 안 됐다 — `+`가 네이티브라 웹은 손짓을 아예 못 받는다**(8판).
    자리를 `+`에 겹쳐 44px로 키워도 창은 여전히 엉뚱한 데 떴다(사용자 제보 —
    `사진을 보고 +버튼을 누르면 엉뚱한곳에 메뉴가떠`). 까닭은 자리가 아니라
    **누른 것이 웹뷰 밖**이라는 데 있다 — 웹이 코드로 `input.click()`을
    부르는 것이라 iOS에게는 붙일 손짓이 없다.
    **그래서 고르는 것도 네이티브로 옮겼다**(`pickPhoto`) —
    `사진 보관함`(PHPicker) · `사진 찍기`(카메라) · `취소` 세 줄짜리
    액션시트를 앱이 띄우고, 고른 그림을 **2560px · JPEG 82%**로 줄여
    base64로 웹에 넘긴다(`lib/image.ts`와 같은 값이다 — **한쪽만 고치지 말 것**).
    웹은 그걸 `Blob`으로 되돌려 예전과 **같은 길**로 올린다(`sendPhoto`).
    - **PHPicker는 권한 문구가 필요 없다**(앱 밖에서 도는 창이다).
      카메라는 `NSCameraUsageDescription`, 사진첩에 저장하는 것은
      `NSPhotoLibraryAddUsageDescription`이 **없으면 그 자리에서 앱이 죽는다.**
    - **`.file-anchor`와 웹 `<input type=file>`은 그대로 둔다** — 웹으로
      열었을 때와 8판이 아닌 앱에서 그 길이 유일하다.
    - **그 창을 `+` 옆에 작게 붙인다**(9판 · 사용자 요청 — `좌측하단으로
      옮겨주고 버튼 크기도 좀 작게`). 아이폰의 `.actionSheet`는 화면 아래를
      가로지르는 큰 창이라 누른 자리와 멀다. `popoverPresentationController`에
      **바의 `plusBtn.frame`을 붙이고** 화살표를 아래로 두면 그 위에 작은
      카드로 선다. **`delegate`가 `.none`을 돌려주는 것이 한 쌍이다** —
      아이폰은 팝오버를 기본으로 큰 창으로 되바꾼다.
      **그 작은 카드에서는 iOS가 `취소` 줄을 스스로 뺀다** — 바탕을 눌러
      닫으면 아무 손잡이도 안 불려 **약속이 영영 안 닫히므로**,
      `popoverPresentationControllerDidDismissPopover`에서 `finishPick(nil)`을
      부른다. 바가 없을 때의 예비 자리(왼쪽 아래)도 함께 둘 것 — 붙일
      자리가 없으면 아이패드에서 그대로 죽는다.
    - **그 창은 닫힌 뒤에 다음 창을 띄운다**(12판 · `afterSheet`).
      팝오버로 바꾸면서 닫히는 방식이 달라졌는데, **아직 닫히는 중인
      화면 위에 `present`를 부르면 iOS가 조용히 무시한다** — 9~11판에서
      **보관함도 카메라도 눌러 봐야 아무 일이 안 일어났다**(사용자 제보 —
      `보관함하고 찍는 것도 둘 다 안돼`). 닫힐 때까지 몇 프레임 기다렸다
      띄우고, 1초를 기다려도 안 닫히면 **까닭(`why`)을 실어 약속을 닫는다** —
      열어 둔 채 두면 웹은 영영 기다린다.
    - **`...DidDismissPopover`를 '보관함·카메라를 누른 것'과 갈라야 한다**
      (`pickGoing`). 그때도 창은 닫히는데, 그걸 취소로 읽어 약속을 닫아
      버리면 **정작 고른 사진이 갈 데가 없다.**
    - **`why`가 붙어 오면 고장이고, 없으면 취소다.** 웹이 그 둘을 갈라
      고장일 때만 문구를 띄우고 웹 칸으로 물러난다. **취소에 문구를 붙이지
      말 것** — 안 보내기로 한 사람에게 오류창이 뜬다.
    - **웹은 12판부터만 앱 창을 쓴다**(`canPickNative()`). 8~11판을 든
      폰은 웹 칸으로 되돌린다 — 창이 엉뚱한 자리에 뜨긴 해도 **뜨기는
      하고**, 무엇보다 **앱을 새로 안 깔아도 웹만 밀면 그날로 고쳐진다.**
      앱 한 바퀴가 30분인데 그동안 사진을 아예 못 보내게 둘 수는 없다.
      **네이티브 쪽이 막혔을 때 웹으로 물러날 길을 늘 남겨 둘 것** —
      판 번호로 가르면 새 앱을 깐 사람은 저절로 좋은 길로 돌아온다.
    - **고르는 셈은 `lib/composer.ts`에 하나로 있다**(`canPickNative` ·
      `pickNativePhoto`). **대화의 `+`와 `내 정보`의 프로필 사진이 같이
      쓴다** — 두 벌로 두었더니 한쪽만 고치는 일이 실제로 났다(위 참고).
      화면이 하는 것은 **못 골랐을 때 무엇으로 물러나느냐**뿐이다.
      돌려주는 것 셋을 반드시 가를 것 — `photo`(보냄) · `cancel`(조용히
      돌아섬) · `fail`(알리고 웹 칸으로). **취소에 문구를 띄우지 말 것.**
    - **`canPickNative()`는 누르기 전에 답할 수 있어야 한다.** 누른 뒤에
      `await composerReady()`를 하면 그 뒤의 `input.click()`을 iOS가
      사용자 손짓으로 안 쳐서 **웹 칸이 아예 안 열린다.** 그래서 사진을
      고르는 화면은 뜰 때 `composerReady()`를 한 번 불러 둔다.
  - **크게 본 사진에 `저장`·`공유` 단추를 둔다**(사용자 요청 — 카톡처럼).
    웹에서는 길게 눌러 저장하는 것이 유일한 길이었는데 **앱에서는 그
    손짓조차 잘 안 잡힌다**. 단추 둘은 **웹이 그리고**(`.photo-zoom-bar`)
    일하는 것만 갈린다 — 8판 앱이면 `savePhoto`·`sharePhoto`(사진첩 저장 ·
    iOS 공유창)로 가고, 그 밖에서는 `sharePhotoFile()`(`lib/share.ts`)이
    파일을 받아 `navigator.share`로 넘긴다. 그것도 없으면 토스트로 알린다 —
    **아무 일도 안 일어나면 고장으로 보인다**(`공유`·`캡쳐`와 같은 잣대다).
    - **분홍을 안 쓴다** — 이 화면에서 '지금 눌러야 할 것'은 보내기 단추
      하나다. 검은 바탕 위 흰 알약이고, 44px로 둔다(`audit.mjs`).
      **칠은 흰색 30%다** — 16%로 두었더니 거의 검은 바탕에서 단추로
      안 읽혔다(사용자 제보 — `시인성이 좀 떨어져`). 사진 위에 걸쳐도
      가장자리가 살게 `inset` 그림자로 테두리를 함께 둘렀다.
    - **단추를 눌러도 창이 안 닫힌다** — 바탕을 눌러야 닫힌다.
    - **저장이 끝나야 답한다.** 예전에는 `UIImageWriteToSavedPhotosAlbum`을
      부르자마자 `ok`로 답했는데, 그러면 권한을 거절당해 **정말로 안 저장된
      때도 `저장했습니다`가 떴다.** 지금은 그 함수의 손잡이
      (`image(_:didFinishSavingWithError:contextInfo:)`)를 받아 답한다.
    - **도는 동안 단추를 잠근다**(`busy`). 저장이 몇 초 걸리는데 그동안
      아무 말이 없어, 안 된 줄 알고 또 눌러 **같은 사진이 여러 장
      저장됐다**(사용자 제보). `저장`은 `저장 중…`으로 바뀐다.
    - **토스트가 이 창보다 위여야 한다**(`.toast-stack`이 1200).
      900으로 두었더니 `사진첩에 저장했습니다`가 **사진 화면(1000) 밑에
      깔려 안 보였고**, 그것이 곧 위의 '여러 장 저장'의 진짜 원인이었다.
      토스트는 `pointer-events: none`이라 위로 올려도 뒤를 안 가로막는다.
      **전체화면 창을 새로 만들 때 1200을 넘기지 말 것.**
  - **사진을 새 창(`target="_blank"`)으로 띄우지 말 것 — 앱에서는 그것이
    곧 사파리로 나가는 것이다**(사용자 제보 — `사진을 올리면 사파리에서
    열려`). 홈 화면 앱에서도 같다. 앱 안에서 크게 보는 자리를 따로 둔다
    (`.photo-zoom` · `Chat.tsx`의 `onPhotoTap`).
    - **말풍선마다 손잡이를 넘기지 않고 목록에서 한 번에 받는다** —
      `Bubble`이 `memo`라 새 함수를 넘기면 쉰 개가 다시 그려진다.
    - **크게 본 화면에서는 iOS 기본 손짓을 막지 않는다** — 길게 눌러
      저장하는 길이 그것뿐이다(목록의 `.chat-image`는 우리 길게 누르기
      때문에 막아 둔다).
    - **탭바(z-index 800)보다 위여야 한다.** 60으로 두었더니 사진 위로
      탭바가 그대로 보였다.
    - **벌려서 키우는 것도 우리가 한다**(`PhotoZoom` · 사용자 제보 —
      `사진 확대는 안되네?`). 앱 안 웹뷰는 화면 자체를 벌려 키우는 것을
      안 받아 주고, 받아 준들 입력칸·탭바까지 같이 커져 더 나쁘다.
      사진만 키우면 웹과 앱이 같은 길을 타므로 **헤드리스로 잰 것이
      폰에서도 맞는다**(`.dev/behave.mjs`의 `사진 크게 보기` 칸).
      - **`touch-action: none`이 한 쌍이다**(`.photo-zoom-view`). 안 주면
        브라우저가 그 손짓을 제가 먹어 우리 셈이 아예 안 돈다 — React의
        touchmove는 passive라 `preventDefault`로는 못 막는다(답장 밀기가
        `pan-y`로 같은 일을 한다).
      - **크기를 state에 안 넣는다.** 손가락을 따라 매 프레임 다시 그리면
        느린 폰에서 그대로 끊긴다 — 요소의 `transform`을 직접 적는다.
      - **벌리는 가운데를 붙박아 둔다**(`zoomAt`). 그냥 키우면 사진이
        손가락에서 달아나 보고 싶은 데를 못 본다.
      - **손을 뗄 때 테두리 안으로 넣는다**(`settle`). 1배로 돌아오면
        자리도 가운데로 되돌린다.
      - **사진을 누르는 것으로는 안 닫는다** — 한 번 누른 그 자리에서
        닫아 버리면 **두 번 누르기가 아예 성립하지 않는다**(첫 누름에서
        창이 사라져 두 번째가 갈 데가 없다. 실제로 그렇게 짰다가
        헤드리스에서 잡았다). 닫는 것은 **사진 바깥**과 `✕`가 맡고,
        키워 둔 동안에는 바깥을 눌러도 안 닫는다(옮기다 닫히면 성가시다).
      - **두 번 누르기가 이뤄지면 셈을 처음으로 돌린다**(`lastTap = 0`).
        안 그러면 세 번째 누름이 또 짝지어져 방금 되돌린 것을 도로 키운다.
      - **`moved` 표는 누를 때마다 비운다.** `touchstart`에서만 비우면
        손짓 뒤에 남은 값이 다음 누름까지 따라와 안 닫힌다.
  - **화면을 덮는 창이 뜨면 네이티브 바를 감출 것**(`overlayUp`).
    그 바는 웹 화면 **위에 얹힌 앱 부품**이라 **웹의 `z-index`로는 못
    덮는다** — 사진을 크게 봤는데 그 위로 입력칸이 그대로 보였다(사용자
    제보 · 사진). 덮는 창은 넷이다: 사진 크게 보기 · 길게 누른 창 ·
    프로필 카드 · 선택 복사. 감추면서 키보드도 내린다.
    감출 수 있는 것은 **7판부터**이고, 옛 앱에서 바를 떼었다 다시 붙이는
    길로 가지 말 것 — 글칸이 통째로 안 뜨는 위험이 더 크다.
    **대화에 전체화면 창을 새로 만들면 그 목록에 더할 것.**
  - **진단 줄에 판 번호를 함께 찍을 것**(`v7 주인1` · 댓글은 `v7 미리1`).
    지금은 걷어냈다 — 다시 팔 일이 생기면 이 자리에 같은 방식으로 넣는다
    (`내 정보` 맨 아래의 `ncStatus()` 한 줄은 앱 판을 가리는 데 쓰이므로 남겼다).
    앱은 새로 깔아야 하고 웹은 밀면 몇 분 뒤에 올라가므로 **둘이 어긋난
    채로 찍힌 사진**이 오기 마련인데, 번호가 없으면 그것을 '고쳤는데도
    그대로'로 잘못 읽는다 — 실제로 그렇게 같은 자리를 두 번 헛돌았다.
  - **기능을 더하면 판 번호를 반드시 올릴 것.** `hidden`을 6판에 슬쩍
    더했다가, 그 값을 모르는 옛 6판 앱에도 웹이 `감춰라`를 보내 어긋났다 —
    웹은 이 번호 하나로 앱이 무엇을 아는지 가린다.
- **없으면 저절로 물러난다.** 웹은 밀면 바로 올라가는데 앱은 새로 만들어
  깔기까지 시간이 걸려 **늘 `새 웹 + 옛 앱`인 사이가 생긴다.**
  `composerReady()`가 `ready()`를 한 번 불러 보고 안 되면 false를 돌려주고,
  그때는 예전 웹 글칸이 그대로 쓰인다. 통째로 끄는 스위치도 웹에 있다
  (`localStorage`의 `teetime:nc` = `off`).
- **손볼 값은 전부 웹에 둔다**(`composerSkin()`) — 크기·색·탭바 자리·단추
  유무가 다 거기서 간다. 네이티브 쪽 값은 예비값일 뿐이다. **앱은 한 바퀴가
  30분인데 웹은 밀면 바로**라, 어긋난 것을 고치는 길이 웹에 있어야 한다.
  크기는 `Chat.css`의 `.chat-input` 한 줄을 그대로 옮긴 것이다 — 한쪽만
  고치면 웹과 앱의 글칸이 달라 보인다.
- **글을 읽고 쓰는 곳을 넷으로 모았다**(`draftValue`·`draftCaret`·`setDraft`·
  `focusDraft`/`blurDraft` in Chat.tsx). 예전에는 `taRef.current.value`를
  직접 읽는 곳이 열 군데였다 — 갈래가 둘이 되면 한쪽만 고치게 된다.
- **여기서는 헤드리스로 확인할 수 없다.** 맥도 아이폰도 없어 Swift는
  **GitHub의 맥이 컴파일해 주는 것까지만** 확인된다(`.github/workflows/ios.yml`이
  `ios/**`가 바뀌면 돈다). 실제 움직임은 TestFlight로 받아 봐야 안다 —
  **그래서 되물러남과 웹 쪽 손잡이를 먼저 만들어 둔 것이다.**

### 날짜는 전부 한국 시각

DB에는 UTC(`timestamptz`)로 넣고 `lib/format.ts`에서만 `Asia/Seoul`로 옮긴다.
기기 시간대를 따르면 해외에 있는 사람에게 티오프 시각이 다르게 보인다.

- `<input type="datetime-local">`은 **기기 시간대**로 값을 다룬다. 그래서
  `toKstInput()` / `fromKstInput()`으로 한국 시각에 못 박아 주고받는다.
- D-day는 밀리초 차이를 86400000으로 나누지 말 것 — 시간대에 따라 하루가
  어긋난다. 두 시각을 각각 한국 날짜(`YYYY-MM-DD`)로 바꾼 뒤 그 날짜끼리 뺀다.

## 주의사항

- **고친 게 안 먹는다고 하면 '옛 화면이 남아 있는가'부터 가릴 것.** 우리
  서비스워커는 캐시를 안 두지만 **브라우저의 HTTP 캐시가 index.html을
  10분쯤 들고 있다**(GitHub Pages가 그렇게 보낸다). 그 문서가 옛 파일
  이름을 가리키면 배포해도 폰에는 예전 화면이 그대로다. 배포 직후에
  확인하면 거의 이 함정에 걸린다. `sw.js`가 문서만 `cache: 'no-store'`로
  받아 오는 것이 그 대비다.
  **판을 화면에 적어 두던 줄은 걷어냈다**(사용자 요청 — `내 정보` 맨 아래에
  `화면 판 8/23 02:20`으로 있었다). 값을 박는 `vite.config.ts`의
  `define: __BUILD__`은 남겨 두었으니, 다시 볼 일이 생기면 그 줄만 도로 붙이면
  된다. 지금은 물어볼 숫자가 없으므로 **강력 새로고침부터 시켜 볼 것.**
  (앱을 쓰는 사람에게는 이 함정이 없다 — 화면이 앱 안에 담겨 있어 **앱을
  새로 깔기 전에는 안 바뀌는 것이 정상**이다. 그쪽은 반대로 `버전 1.0` 옆
  스토어의 빌드 번호로 가린다.)
- **화면에 적는 판은 `1.28`처럼 뒷자리가 빌드 번호다**(`APP_VERSION` in
  types.ts · 사용자 요청 — `빌드가 28이다면 1.28 이런식으로`).
  - **앞자리(`MAJOR`)만 손으로 정하고 `MARKETING_VERSION`과 맞춘다.**
    앞엣것은 `내 정보` 맨 아래에 회원이 보는 값이고 뒤엣것은 앱스토어에
    적히는 값인데, **Xcode 쪽을 웹에서 읽을 길이 없어** 한 군데로 못 모았다.
    판을 올릴 때 두 곳을 함께 고칠 것
    (`ios/App/App.xcodeproj/project.pbxproj`에 두 번 나온다).
  - **뒷자리는 `ios.yml`이 넣는다** — `VITE_APP_BUILD`와
    `CURRENT_PROJECT_VERSION`에 **같은 `github.run_number`를 준다.**
    그래야 화면의 `1.28`과 TestFlight의 `1.0 (28)`이 같은 숫자가 되어,
    **폰에서 새 앱을 받았는지 그것 하나로 가릴 수 있다** — 화면이 앱 안에
    담겨 있어 앱을 새로 깔기 전에는 안 바뀌는 것이 정상이기 때문이다.
    **한쪽만 고치지 말 것.** 빌드 번호를 손으로 적지도 말 것.
  - **웹(GitHub Pages)에서는 `1.0`이다** — 거기엔 빌드 번호가 없고, 웹은
    밀면 몇 분 뒤 저절로 바뀌므로 가릴 값도 필요 없다. 덕분에 **그 숫자만
    봐도 앱인지 웹인지 갈린다.** 웹 워크플로의 `run_number`를 대신 넣지
    말 것 — 두 번호는 서로 다른 셈이라 같은 자리에 적으면 거짓말이 된다.
- **`.env` 없이 빌드한 결과로 판단하지 말 것.** `isConfigured`가
  `import.meta.env.VITE_*`로 정해지는데 Vite는 이 값을 **빌드 시점에 박아 넣는다.**
  `.env`가 없으면 `false`가 상수로 굳어 `Gate()`가 늘 `<Setup/>`을 반환한다.
  번들 내용을 확인할 땐 값을 아무거나 넣어서 빌드할 것
  (`VITE_SUPABASE_URL=https://x.supabase.co VITE_SUPABASE_ANON_KEY=dummy npm run build`).
  - **크기로는 못 가린다 — 예전에 적어 둔 `441KB / 497KB`는 이제 틀린 값이다.**
    그때는 화면이 통째로 트리셰이킹돼 작은 묶음이 나왔는데, 지금은 **둘 다
    671KB로 똑같이 나온다**(번들러가 바뀐 탓으로 보인다). 크기만 보고
    `제대로 들었다`고 판단하지 말 것 — 화면을 열어 봐야 안다.
  - 지금 크기는 **688KB(gzip 201KB)**다. Capacitor를 붙이며 662.5 → 671.2로
    **8.7KB**, 네이티브 글칸을 붙이며 671.2 → 688.6으로 **17.4KB** 늘었다 —
    뒤엣것은 `registerPlugin`이 딸려 오는 몫이라 웹에서도 실린다.
- **`.env`를 커밋하지 말 것.** `anon` 키는 공개돼도 되는 값이지만
  **`service_role` 키는 클라이언트 코드나 저장소에 절대 들어가면 안 된다** —
  그건 RLS를 통째로 건너뛴다.
- **`src/lib/types.ts`의 행 타입은 `interface`가 아니라 `type`이어야 한다.**
  인터페이스는 암묵적 인덱스 시그니처가 없어서 supabase-js의 `Database` 제네릭을
  만족하지 못한다. `interface`로 바꾸면 스키마 전체가 `never`로 무너지고
  모든 쿼리에서 타입 오류가 난다.
- **붙어 있는 요소(sticky)에는 바탕색을 꼭 깔 것.** 투명하게 두면 밑에 있는 글이
  버튼을 뚫고 지나간다(`.round-actions`가 실제로 그랬다).
- **입력칸의 안내 글씨는 `components/Hinted.tsx` 하나로 그린다**
  (모양은 global.css의 `.input-wrap`/`.input-hint`). 브라우저의
  `placeholder`를 **한글 치는 칸에는 쓰지 말 것** — iOS가 조합 중인 글자를
  '내용 없음'으로 봐서 **첫 글자에 한 번 번쩍인다.** 대화·댓글에서 겪고
  모집 열기의 골프장 칸에서 또 제보가 들어와, 닉네임·공지 제목·투표
  제목·항목·전달 내용까지 한 번에 옮겼다.
  **기준은 글자가 아니라 초점이다** — 글자로 정하면 조합 중에 다시 뜬다.
  감싸면 칸이 인라인이라 밑줄 자리만큼 6px이 붙으므로 `display: block`을
  함께 준다(안 그러면 감싼 것만으로 화면이 길어진다).
  전화번호처럼 **숫자만 치는 칸은 그대로 `placeholder`를 써도 된다.**
- **날짜를 고르는 자리는 `components/DateTimeField.tsx` 하나다** — 라운드
  `티오프`와 투표 `마감 시각`이 같이 쓴다. 안에서 달력(`DayCal`)을 펴고
  **시·분만 브라우저 칸(`type="time"`)이다**(그 창은 아이폰이 늘 같은
  굴림판으로 띄워 화면마다 안 갈린다. 조 편성의 조별 시각도 같은 칸이다).
  누르는 자리는 `<input>`이 아니라 **`<button>`이고 값은 `data-value`에
  실려 있다** — `.dev/behave.mjs`가 그걸 읽는다.
  **접힌 채로 시작한다** — 모집 열기는 이미 한 화면을 넘기므로 달력을 늘
  펴 두면 저장 단추가 더 멀어진다.
- **`.empty`를 다른 뜻으로 재사용하지 말 것.** 빈 목록 안내에 쓰는
  클래스라 `text-align: center` · 작은 글씨 · `--gap-lg` 여백이 함께 딸려
  온다. `DateTimeField`의 '아직 안 고름'을 `.picked.empty`로 두었다가
  **칸이 44 → 50px로 부풀고 글자가 가운데로 갔다**(헤드리스로 잡았다).
  지금 이름은 `.picked.blank`다. 새 상태 이름을 지을 때는
  `getComputedStyle`로 한 번 재 볼 것 — 눈으로는 '좀 어긋나네' 정도로만 보인다.
- **아이폰은 날짜 칸(`date`·`datetime-local`)을 누르는 순간 값을 오늘로
  정하고 `change`를 던진다.** 그러니 **그 신호로 무엇을 만들지 말 것** —
  사용자가 고르기도 전에 오늘이 들어간다(투표 항목에서 실제로 그랬다).
  **지금 우리 화면에는 그 칸이 없다**(위 `DateTimeField`) — 남은 것은
  `type="time"`뿐이고 거기는 날짜를 안 다룬다. 다시 쓸 일이 생기면 이
  함정부터 떠올릴 것.
- **키보드가 올라와 있는 동안의 매무새는 `lib/keyboard.ts`가 맡는다 —
  대화 말고 모든 화면.** 대화 화면은 제 셈을 따로 들고 있는데(`Chat.tsx`),
  **폼 화면에는 그것이 아예 없었다**(사용자 제보 — `키보드가 탭바 밑으로
  나와서 화면도 이상하고 키보드를 내리고싶어도 내릴수가 없어`).
  - **화면이 바뀌면 탭바를 감추던 표시를 통째로 걷는다**(`HIDES_TABBAR` —
    `kb-typing` · `nc-typing` · `kb-open` · `kb-bar`). 사용자 제보 —
    `뒤로가기하면 가끔 탭바가 사라지는 경우가있어`. 글칸에 초점을 둔 채
    뒤로 가면 **탭바가 사라진 채로 굳었다.** 세 가지가 겹쳤다:
    ① 아래 효과가 `[onChat]`으로 걸려 있어 **폼 화면끼리 오갈 때는 다시
    돌지 않아** 그 뒷정리가 안 불렸고 ② 그래서 기댈 곳이 `focusout`뿐인데
    **웹킷은 초점이 있던 요소가 사라질 때 그걸 안 보내 준다**(크로미움은
    보낸다 — 그래서 헤드리스로는 안 잡히는 자리다) ③ 나머지 셋도 각자
    제 화면에서 걷는데 그 코드가 어긋나면 같은 자국이 남는다.
    **한 곳씩 챙기지 말 것** — 감추는 자리가 넷이라 반드시 하나를 빠뜨리고,
    빠뜨리면 화면이 잠긴 것처럼 보여 원래보다 나쁘다(JTFAG의
    `watchOverlays`와 같은 결이다). 화면을 옮겼다는 것은 **적던 칸이 이미
    사라졌다는 뜻**이라 넷 다 남아 있을 이유가 없다.
    **새로 탭바를 감추는 표시를 만들면 그 목록에 더할 것.**
    - 곁들여 둘을 함께 고쳤다 — 대화 효과의 뒷정리가 `kb-open`만 걷고
      **`kb-bar`를 안 걷고 있었고**, 댓글의 `openBar()`가 `await` **뒤에**
      표시를 세워서 그 사이에 화면을 떠나면 **아무도 안 닫은 채로**
      `nc-typing`이 붙었다(지금은 기다리기 전에 세운다).
  - **탭바를 감춘다**(`body.kb-typing .tabbar`). 앱은 `resize: 'native'`라
    키보드가 올라오면 웹뷰가 줄어드는데, 탭바는 `position: fixed; bottom: 0`
    이라 **줄어든 창의 바닥**(= 키보드 바로 위)에 그대로 얹힌다.
    감춘 만큼 `.page`의 아래 여백도 빼고, `.form-actions`·`.round-actions`는
    `bottom: 0`으로 내려 키보드 바로 위에 붙인다 — 저장 단추가 손에 닿아야 한다.
  - **글칸이 아닌 데를 누르면 초점을 뗀다** = 키보드가 내려간다. 한글 자판에는
    `완료`가 없어 이 길이 없으면 내릴 방법이 아예 없다. 단추·링크·`label`·
    고르는 칸은 **누르는 것 자체가 일이라** 건드리지 않는다(그 자리에서
    초점을 떼면 눌림이 통째로 사라지는 기기가 있다).
    폼 화면의 절반쯤이 이 '내리는 자리'다 — 재서 확인했다.
  - **키보드가 가린 자리에 있는 칸을 끌어 올린다**(`reveal`. 사용자 제보 —
    `댓글쓰는창이 키보드가 가려서 볼수가없어`). 브라우저는 **초점이 갈 때**
    한 번 굴려 주는데 **화면이 줄어드는 것은 그 뒤**라(플러그인이 0.45초
    늦춘다) 그때 다시 굴려 주지는 않는다 — 누를 때는 보이던 칸이 키보드가
    다 올라오고 나면 그 아래로 내려간다. 재 보니 댓글 칸이 보이는 화면보다
    **165px 아래**에 있었다.
    - **줄어들기를 기다리지 않는다 — 줄어들 것을 미리 빼고 지금 굴린다.**
      처음에는 창이 줄어든 뒤에 굴리게 두었는데 **그 0.45초가 그대로 눈에
      보였다**(사용자 제보 — `순간 댓글창이 안보여서 뭐지? 이러면 댓글창이
      보여`). 대화 화면이 `kbHint`로 지난번 키보드 높이를 미리 써먹는 것과
      같은 수이고, **열쇠도 그것과 같이 쓴다**(`teetime:kbh:<가로폭>`) —
      대화는 플러그인이 알려 준 값을, 여기서는 창이 줄어든 만큼을 적어
      서로 배워 준다. **한쪽만 고치지 말 것.** 한 번도 안 겪은 판에서는
      화면의 42%로 잡는다.
    - **굴릴 자리를 먼저 만든다**(`padFor` → `--kb-pad`). **미리 굴리려고
      해도 화면이 줄기 전에는 굴릴 자리가 없다** — 댓글 칸은 글의 맨 아래라
      이미 끝까지 굴러가 있다(재 보니 `scrollTop`이 딱 최대값이었다).
      치는 동안에만 `.page` 아래에 키보드가 가릴 만큼(+`GAP`) 빈자리를
      붙인다. **이미 줄어든 만큼은 뺀다** — 앱은 웹뷰가 줄어 저절로 자리가
      생기지만 **사파리 탭에서는 문서 높이가 안 줄어들어** 이게 없으면
      끝내 못 올라간다. 두 판을 한 셈으로 덮으려고
      `documentElement.clientHeight`로 견준다.
    - **이미 보일 자리면 아무 일도 안 한다** — 사람이 굴려 둔 자리를 안
      빼앗는다. 얼마나 띄울지는 **`GAP`(64px)이 정한다** — 칸만 올리면
      옆의 `등록`이 잘린다. CSS의 `scroll-margin`으로 두면 두 군데가 된다.
    - **여러 번 부른다** — 화면이 한 번에 안 줄어서다(초점이 가는 그 자리에서
      곧바로 · 창이 줄 때마다 · 0.35초와 0.65초 뒤). 헛걸음은 값이 싸다.
    - **부드럽게 굴리지 않는다.** 키보드가 올라오는 그 순간이라 느린
      폰에서는 그대로 끊긴다.
  - **대화 화면에서는 누르기로 안 내리고 끌어 올리지도 않는다**(`onChat`).
    거기는 목록을 눌러 인용으로 뛰거나 반응을 다는 자리가 많고, 입력칸이
    화면에 직접 붙는 데다 초점이 떠나는 것을 150ms 기다렸다 접는 셈까지
    얽혀 있다. 탭바를 감추는 것만 같이 돈다(대화가 쓰던 `kb-open`·`kb-bar`와
    하는 일이 같아 서로 안 다툰다).
  - `.dev/behave.mjs`의 `키보드가 댓글 칸을 안 가린다` 칸이 붙들어 둔다 —
    **헤드리스에 키보드는 없지만 창을 줄이는 것으로 같은 길을 탄다**
    (`resize: 'native'`가 하는 일이 그것뿐이다). **창을 줄이기 전에 이미
    올라와 있는가**와 **줄인 뒤에 다시 안 움직이는가**를 함께 본다 —
    뒤엣것이 곧 `늦게 뜬다`는 느낌의 정체다.
- **스위치는 `components/Switch.tsx` 하나뿐이다** (모양은 global.css의 `.switch`).
  투표 만들기·공지 고정·알림 설정이 같이 쓴다. 같은 이름으로 하나 더 만들었다가
  뒤에 실린 CSS가 이겨 **손잡이가 회색으로 나왔다** — 새로 만들지 말 것.
- **`.btn`은 `white-space: nowrap`이다.** 좁은 줄에 끼면 글자가 세로로 접혀서
  "지우\n기"가 된다. 지우지 말 것.
- 입력칸 글자는 **16px 이상**으로 둔다(`.input`). 그보다 작으면 iOS 사파리가
  입력할 때 화면을 확대한다.
- 무한 애니메이션과 `backdrop-filter`는 쓰지 않는다. 늘 켜져 있는 그리기 비용이
  안드로이드에서 화면을 끊기게 한다. `transform`과 `opacity`만 움직일 것.
- UI 문구는 전부 한국어다. 영어로 바꾸지 말 것.
